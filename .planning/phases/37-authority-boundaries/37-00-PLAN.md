---
phase: 37-authority-boundaries
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/authority-matrix.test.js
  - tests/deduplication.test.js
  - tests/two-wave-detection.test.js
  - tests/mocks/authority-matrix.yaml
  - tests/mocks/sample-findings.json
autonomous: true
requirements:
  - AUTH-01
  - AUTH-05
  - AUTH-03
  - WAVE-01
must_haves:
  truths:
    - "Test suite exists for authority matrix validation"
    - "Test suite exists for deduplication logic"
    - "Test suite exists for two-wave pattern detection"
    - "Test fixtures provide sample data for testing"
  artifacts:
    - path: "tests/authority-matrix.test.js"
      provides: "Authority matrix validation tests"
      min_lines: 50
    - path: "tests/deduplication.test.js"
      provides: "Deduplication logic tests"
      min_lines: 50
    - path: "tests/two-wave-detection.test.js"
      provides: "Two-wave pattern detection tests"
      min_lines: 40
    - path: "tests/mocks/authority-matrix.yaml"
      provides: "Sample authority matrix for testing"
      min_lines: 30
    - path: "tests/mocks/sample-findings.json"
      provides: "Sample review findings for testing"
      min_lines: 50
  key_links:
    - from: "tests/authority-matrix.test.js"
      to: "tests/mocks/authority-matrix.yaml"
      via: "loadFixture()"
      pattern: "loadFixture.*authority-matrix"
    - from: "tests/deduplication.test.js"
      to: "tests/mocks/sample-findings.json"
      via: "loadFixture()"
      pattern: "loadFixture.*sample-findings"
---

<objective>
Create Wave 0 test scaffolding for Phase 37. Establish automated test coverage for authority matrix validation, deduplication logic, and two-wave pattern detection before implementation begins.

Purpose: Provide Nyquist-compliant test infrastructure that validates core requirements (AUTH-01, AUTH-05, AUTH-03, WAVE-01) before implementation code is written. Enables test-driven development and automated verification during execution.
Output: 5 test files with comprehensive coverage of authority boundaries and two-wave parallelism.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-authority-boundaries/37-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create authority matrix test suite</name>
  <files>tests/authority-matrix.test.js, tests/mocks/authority-matrix.yaml</files>
  <action>
Create comprehensive test suite for authority matrix validation at tests/authority-matrix.test.js:

1. Create tests/ directory if it doesn't exist
2. Create tests/mocks/ directory for test fixtures
3. Create tests/mocks/authority-matrix.yaml with sample authority matrix:
   - Define 3-4 sample agents with exclusive domains
   - Include security-engineer (OWASP, STRIDE domains)
   - Include sre-chaos (SLOs, chaos engineering domains)
   - Include code-reviewer (code style, patterns domains)
   - Include frontend-developer (UI/UX, accessibility domains)
   - Show overlap detection scenarios
   - Show valid exclusive domain assignments

4. Create tests/authority-matrix.test.js with test cases:
   - Load and parse YAML authority matrix
   - Validate all agents have exclusive_domains defined
   - Validate no overlapping domains between agents
   - Validate domain format (kebab-case strings)
   - Validate agent references exist in agent registry
   - Test authority lookup by domain
   - Test conflict detection when two agents claim same domain
   - Test matrix serialization/deserialization
   - Use Node.js built-in test runner (node --test) or vitest if project uses it
   - Include before/after hooks for fixture loading

Tests must be runnable with: npm test or node --test
Each test should have descriptive name and clear assertion
</action>
  <verify>
    <automated>test -f tests/authority-matrix.test.js && test -f tests/mocks/authority-matrix.yaml && node --test tests/authority-matrix.test.js 2>/dev/null || echo "Tests created, run manually"</automated>
    <manual>Check that tests/authority-matrix.test.js exists with at least 50 lines and imports the mock fixture</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Authority matrix test suite exists with 50+ lines, validates AUTH-01 and AUTH-05 requirements, includes mock fixture with sample agents and domains</done>
</task>

<task type="auto">
  <name>Task 2: Create deduplication logic test suite</name>
  <files>tests/deduplication.test.js, tests/mocks/sample-findings.json</files>
  <action>
Create comprehensive test suite for review finding deduplication at tests/deduplication.test.js:

1. Create tests/mocks/sample-findings.json with sample review findings:
   - Include 8-10 sample findings with varying severity
   - Include duplicates (same file:line, different messages)
   - Include near-duplicates (same file, different lines)
   - Include unique findings
   - Severity levels: BLOCKER, WARNING, SUGGESTION
   - Finding structure: { file, line, severity, rule, message, agent }
   - Example: security finding on auth.js:45, code style finding on auth.js:45 (same line, different domains)

2. Create tests/deduplication.test.js with test cases:
   - Deduplicate by file:line string key
   - Keep highest severity when duplicates found (BLOCKER > WARNING > SUGGESTION)
   - Preserve unique findings
   - Filter findings by agent domain (keep only findings in agent's exclusive_domains)
   - Test out-of-domain filtering (security finding from code-reviewer should be filtered)
   - Test deduplication with empty findings array
   - Test deduplication with single finding
   - Test severity comparison logic
   - Test domain ownership validation
   - Export deduplication functions for use in review-panel skill
   - Use pure functions with no side effects
   - Include edge cases: null inputs, undefined severities, missing file/line

Implement deduplicateFindings(findings) function that:
- Takes array of finding objects
- Returns deduplicated array (by file:line, highest severity wins)

Implement filterByDomain(findings, agent, authorityMatrix) function that:
- Takes findings array, agent name, and authority matrix
- Returns only findings where finding.rule is in agent's exclusive_domains
</action>
  <verify>
    <automated>test -f tests/deduplication.test.js && test -f tests/mocks/sample-findings.json && node --test tests/deduplication.test.js 2>/dev/null || echo "Tests created, run manually"</automated>
    <manual>Check that tests/deduplication.test.js exists with at least 50 lines and validates deduplication logic</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Deduplication test suite exists with 50+ lines, validates AUTH-03 requirement, includes sample findings fixture with duplicates and varying severity levels</done>
</task>

<task type="auto">
  <name>Task 3: Create two-wave pattern detection test suite</name>
  <files>tests/two-wave-detection.test.js</files>
  <action>
Create test suite for two-wave execution pattern detection at tests/two-wave-detection.test.js:

1. Create tests/two-wave-detection.test.js with test cases:
   - Detect Wave A vs Wave B tasks based on dependencies
   - Identify tasks with no dependencies as Wave A candidates
   - Identify tasks depending on Wave A outputs as Wave B
   - Validate wave boundary gates (Requirements → Architecture → Production)
   - Test parallel execution safety within waves
   - Test dependency graph construction from task metadata
   - Test cycle detection in dependency graph
   - Test wave assignment algorithm
   - Test gate validation (Wave A complete before Wave B starts)
   - Test artifact passing between waves
   - Test dynamic agent spawning detection for Wave A
   - Test remediation phase identification (parallel to validation)

Implement detectWaves(tasks) function that:
- Takes array of task objects with dependencies
- Returns { waveA: [...], waveB: [...], gates: [...] }
- Validates no circular dependencies
- Assigns each task to appropriate wave

Implement validateWaveGates(waveAComplete, waveBStart) function that:
- Validates Wave A completion criteria met
- Validates gates passed before Wave B execution
- Returns boolean or detailed validation result

Include test scenarios:
- Simple linear dependency (A → B)
- Diamond dependency (A → B,C → D)
- Parallel Wave A tasks with no interdependencies
- Wave B tasks all depending on Wave A outputs
- Complex graph with 3+ waves (should detect as error)
</action>
  <verify>
    <automated>test -f tests/two-wave-detection.test.js && node --test tests/two-wave-detection.test.js 2>/dev/null || echo "Tests created, run manually"</automated>
    <manual>Check that tests/two-wave-detection.test.js exists with at least 40 lines and validates wave detection logic</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Two-wave detection test suite exists with 40+ lines, validates WAVE-01 requirement, includes tests for wave assignment, gate validation, and dependency graph construction</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify all 5 test files exist with correct paths
2. Run test suite: npm test or node --test
3. Verify all tests pass (or at minimum, tests are syntactically valid and runnable)
4. Check that mock fixtures provide adequate test data
5. Verify test coverage addresses AUTH-01, AUTH-05, AUTH-03, and WAVE-01 requirements
</verification>

<success_criteria>
- tests/authority-matrix.test.js exists and validates authority matrix YAML structure and domain ownership
- tests/deduplication.test.js exists and validates file:line deduplication and severity prioritization
- tests/two-wave-detection.test.js exists and validates wave detection and gate logic
- tests/mocks/authority-matrix.yaml exists with sample agent domain assignments
- tests/mocks/sample-findings.json exists with sample review findings including duplicates
- All tests are runnable with standard Node.js test runner or project test framework
- Test suite provides Nyquist-compliant automated verification for Phase 37 implementation
</success_criteria>

<output>
After completion, create `.planning/phases/37-authority-boundaries/37-00-SUMMARY.md`

Update phase plans list to include 37-00 as Wave 0 plan:
- Wave 0: 37-00 (Test Scaffolding)
- Wave 1: 37-01, 37-02, 37-03
- Wave 2: 37-04
</output>
