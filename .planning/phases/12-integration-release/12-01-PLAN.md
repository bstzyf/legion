---
phase: 12
plan: 1
title: "Regression Testing & Documentation Updates"
wave: 1
depends_on: []
agents:
  - testing-reality-checker
  - engineering-senior-developer
must_haves:
  - All 29 existing test files pass without regressions
  - CLAUDE.md documents all v6.0 features accurately
  - .gitignore includes node_modules/
  - package.json files field includes all publishable artifacts
files_modified:
  - CLAUDE.md
  - .gitignore
  - package.json
files_forbidden:
  - agents/*.md
  - commands/*.md
  - skills/*/SKILL.md
expected_artifacts:
  - CLAUDE.md (updated with v6.0 feature documentation)
  - .gitignore (updated with node_modules/)
  - package.json (files field audited)
verification_commands:
  - "node --test"
  - "node scripts/release-check.js"
---

<objective>
Run cross-phase regression testing across all 29 test files to verify no regressions from phases 1-11. Update CLAUDE.md to document all v6.0 features. Audit .gitignore and package.json files field for repo hygiene.
</objective>

<context>
- .planning/phases/12-integration-release/12-CONTEXT.md (phase context with full v6.0 feature list)
- CLAUDE.md (current project documentation)
- package.json (current package config)
- .gitignore (current ignore rules)
- tests/*.test.js (all test files)
</context>

<tasks>
## Task 1: Cross-Phase Regression Testing

**Action**: Run the full test suite and fix any regressions.

1. Run `node --test` to execute all 29 test files
2. If any tests fail:
   a. Categorize failures: regression (worked before) vs pre-existing vs environment-specific
   b. Fix regressions — these are the priority
   c. Document any pre-existing issues found
3. If all tests pass: record the clean test run output
4. Run `bash scripts/validate.sh` to verify structural validation passes

**Verify**: `node --test` exits with code 0 (all tests pass)

**Done when**: All 29 test files pass, validation script passes, no regressions remain.

## Task 2: CLAUDE.md Documentation Updates

**Action**: Update CLAUDE.md to accurately reflect all v6.0 features.

1. Read current CLAUDE.md thoroughly
2. Cross-reference against the v6.0 feature list in 12-CONTEXT.md
3. Update the following sections:
   - **Authority Matrix** section: verify control modes table and escalation protocol are documented (added in phases 3, 10)
   - **Wave Handoff Conventions** section: verify agent-to-agent communication is documented (added in phase 10)
   - **Memory Layer** section: verify `task_type` field mention if relevant (added in phase 6)
   - Add any missing v6.0 feature references
4. Verify counts: 12 commands, 25 skills, 53 agents — update if any changed
5. Do NOT alter the structure or style of CLAUDE.md — only add/update factual content

**Verify**: Read CLAUDE.md and confirm all 11 v6.0 phase features are reflected

**Done when**: CLAUDE.md accurately documents all v6.0 capabilities without missing features.

## Task 3: Repo Hygiene Audit

**Action**: Fix .gitignore gaps and audit package.json files field.

1. Add `node_modules/` to `.gitignore` (currently missing)
2. Audit `package.json` `files` field:
   - Current: `bin/`, `agents/`, `commands/`, `skills/`, `adapters/`, `settings.json`, `checksums.sha256`, `docs/security/`, `docs/settings.schema.json`
   - Check if `docs/control-modes.md` should be included (added in phase 3)
   - Check if any other v6.0 docs need inclusion
   - Verify no unnecessary files would be published
3. Run `npm pack --dry-run` to verify the package contents look correct

**Verify**: `grep -q node_modules .gitignore` succeeds; `npm pack --dry-run` shows expected files

**Done when**: .gitignore is complete, package.json files field is accurate, npm pack dry-run shows clean output.
</tasks>

<verification>
```bash
# All tests pass
node --test

# Validation passes
bash scripts/validate.sh

# Release check passes
node scripts/release-check.js

# .gitignore includes node_modules
grep -q "node_modules" .gitignore && echo "PASS" || echo "FAIL"

# npm pack shows expected contents
npm pack --dry-run 2>&1 | head -30
```
</verification>

<success_criteria>
- All 29 test files pass with zero failures
- CLAUDE.md documents all v6.0 features from phases 1-11
- .gitignore includes node_modules/
- package.json files field verified and corrected if needed
- validate.sh and release-check.js both pass
</success_criteria>

<output>
Produce `.planning/phases/12-integration-release/12-01-SUMMARY.md` with:
- Test results (pass/fail counts, any regressions found and fixed)
- CLAUDE.md changes made
- Repo hygiene changes made
- Verification command outputs
- Handoff context for Plan 12-02
</output>
