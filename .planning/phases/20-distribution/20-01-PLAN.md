---
phase: 20
plan: 1
title: "Create marketplace entry, rewrite README, produce CHANGELOG and CONTRIBUTING"
wave: 1
depends_on: []
autonomous: true
files_modified:
  - .claude-plugin/marketplace.json
  - README.md
  - CHANGELOG.md
  - CONTRIBUTING.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
  - DIST-04
must_haves:
  truths:
    - ".claude-plugin/marketplace.json is valid JSON with $schema, name, owner, and plugins array containing the agency-workflows plugin entry (DIST-01)"
    - "README.md has installation instructions for both `claude plugin marketplace add` + `plugin install` and `--plugin-dir`, lists prerequisites, and includes a getting started guide covering the /agency:start → plan → build → review workflow (DIST-02)"
    - "CHANGELOG.md documents v1.0 (14 phases, 54 requirements, initial release) and v2.0 (plugin conversion, advisory capabilities) in Keep a Changelog format (DIST-03)"
    - "CONTRIBUTING.md explains local testing with `--plugin-dir`, plugin component structure, and how to add agents/skills/commands (DIST-04)"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace entry enabling `claude plugin marketplace add dasbl/agency-agents`"
      contains: "agency-workflows"
    - path: "README.md"
      provides: "Installation guide, feature overview, and getting started documentation for the plugin"
      contains: "plugin marketplace add"
    - path: "CHANGELOG.md"
      provides: "Version history documenting v1.0 and v2.0 milestones"
      contains: "2.0.0"
    - path: "CONTRIBUTING.md"
      provides: "Developer guide for testing and extending the plugin locally"
      contains: "--plugin-dir"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: ".claude-plugin/plugin.json"
      via: "marketplace.json lists the plugin; plugin.json provides the manifest metadata"
      pattern: "marketplace discovery"
    - from: "README.md"
      to: ".claude-plugin/marketplace.json"
      via: "README references the installation command that triggers marketplace lookup"
      pattern: "documentation"
    - from: "CONTRIBUTING.md"
      to: "README.md"
      via: "README links to CONTRIBUTING.md for development setup"
      pattern: "documentation"
---

<objective>
Create the four distribution artifacts that make the Agency Workflows plugin installable, discoverable, and contributor-friendly.

Purpose: With the plugin structure complete (phases 15-19), this phase wraps it for distribution — a marketplace entry so `claude plugin marketplace add` discovers it, a README so users know what it does and how to start, a changelog so users can track what shipped, and a contributing guide so developers can test and extend locally.
Output: marketplace.json, README.md (rewritten), CHANGELOG.md (new), CONTRIBUTING.md (new)
</objective>

<context>
@.planning/phases/20-distribution/20-CONTEXT.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v1.0-ROADMAP.md
@.claude-plugin/plugin.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketplace.json</name>
  <files>.claude-plugin/marketplace.json</files>
  <action>
Create `.claude-plugin/marketplace.json` following the official Anthropic marketplace schema.

Write exactly this content:

```json
{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "agency-workflows",
  "description": "Orchestrate 51 AI specialist personalities as coordinated teams — project initialization, phase planning, parallel execution, quality review, and portfolio management",
  "owner": {
    "name": "dasbl",
    "url": "https://github.com/dasbl"
  },
  "plugins": [
    {
      "name": "agency-workflows",
      "description": "Orchestrate 51 AI specialist personalities as coordinated teams — project initialization, phase planning, parallel execution, quality review, and portfolio management",
      "version": "2.0.0",
      "author": {
        "name": "dasbl",
        "url": "https://github.com/dasbl"
      },
      "source": ".",
      "category": "productivity",
      "homepage": "https://github.com/dasbl/agency-agents"
    }
  ]
}
```

Field rationale:
- **$schema**: Official Anthropic marketplace schema URL — enables validation
- **name**: `agency-workflows` — matches plugin.json name
- **description**: Same as plugin.json — consistent messaging
- **owner**: GitHub username and profile URL
- **plugins[0].source**: `"."` — the plugin IS this repo (not a subdirectory)
- **plugins[0].category**: `productivity` — Agency orchestrates workflows, not a language tool or testing framework
- **plugins[0].homepage**: Links back to the GitHub repo for discovery

IMPORTANT: This file goes INSIDE `.claude-plugin/` alongside `plugin.json`. The `.claude-plugin/` directory now contains exactly 2 files.
  </action>
  <verify>
Verify marketplace.json is valid JSON with all required fields:
```bash
cat .claude-plugin/marketplace.json | python3 -c "
import sys, json
d = json.load(sys.stdin)
assert '\$schema' in d, 'Missing schema'
assert d['name'] == 'agency-workflows', 'Wrong name'
assert 'owner' in d, 'Missing owner'
assert 'plugins' in d and len(d['plugins']) == 1, 'Should have exactly 1 plugin'
p = d['plugins'][0]
assert p['source'] == '.', 'Source should be .'
assert p['category'] == 'productivity', 'Wrong category'
print('Valid marketplace.json with all fields')
"

# Verify .claude-plugin/ has exactly 2 files
ls .claude-plugin/ | wc -l | xargs -I{} test {} -eq 2 && echo "PASS: 2 files in .claude-plugin/" || echo "FAIL: unexpected file count"
```
  </verify>
  <done>`.claude-plugin/marketplace.json` exists with valid schema, owner, and single plugin entry pointing to repo root. `claude plugin marketplace add dasbl/agency-agents` can now discover the plugin.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md for v2.0 plugin format</name>
  <files>README.md</files>
  <action>
Rewrite `README.md` to reflect the v2.0 plugin structure. Preserve the "Standing on the Shoulders of Giants" section (valuable attribution content) but update everything else.

The new README must include these sections in order:

**1. Title and tagline** (keep existing)
```
# The Agency Workflows

Orchestrate 51 AI specialist personalities as coordinated teams in Claude Code.
```

**2. What It Does** (keep existing, it's accurate)

**3. Installation** (NEW — replace old Quick Start)
```
## Installation

### From GitHub (recommended)

\```bash
# Step 1: Add the marketplace
claude plugin marketplace add dasbl/agency-agents

# Step 2: Install the plugin
claude plugin install agency-workflows@agency-workflows
\```

Or from inside the Claude Code TUI:
\```
/plugin marketplace add dasbl/agency-agents
/plugin install agency-workflows@agency-workflows
\```

### Local development

\```bash
git clone https://github.com/dasbl/agency-agents.git
claude --plugin-dir ./agency-agents
\```

### Prerequisites

- [Claude Code](https://docs.anthropic.com/en/docs/claude-code) CLI installed
- No additional dependencies — pure Claude Code primitives
```

**4. Getting Started** (NEW — replaces old Quick Start with more detail)
```
## Getting Started

1. Install the plugin (see above)
2. In any project directory, run `/agency:start`
3. Answer the guided questions — the system explores your vision before jumping to implementation
4. Review the generated PROJECT.md and ROADMAP.md
5. Run `/agency:plan 1` to plan the first phase with agent recommendations
6. Run `/agency:build` to execute with parallel agent teams
7. Run `/agency:review` for quality review
8. Repeat `/agency:plan N` → `/agency:build` → `/agency:review` for each phase
```

**5. Commands** (keep existing table — it's accurate and up to date)

**6. How It Works** (keep existing flow diagram — it's accurate)

**7. The 51 Agents** (update table — keep content but fix the roster link)
- Change `agency-agents/README.md` link to point to `agents/` directory
- Or remove the link if no agents/README.md exists

**8. Architecture** (REWRITE — must show new plugin structure)
```
## Architecture

\```
agency-agents/              ← Plugin root
├── .claude-plugin/
│   ├── plugin.json         ← Plugin manifest (name, version, author, keywords)
│   └── marketplace.json    ← Marketplace entry for `claude plugin marketplace add`
├── settings.json           ← Plugin settings (empty — multi-agent, not single-agent)
├── CLAUDE.md               ← Project instructions (injected into Claude Code context)
├── commands/               ← 9 /agency: command entry points
│   ├── start.md
│   ├── plan.md
│   ├── build.md
│   ├── review.md
│   ├── status.md
│   ├── quick.md
│   ├── portfolio.md
│   ├── milestone.md
│   └── agent.md
├── skills/                 ← 15 reusable workflow skills
│   ├── workflow-common/SKILL.md     ← Shared constants and conventions
│   ├── agent-registry/SKILL.md     ← 51 agent catalog + recommendation
│   ├── questioning-flow/SKILL.md   ← 3-stage adaptive conversation
│   ├── phase-decomposer/SKILL.md   ← Phase decomposition with domain detection
│   ├── wave-executor/SKILL.md      ← Parallel execution with personality injection
│   ├── execution-tracker/SKILL.md  ← Progress tracking + atomic commits
│   ├── review-loop/SKILL.md        ← Dev-QA loop with structured feedback
│   └── + 8 more (portfolio, milestone, memory, agents, GitHub, brownfield, marketing, design)
├── agents/                 ← 51 personality .md files (flat, with division in frontmatter)
│   ├── engineering-senior-developer.md
│   ├── design-ui-designer.md
│   ├── marketing-content-creator.md
│   ├── testing-reality-checker.md
│   └── ... (47 more)
└── .planning/              ← Project state (generated per-project, not part of plugin)
    ├── PROJECT.md
    ├── ROADMAP.md
    ├── STATE.md
    └── phases/
\```
```

**9. Standing on the Shoulders of Giants** (PRESERVE — keep lines 44-129 from old README exactly as-is, including all subsections about GSD, Conductor, Shipyard, Best Practice, Daem0n)

**10. Design Principles** (keep existing — it's accurate)

**11. Optional Features** (keep existing table — it's accurate)

**12. Requirements** (update)
```
## Requirements

- [Claude Code](https://docs.anthropic.com/en/docs/claude-code) CLI
- No additional dependencies
```

**13. Contributing** (update link)
```
## Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md) for development setup and agent design guidelines.
```

**14. License** (NEW)
```
## License

MIT
```

IMPORTANT:
- The "Standing on Shoulders" section (including all 5 sub-sections and the comparison table) MUST be preserved word-for-word. This is the most valuable unique content in the README.
- Remove the old "Architecture" section that references `.claude/commands/agency/` and `agency-agents/{division}/` — those paths no longer exist.
- Remove the old "Quick Start" section that says "Copy this repository" — that's not how plugins work.
- All file paths in the README must reference the current plugin structure (`commands/`, `skills/`, `agents/`).
  </action>
  <verify>
Verify README has all required sections and no stale paths:
```bash
# Check required sections exist
for section in "Installation" "Getting Started" "Commands" "Architecture" "Standing on the Shoulders" "Design Principles" "Contributing" "License"; do
  grep -q "## $section" README.md && echo "PASS: $section section exists" || echo "FAIL: missing $section"
done

# Check installation commands are present
grep -q "plugin marketplace add" README.md && echo "PASS: marketplace add command" || echo "FAIL: missing marketplace add command"
grep -q "plugin install agency-workflows" README.md && echo "PASS: plugin install command" || echo "FAIL: missing plugin install command"
grep -q "\-\-plugin-dir" README.md && echo "PASS: plugin-dir flag" || echo "FAIL: missing plugin-dir"

# Check no stale paths remain
grep -q "\.claude/commands/agency" README.md && echo "FAIL: stale .claude/commands/ path" || echo "PASS: no stale command paths"
grep -q "\.claude/skills/agency" README.md && echo "FAIL: stale .claude/skills/ path" || echo "PASS: no stale skill paths"
grep -q "agency-agents/" README.md && echo "WARN: check agency-agents/ references — may be stale" || echo "PASS: no agency-agents/ refs"

# Check architecture shows plugin structure
grep -q "\.claude-plugin/" README.md && echo "PASS: shows .claude-plugin/" || echo "FAIL: missing .claude-plugin/ in architecture"
grep -q "marketplace.json" README.md && echo "PASS: mentions marketplace.json" || echo "FAIL: missing marketplace.json"
```
  </verify>
  <done>README.md rewritten for v2.0 plugin format. Installation section covers both `plugin marketplace add` + `plugin install` and `--plugin-dir`. Architecture shows current directory layout. "Standing on Shoulders" section preserved. No stale paths.</done>
</task>

<task type="auto">
  <name>Task 3: Create CHANGELOG.md, CONTRIBUTING.md, and update project state</name>
  <files>CHANGELOG.md, CONTRIBUTING.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <action>

### Part A: Create CHANGELOG.md

Create `CHANGELOG.md` at the project root in [Keep a Changelog](https://keepachangelog.com/) format:

```markdown
# Changelog

All notable changes to The Agency Workflows plugin are documented here.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
This project uses [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [2.0.0] - 2026-03-02

### Changed
- Converted from `.claude/` directory config to proper Claude Code plugin format
- Moved 9 commands from `.claude/commands/agency/` to `commands/` at plugin root
- Converted 15 skills to `skills/{name}/SKILL.md` directory structure with co-located templates
- Migrated 51 agent files from `agency-agents/{division}/` to flat `agents/` directory
- Updated all internal path references to plugin-relative format
- Agent frontmatter now includes `name`, `description`, and `division` fields

### Added
- `.claude-plugin/plugin.json` — plugin manifest for Claude Code recognition
- `.claude-plugin/marketplace.json` — marketplace entry for `claude plugin marketplace add`
- `settings.json` — plugin settings file (empty — multi-agent system, not single-agent)
- Agent registry updated with plugin-relative paths for all 51 agents
- Installation via `claude plugin marketplace add dasbl/agency-agents` + `claude plugin install agency-workflows@agency-workflows`
- Local development testing via `claude --plugin-dir .`

### Removed
- `.claude/commands/agency/` directory (commands migrated to `commands/`)
- `.claude/skills/agency/` directory (skills migrated to `skills/`)
- `agency-agents/` directory (agents migrated to `agents/`)
- `.planning/templates/` directory (templates co-located with questioning-flow skill)

## [1.0.0] - 2026-03-01

### Added
- **9 commands**: `/agency:start`, `/agency:plan`, `/agency:build`, `/agency:review`, `/agency:status`, `/agency:quick`, `/agency:portfolio`, `/agency:milestone`, `/agency:agent`
- **15 skills** (9,196 lines): workflow-common, agent-registry, questioning-flow, phase-decomposer, wave-executor, execution-tracker, review-loop, portfolio-manager, milestone-tracker, memory-manager, agent-creator, github-sync, codebase-mapper, marketing-workflows, design-workflows
- **51 agent personalities** across 9 divisions: Engineering (7), Design (6), Marketing (8), Testing (7), Product (3), Project Management (5), Support (6), Spatial Computing (6), Specialized (3)
- Full personality injection — agents spawned with complete .md as system instructions
- Wave-based parallel execution with max 3 tasks per plan
- Hybrid agent selection — workflow recommends, user confirms
- Dev-QA review loop with max 3 cycles before escalation
- Cross-session memory with importance scoring and time-based decay
- GitHub integration (issues, PRs, milestones) — opt-in, graceful degradation
- Brownfield codebase analysis — architecture mapping before planning
- Marketing workflows — campaign planning, content calendars, channel coordination
- Design workflows — design systems, UX research, three-lens review (brand, accessibility, usability)
- Multi-project portfolio management
- Milestone tracking and archiving
```

### Part B: Create CONTRIBUTING.md

Create `CONTRIBUTING.md` at the project root:

```markdown
# Contributing to The Agency Workflows

## Development Setup

### Prerequisites

- [Claude Code](https://docs.anthropic.com/en/docs/claude-code) CLI installed

### Local Testing

Clone the repository and load it as a local plugin:

\```bash
git clone https://github.com/dasbl/agency-agents.git
cd agency-agents
claude --plugin-dir .
\```

As you make changes, restart Claude Code to pick up updates. You can test:
- Commands with `/agency:start`, `/agency:plan 1`, etc.
- Skills are loaded automatically from `skills/{name}/SKILL.md`
- Agents appear in the registry via `skills/agent-registry/SKILL.md`

### Loading Multiple Plugins

If you're developing alongside other plugins:

\```bash
claude --plugin-dir ./agency-agents --plugin-dir ./other-plugin
\```

## Plugin Structure

\```
commands/           9 command entry points (/agency:*)
skills/             15 workflow skills (SKILL.md format)
agents/             51 agent personality files (.md)
.claude-plugin/     Plugin manifest + marketplace entry
settings.json       Plugin settings
CLAUDE.md           Project-level instructions
\```

### Commands (`commands/`)

Each command is a `.md` file that serves as an entry point for an `/agency:*` slash command. Commands orchestrate skills and spawn agents — they don't contain business logic directly.

### Skills (`skills/{name}/SKILL.md`)

Skills are reusable workflow components. Each skill lives in a directory with a `SKILL.md` file and optional supporting files (templates, references). Skills have `name` and `description` frontmatter.

### Agents (`agents/{name}.md`)

Agent personality files define how each AI specialist behaves when spawned. Each file has YAML frontmatter with `name`, `description`, and `division` fields, followed by the full personality description including expertise, communication style, hard rules, and quirks.

## Adding a New Agent

1. Create `agents/{division}-{name}.md` with frontmatter:
   \```yaml
   ---
   name: Your Agent Name
   description: One-line description of what this agent does
   division: Division Name
   ---
   \```
2. Write the personality (80-120 lines recommended): expertise, style, rules, quirks
3. Add the agent to the catalog in `skills/agent-registry/SKILL.md`
4. Test with `/agency:quick "task for your agent"`

## Adding a New Skill

1. Create `skills/{skill-name}/SKILL.md` with frontmatter:
   \```yaml
   ---
   name: skill-name
   description: What this skill does and when to use it
   ---
   \```
2. Write the skill logic following existing skill patterns
3. Reference from commands via `<execution_context>`

## Adding a New Command

1. Create `commands/{name}.md`
2. Include `<execution_context>` declarations for any skills the command uses
3. Follow the existing command pattern: objective → context → step-by-step process

## Code Style

- **Markdown only** — no custom tooling, no scripts, no build steps
- **Max 3 tasks per plan** — keeps work focused and reviewable
- **Full personality injection** — agent files are complete, not fragments
- **Human-readable state** — `.planning/` files are plain markdown
```

### Part C: Update project state

1. Update `.planning/ROADMAP.md`:
   - Phase 20 details: set Plans to "1"
   - Add plan entry: `Plan 20-01 (Wave 1): Create marketplace entry, rewrite README, produce CHANGELOG and CONTRIBUTING`
   - Progress table: Phase 20 row → "1/1", "Planned"

2. Update `.planning/REQUIREMENTS.md`:
   - Keep DIST-01 through DIST-04 as unchecked (they'll be checked after execution, not during planning)
   - No changes needed — traceability is already correct

3. Update `.planning/STATE.md`:
   - Current Position: Phase 20 (Distribution) — Planned
   - Plan: 20-01 (planned, 3 tasks)
   - Status: Phase 20 planned — 1 plan, 1 wave, 4 files to create/rewrite
   - Last activity: Phase 20 planning
   - Next Steps: Run `/gsd:execute-phase 20` to execute
  </action>
  <verify>
```bash
# CHANGELOG exists and has both versions
grep -q "## \[2.0.0\]" CHANGELOG.md && echo "PASS: v2.0 section" || echo "FAIL: missing v2.0"
grep -q "## \[1.0.0\]" CHANGELOG.md && echo "PASS: v1.0 section" || echo "FAIL: missing v1.0"

# CONTRIBUTING exists and has dev testing section
grep -q "plugin-dir" CONTRIBUTING.md && echo "PASS: CONTRIBUTING has plugin-dir" || echo "FAIL: missing plugin-dir"
grep -q "Development Setup" CONTRIBUTING.md && echo "PASS: has dev setup" || echo "FAIL: missing dev setup"

# ROADMAP updated
grep -q "Plan 20-01" .planning/ROADMAP.md && echo "PASS: ROADMAP has plan 20-01" || echo "FAIL"

# STATE updated
grep -q "Phase 20.*Planned\|Phase: 20" .planning/STATE.md && echo "PASS: STATE references Phase 20" || echo "FAIL"
```
  </verify>
  <done>CHANGELOG.md created with v1.0 and v2.0 entries. CONTRIBUTING.md created with dev testing guide. ROADMAP.md, STATE.md updated to reflect Phase 20 planning complete.</done>
</task>

</tasks>

<verification>
Run ALL verification checks in sequence:

```bash
echo "=== Task 1: marketplace.json ==="
test -f .claude-plugin/marketplace.json && echo "PASS: file exists" || echo "FAIL: missing"
cat .claude-plugin/marketplace.json | python3 -c "
import sys, json
d = json.load(sys.stdin)
checks = [
  ('\$schema' in d, 'has schema'),
  (d.get('name') == 'agency-workflows', 'correct name'),
  ('owner' in d, 'has owner'),
  (len(d.get('plugins', [])) == 1, 'has 1 plugin'),
  (d['plugins'][0]['source'] == '.', 'source is .'),
  (d['plugins'][0]['category'] == 'productivity', 'category is productivity'),
]
for ok, msg in checks:
  print(f\"{'PASS' if ok else 'FAIL'}: {msg}\")
"

echo ""
echo "=== Task 2: README.md ==="
for section in "Installation" "Getting Started" "Commands" "Architecture" "Standing on the Shoulders" "Design Principles" "Contributing" "License"; do
  grep -q "## $section" README.md && echo "PASS: $section" || echo "FAIL: $section"
done
grep -q "plugin marketplace add" README.md && echo "PASS: marketplace add cmd" || echo "FAIL: marketplace add cmd"
grep -q "\-\-plugin-dir" README.md && echo "PASS: plugin-dir" || echo "FAIL: plugin-dir"
grep -q "\.claude/commands/agency" README.md && echo "FAIL: stale path" || echo "PASS: no stale paths"

echo ""
echo "=== Task 3: CHANGELOG + CONTRIBUTING + State ==="
test -f CHANGELOG.md && echo "PASS: CHANGELOG exists" || echo "FAIL: missing"
test -f CONTRIBUTING.md && echo "PASS: CONTRIBUTING exists" || echo "FAIL: missing"
grep -q "Plan 20-01" .planning/ROADMAP.md && echo "PASS: ROADMAP" || echo "FAIL: ROADMAP"
grep -q "Phase.*20" .planning/STATE.md && echo "PASS: STATE" || echo "FAIL: STATE"
```
</verification>

<success_criteria>
1. `.claude-plugin/marketplace.json` is valid JSON matching Anthropic's schema with agency-workflows plugin entry (DIST-01)
2. `README.md` has Installation (marketplace add + plugin install + --plugin-dir), Getting Started, Commands, Architecture sections with current paths and no stale references (DIST-02)
3. `CHANGELOG.md` documents both v1.0 (initial release) and v2.0 (plugin conversion) milestones (DIST-03)
4. `CONTRIBUTING.md` explains `--plugin-dir` testing, plugin structure, and how to add agents/skills/commands (DIST-04)
5. ROADMAP.md and STATE.md updated to reflect Phase 20 planning
</success_criteria>

<output>
Phase 20 planned. Results:
- Plan 20-01: 1 wave, 3 tasks, 4 distribution artifacts
- Files to create: marketplace.json, CHANGELOG.md, CONTRIBUTING.md
- Files to rewrite: README.md
- Files to update: ROADMAP.md, STATE.md
- Requirements covered: DIST-01, DIST-02, DIST-03, DIST-04
- Next: Run `/gsd:execute-phase 20` to execute
</output>
