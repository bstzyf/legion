---
phase: 36-polymath-integration
plan: 03
type: execute
wave: 2
depends_on: [36-01, 36-02]
files_modified:
  - commands/start.md
  - skills/workflow-common/SKILL.md
  - skills/agent-registry/CATALOG.md
autonomous: true
requirements: [POLY-06]

must_haves:
  truths:
    - "/legion:start asks user: 'Explore first with Polymath, or jump straight to planning?'"
    - "User can select exploration option before PROJECT.md creation begins"
    - "If user chooses exploration, /legion:explore is invoked with context"
    - "After exploration completes, user can return to /legion:start seamlessly"
    - "Start command workflow documents the exploration integration point"
  artifacts:
    - path: "commands/start.md"
      provides: "Updated start command with exploration option"
      contains: "Explore first with Polymath"
    - path: "skills/workflow-common/SKILL.md"
      provides: "Command-to-skill mapping updated for explore command"
      contains: "explore"
  key_links:
    - from: "commands/start.md"
      to: "commands/explore.md"
      via: "start command offers exploration option"
      pattern: "legion:explore"
    - from: "commands/explore.md"
      to: "commands/start.md"
      via: "exploration can transition to start"
      pattern: "legion:start"
---

<objective>
Integrate the exploration workflow into `/legion:start`. When users initialize a project, offer them the option to explore and crystallize their idea with Polymath before formal planning begins.

Purpose: Ensure users have the opportunity to clarify vague ideas before committing to structured planning. Makes exploration a first-class citizen in the Legion workflow, not an afterthought.

Output: Updated `commands/start.md`, updated `skills/workflow-common/SKILL.md`
</objective>

<execution_context>
@./.config/opencode/get-shit-done/workflows/execute-plan.md
@./.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Current files to modify:
@commands/start.md — the command to update with exploration option
@commands/explore.md — for cross-reference on transition flow
@skills/workflow-common/SKILL.md — for command-to-skill mapping

Key integration points:
- After pre-flight check (step 1 in start.md)
- Before brownfield detection (step 2) OR integrated into it
- Must preserve all existing start.md functionality
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update /legion:start with exploration option</name>
  <files>commands/start.md</files>
  <action>
Update `commands/start.md` to add exploration option at the appropriate point in the workflow.

**Current workflow (steps 1-3):**
1. Pre-flight check (existing project detection)
2. Brownfield detection (existing codebase scan)
3. Ensure directory structure

**New workflow insert between steps 1 and 2:**

Add as new Step 2 (shift current steps 2+ to 3+):

```markdown
2. EXPLORATION OFFER
   After pre-flight check passes (no existing project OR user confirmed reinitialize):
   
   Use adapter.ask_user with structured choice:
   
   "Before we create your project plan, would you like to explore and clarify your idea first?"
   
   Options:
   - **"Yes, explore with Polymath"** (Recommended for new ideas)
     → Display: "Polymath will guide you through structured exploration to crystallize your concept before planning."
     → Invoke `/legion:explore` workflow
     → After exploration completes and user selects "Proceed to planning":
       - Capture the crystallized concept from exploration output
       - Pre-populate Stage 1 (Vision & Identity) questioning with the crystallized summary
       - Skip the opening "What are you building?" prompt
       - Continue to brownfield detection (new step 3)
     → After exploration completes and user selects "Explore more" or "Park":
       - Respect the user's decision
       - If "Explore more": loop back to exploration with narrowed scope
       - If "Park": exit with summary saved to `.planning/exploration-{timestamp}.md`
   
   - **"No, jump straight to planning"** (Best if you already have a clear concept)
     → Skip exploration
     → Continue directly to brownfield detection (new step 3)
     → Proceed with standard questioning flow
   
   Default selection: "Yes, explore with Polymath" (first option)
```

**Update downstream step references:**
- Current step 2 (Brownfield detection) → becomes step 3
- Current step 3 (Ensure directory structure) → becomes step 4
- Current steps 4-11 → become steps 5-12

**Update brownfield detection integration:**
In the new step 3 (brownfield detection), add:
```markdown
If exploration was completed (user chose "Yes, explore with Polymath"):
- Display: "Exploration summary: [crystallized summary]"
- Use this context during brownfield analysis
- If existing codebase detected, ask: "Build on the existing code to realize your explored concept, or start fresh?"
```

**Update Stage 1 questioning (now step 5):**
```markdown
If exploration was completed:
- Skip: "What are you building? Give me the elevator pitch."
- Instead open with: "Here's what we crystallized in exploration: [summary]. Let's confirm the details."
- Pre-populate: project_name, project_description, value_proposition from exploration
- Focus questioning on what's still missing or needs confirmation

If no exploration (user chose "No, jump straight to planning"):
- Keep existing Stage 1 flow unchanged
```

**Add to decision_matrix section:**
```markdown
| Exploration completed | Use crystallized summary in Stage 1 | Pre-populate questioning |
| User skipped exploration | Run standard Stage 1 questioning | No changes |
| Exploration parked | Exit start command, preserve exploration file | User can re-run start later |
```

**Update success_criteria if present:**
Add criterion:
- User is offered exploration option before formal planning begins
  </action>
  <verify>
    1. `grep -c "Explore first with Polymath" commands/start.md | xargs -I{} test {} -ge 1 && echo "Exploration option present" || echo "MISSING EXPLORATION OPTION"`
    2. `grep -c "legion:explore" commands/start.md | xargs -I{} test {} -ge 1 && echo "References explore command" || echo "MISSING EXPLORE REFERENCE"`
    3. `grep -c "crystallized" commands/start.md | xargs -I{} test {} -ge 1 && echo "Crystallization concept integrated" || echo "MISSING CRYSTALLIZATION"`
    4. `grep -c "exploration-{timestamp}" commands/start.md | xargs -I{} test {} -ge 1 && echo "Exploration output path referenced" || echo "MISSING OUTPUT PATH"`
    5. Verify step numbering is consistent: `grep "^[0-9]\\+\." commands/start.md | head -5` should show sequential steps
  </verify>
  <done>Start command updated with exploration option as step 2, proper step renumbering, integration with crystallized output, and seamless transition flows for proceed/park decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Update workflow-common command mappings</name>
  <files>skills/workflow-common/SKILL.md</files>
  <action>
Update `skills/workflow-common/SKILL.md` to register the explore command and its relationships.

**Update 1: Add /legion:explore to Command-to-Skill Mapping**

Find the Command-to-Skill Mapping table. Add a new row:

```markdown
| /legion:explore | polymath-engine, questioning-flow | agent-registry | Entry point for pre-flight exploration. Spawns Polymath agent. Research-first workflow. |
```

Insert it alphabetically (after `/legion:build`, before `/legion:milestone`).

**Update 2: Add State File Location for exploration outputs**

Find the State File Locations table. Add a new row:

```markdown
| Exploration Documents | `.planning/exploration-{timestamp}.md` | Crystallized exploration outputs from `/legion:explore` sessions |
```

Insert it alphabetically (after "Design Documents", before "Memory Files").

**Update 3: Add cross-reference note**

In the Command-to-Skill Mapping section, add a note:

```markdown
**Command Relationships:**
- `/legion:start` conditionally invokes `/legion:explore` before formal planning
- `/legion:explore` can transition to `/legion:start` when user selects "Proceed to planning"
- This forms a two-phase entry: exploration (optional) → initialization (required)
```

**Update 4: Add Polymath to Agent Registry reference**

In the Agent Registry section (if present), or add a new section:

```markdown
### Pre-Flight Alignment Agent

**Polymath** (`agents/polymath.md`)
- **Role:** Pre-flight alignment specialist
- **Invoked by:** `/legion:explore` command
- **Purpose:** Crystallize raw ideas before formal planning
- **Key behaviors:** Research-first workflow, structured choices only (no open-ended questions), 7-exchange limit
- **Division:** Specialized
```
  </action>
  <verify>
    1. `grep -c "/legion:explore" skills/workflow-common/SKILL.md | xargs -I{} test {} -ge 1 && echo "Explore command in mapping" || echo "MISSING EXPLORE COMMAND"`
    2. `grep -c "polymath-engine" skills/workflow-common/SKILL.md | xargs -I{} test {} -ge 1 && echo "Polymath engine referenced" || echo "MISSING POLYMATH ENGINE"`
    3. `grep -c "exploration-{timestamp}" skills/workflow-common/SKILL.md | xargs -I{} test {} -ge 1 && echo "Exploration documents in state files" || echo "MISSING STATE FILE ENTRY"`
    4. `grep -c "Command Relationships" skills/workflow-common/SKILL.md | xargs -I{} test {} -ge 1 && echo "Command relationships documented" || echo "MISSING RELATIONSHIPS"`
  </verify>
  <done>workflow-common updated with explore command in Command-to-Skill Mapping, exploration documents in State File Locations, command relationship documentation, and Polymath agent reference.</done>
</task>

<task type="auto">
  <name>Task 3: Update agent registry catalog</name>
  <files>skills/agent-registry/CATALOG.md</files>
  <action>
Ensure `skills/agent-registry/CATALOG.md` properly documents the Polymath agent and its task types.

**Check for existing entry:**
The CATALOG.md should already have a polymath entry from Plan 36-01. Verify it exists and is complete.

**If entry exists, verify it includes:**
- Row in Specialized Division table
- Task types: exploration, clarification, research-first, structured-questions, gap-detection

**If entry missing or incomplete, add/update:**

In the `### Specialized Division` table, ensure this row exists:

```markdown
| polymath | `agents/polymath.md` | Pre-flight alignment specialist who crystallizes raw ideas through structured exploration | exploration, clarification, research-first, structured-questions, gap-detection |
```

**Add exploration tag to task type index (if present):**

If CATALOG.md has a task type index or cross-reference section, add:
```markdown
**exploration**: polymath
**clarification**: polymath
**research-first**: polymath
**structured-questions**: polymath
**gap-detection**: polymath
```

**Note:** This is verification/fallback — Plan 36-01 should have already added this. If present, this task is a no-op verification.
  </action>
  <verify>
    1. `grep -c "polymath" skills/agent-registry/CATALOG.md | xargs -I{} test {} -ge 1 && echo "Polymath in catalog" || echo "POLYMATH MISSING FROM CATALOG"`
    2. `grep -c "exploration" skills/agent-registry/CATALOG.md | xargs -I{} test {} -ge 1 && echo "Exploration task type present" || echo "MISSING EXPLORATION TAG"`
    3. `grep -c "gap-detection" skills/agent-registry/CATALOG.md | xargs -I{} test {} -ge 1 && echo "Gap detection task type present" || echo "MISSING GAP-DETECTION TAG"`
  </verify>
  <done>Agent registry catalog verified to include Polymath agent with complete task-type tags (exploration, clarification, research-first, structured-questions, gap-detection).</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `commands/start.md` contains "Explore first with Polymath, or jump straight to planning?" question
- [ ] Start command properly renumbered (steps 2-12 updated to account for exploration insert)
- [ ] Start command handles exploration proceed/park transitions correctly
- [ ] Stage 1 questioning updated to use crystallized summary when exploration completed
- [ ] `skills/workflow-common/SKILL.md` has /legion:explore in Command-to-Skill Mapping
- [ ] workflow-common has exploration documents in State File Locations
- [ ] workflow-common documents command relationships (explore ↔ start)
- [ ] `skills/agent-registry/CATALOG.md` has polymath entry with correct task types
</verification>

<success_criteria>
- /legion:start offers exploration option before formal planning (POLY-06 satisfied)
- User can select "Explore first with Polymath" with clear indication of what it does
- Seamless transition from exploration to start when user proceeds
- Proper state preservation when user parks
- All command and skill registries updated with exploration workflow
- Complete integration between explore and start commands
</success_criteria>

<output>
After completion, create `.planning/phases/36-polymath-integration/36-03-SUMMARY.md`

Update STATE.md: Phase 36 planning complete, 3 plans created.
</output>
