---
phase: 35-consolidation-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/workflow-common/SKILL.md
  - skills/memory-manager/SKILL.md
autonomous: true
requirements: [CON-01, CON-02, CON-03]

must_haves:
  truths:
    - "workflow-common/SKILL.md has a dedicated Agent Team Conventions section documenting the full Teams lifecycle (TeamCreate, spawn with team_name, SendMessage, shutdown, TeamDelete)"
    - "workflow-common/SKILL.md documents the CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS flag requirement"
    - "workflow-common/SKILL.md documents that teammates cannot spawn nested teams"
    - "memory-manager/SKILL.md has a section documenting the relationship between Legion memory and Claude Code memory"
    - "memory-manager/SKILL.md defines the boundary: Legion memory is project-local agent orchestration data, Claude Code memory is platform-level auto-managed context"
    - "memory-manager/SKILL.md states Legion reads from Claude Code memory when available but does NOT write to it"
    - "workflow-common/SKILL.md Memory Conventions section mentions Claude Code memory integration"
  artifacts:
    - path: "skills/workflow-common/SKILL.md"
      provides: "Agent Team Conventions section and Claude Code memory mention"
      contains: "Agent Team Conventions"
    - path: "skills/memory-manager/SKILL.md"
      provides: "Claude Code Memory Alignment section"
      contains: "Claude Code Memory"
  key_links:
    - from: "skills/workflow-common/SKILL.md"
      to: "skills/wave-executor/SKILL.md"
      via: "Agent Team Conventions section references wave-executor as the primary implementer"
      pattern: "wave-executor"
    - from: "skills/workflow-common/SKILL.md"
      to: "skills/review-loop/SKILL.md"
      via: "Agent Team Conventions section references review-loop as a Teams implementer"
      pattern: "review-loop"
    - from: "skills/memory-manager/SKILL.md"
      to: "skills/workflow-common/SKILL.md"
      via: "Memory alignment section references Memory Conventions in workflow-common"
      pattern: "workflow-common"
---

<objective>
Add Agent Team Conventions documentation and Claude Code memory alignment to the shared workflow infrastructure files.

Purpose: Finding 8 (Agent Teams Migration) and Finding 9 (Memory System Alignment) from the Phase 35 CONTEXT.md require documentation updates to the shared convention files that all commands and skills reference. The Agent Teams pattern is already implemented in wave-executor, review-loop, build, and review — but the shared conventions hub (workflow-common) lacks a dedicated section explaining the pattern, constraints, and the experimental flag requirement. Similarly, the memory-manager has no documentation of how Legion memory relates to Claude Code's built-in memory system.

Output: Updated `skills/workflow-common/SKILL.md` with Agent Team Conventions section and Claude Code memory mention. Updated `skills/memory-manager/SKILL.md` with Claude Code Memory Alignment section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-consolidation-audit/35-CONTEXT.md

Key source files (read these before editing):
@skills/workflow-common/SKILL.md (target — add Agent Team Conventions + update Memory Conventions)
@skills/memory-manager/SKILL.md (target — add Claude Code Memory Alignment section)
@skills/wave-executor/SKILL.md (reference — existing Agent Teams implementation to align with)
@skills/review-loop/SKILL.md (reference — existing Agent Teams implementation to align with)
@commands/build.md (reference — existing Teams usage pattern)
@commands/review.md (reference — existing Teams usage pattern)

<interfaces>
From wave-executor/SKILL.md — existing Agent Teams enforcement block:
```
> **MANDATORY: Agent Teams are the ONLY execution method.**
> 1. TeamCreate — before any agents are spawned
> 2. TaskCreate — one task per plan, with cross-wave addBlockedBy dependencies
> 3. Agent with team_name — every agent spawn MUST include the team_name parameter
> 4. SendMessage — agents report results to the coordinator
> 5. SendMessage(type: "shutdown_request") — graceful shutdown
> 6. TeamDelete — clean up after execution
```

From workflow-common/SKILL.md — existing Wave Execution Pattern:
```
1. TeamCreate — create one Team for the entire phase
2. TaskCreate — create one Task per plan, setting addBlockedBy
3. Group plans by wave number
4. For each wave, spawn agents via Agent tool with team_name
5. Agents report results via SendMessage to the coordinator
6. Coordinator collects — wait for all agents before advancing
7. Repeat for subsequent waves
8. Shutdown + TeamDelete — send shutdown_request, then TeamDelete
```

From workflow-common/SKILL.md — existing Memory Conventions section:
```
### Memory Purpose
Cross-session learning layer that tracks agent performance and task outcomes.

### Memory Lifecycle
Absent → Created (first store) → Growing → Mature

### Memory Paths
(OUTCOMES.md, PATTERNS.md, ERRORS.md, PREFERENCES.md)

### Memory Integration Points
(build/review/plan/status operations)

### Graceful Degradation Rule
### Branch Awareness
### Semantic Compaction
```

From memory-manager/SKILL.md — Section 1 Principles:
```
1. Passive and opt-in
2. Human-readable markdown
3. Graceful degradation
4. Append-only records
5. Supplement, not override
6. Minimal footprint — .planning/memory/, four files
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Agent Team Conventions section to workflow-common/SKILL.md</name>
  <files>skills/workflow-common/SKILL.md</files>
  <action>
    Read `skills/workflow-common/SKILL.md` in full. Apply TWO additions:

    **A. Add a new "## Agent Team Conventions" section**

    Insert this section AFTER the existing "## Wave Execution Pattern" section and BEFORE "## State Update Pattern". This section documents the Agent Teams pattern as a shared convention that wave-executor and review-loop implement.

    Write the section with this content:

    ```markdown
    ## Agent Team Conventions

    ### Agent Teams Requirement

    All agent spawning in Legion MUST use Claude Code Agent Teams. Bare subagents (Agent tool without `team_name`) are prohibited. This ensures coordination, result collection, and lifecycle management work correctly across all commands.

    **Prerequisite**: The `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` setting must be enabled. If Teams are unavailable, commands MUST stop and instruct the user to enable the flag — they MUST NOT silently fall back to bare subagents.

    ### Teams Lifecycle

    Every command that spawns agents follows this lifecycle:

    | Step | Tool | When | Purpose |
    |------|------|------|---------|
    | 1 | `TeamCreate` | Once per phase/review | Create a named team (e.g., `"phase-04-execution"`, `"phase-05-review"`) |
    | 2 | `TaskCreate` | Once per plan/finding | Create shared tasks with `addBlockedBy` for cross-wave dependencies |
    | 3 | `Agent` with `team_name` | Per agent spawn | Spawn agent as a teammate — MUST include `team_name` parameter |
    | 4 | `SendMessage` | Agent completion | Agent reports structured summary to coordinator (~200 tokens vs ~2000+ from Agent return) |
    | 5 | `SendMessage(type: "shutdown_request")` | Phase/review end | Graceful shutdown of all spawned agents |
    | 6 | `TeamDelete` | After all agents shut down | Clean up Team configuration |

    ### Key Constraints

    - **One Team per phase** (`/legion:build`) or **one Team per review lifecycle** (`/legion:review`). Not per wave, not per cycle.
    - **No nested teams** — teammates cannot spawn their own Teams. Only the lead session (the command itself) creates and manages the Team.
    - **team_name is mandatory** — every `Agent` tool call MUST include the `team_name` parameter. This is enforced in `wave-executor` Section 1 and `review-loop` Section 1.
    - **Agents report via SendMessage, not Agent return** — keeps the coordinator's context window small by receiving lightweight summaries instead of full execution traces.
    - **Shutdown runs on ALL paths** — success, failure, and escalation. Never leave orphaned agents or stale Team configurations.
    - **Recommended team size**: 3-5 teammates per Team, 5-6 tasks per teammate. Larger teams increase coordination overhead.

    ### Command-to-Team Mapping

    | Command | Team Name Pattern | Agent Count | Team Lead |
    |---------|------------------|-------------|-----------|
    | `/legion:build` | `phase-{NN}-execution` | 1 per plan in wave (parallel within wave) | Build command orchestrator |
    | `/legion:review` | `phase-{NN}-review` | 2-4 reviewers + fix agents per cycle | Review command orchestrator |

    ### Implementation References

    The Agent Teams lifecycle is fully implemented in:
    - `wave-executor` SKILL.md — Section 1 (enforcement block), Section 4 (wave execution with Teams)
    - `review-loop` SKILL.md — Section 1 (enforcement block), Section 3 (review spawning with Teams)
    - `build` command — Step 4 (Team setup + wave execution)
    - `review` command — Step 4 (Team setup + review cycle)
    ```

    **B. Update the Memory Conventions section**

    In the existing "## Memory Conventions" section, add a new subsection "### Claude Code Memory Integration" at the END of the Memory Conventions section (after "### Semantic Compaction"). Write it with this content:

    ```markdown
    ### Claude Code Memory Integration

    Claude Code has its own built-in memory system at `~/.claude/projects/{project}/memory/MEMORY.md`. This is platform-level, auto-managed memory that stores user preferences, project patterns, and cross-session context.

    **Relationship to Legion Memory:**
    - **Claude Code memory** = platform-level, auto-managed, general context (user preferences, project conventions)
    - **Legion memory** = project-local, explicit, agent orchestration-specific, git-tracked (outcomes, patterns, errors, preferences)
    - These systems are **complementary, not competing** — they serve different audiences and purposes

    **Integration rules:**
    1. Legion MAY read from Claude Code memory when available (user preferences may inform agent selection)
    2. Legion MUST NOT write to Claude Code memory (different audience: platform vs agent routing)
    3. Legion MUST NOT duplicate its data into Claude Code memory
    4. If Claude Code memory is absent: skip silently (same degradation pattern as all Legion memory)

    See `memory-manager` SKILL.md Section 14 for the full alignment documentation.
    ```

    Also update the "## Division Constants" section to use Title Case division names to match the normalization being done in Plan 01 (change the lowercase-hyphenated names in the DIVISIONS array to Title Case). Change:

    ```
    DIVISIONS = [
      "engineering",
      "design",
      "marketing",
      "product",
      "project-management",
      "testing",
      "support",
      "spatial-computing",
      "specialized"
    ]
    ```

    To:

    ```
    DIVISIONS = [
      "Engineering",
      "Design",
      "Marketing",
      "Product",
      "Project Management",
      "Testing",
      "Support",
      "Spatial Computing",
      "Specialized"
    ]
    ```

    And update the Agent Personality Paths section's **Divisions** line from:
    ```
    **Divisions**: engineering, design, marketing, product, project-management, testing, support, spatial-computing, specialized, custom
    ```
    To:
    ```
    **Divisions**: Engineering, Design, Marketing, Product, Project Management, Testing, Support, Spatial Computing, Specialized, Custom
    ```

    Do NOT change any other sections. Preserve all existing content exactly as is.
  </action>
  <verify>
    1. `grep "Agent Team Conventions" skills/workflow-common/SKILL.md` returns a match
    2. `grep "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS" skills/workflow-common/SKILL.md` returns a match
    3. `grep "No nested teams" skills/workflow-common/SKILL.md` returns a match
    4. `grep "Claude Code Memory Integration" skills/workflow-common/SKILL.md` returns a match
    5. `grep "complementary, not competing" skills/workflow-common/SKILL.md` returns a match
    6. `grep "memory-manager.*Section 14" skills/workflow-common/SKILL.md` returns a match
    7. `grep '"Project Management"' skills/workflow-common/SKILL.md` returns a match (Title Case divisions)
    8. `grep '"Spatial Computing"' skills/workflow-common/SKILL.md` returns a match (Title Case divisions)
    9. `grep "project-management" skills/workflow-common/SKILL.md` returns NO match in the DIVISIONS array (lowercase removed)
  </verify>
  <done>
    workflow-common/SKILL.md has a dedicated Agent Team Conventions section documenting the full Teams lifecycle, the experimental flag requirement, the no-nested-teams constraint, command-to-team mapping, and implementation references. Memory Conventions section includes Claude Code memory integration subsection. Division Constants updated to Title Case to match Plan 01 normalization.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Claude Code Memory Alignment section to memory-manager/SKILL.md</name>
  <files>skills/memory-manager/SKILL.md</files>
  <action>
    Read `skills/memory-manager/SKILL.md` in full. Add a new "## Section 14: Claude Code Memory Alignment" section at the END of the file, after the existing Section 13 (Preference Pairs) and before any trailing content.

    Write the section with this content:

    ```markdown
    ---

    ## Section 14: Claude Code Memory Alignment

    Documents how Legion's memory system relates to Claude Code's built-in memory, defines the boundary between them, and specifies integration rules.

    ### Two Memory Systems

    Legion projects operate with two complementary memory systems:

    | Property | Claude Code Memory | Legion Memory |
    |----------|-------------------|---------------|
    | **Location** | `~/.claude/projects/{project}/memory/MEMORY.md` | `.planning/memory/` (OUTCOMES, PATTERNS, ERRORS, PREFERENCES) |
    | **Managed by** | Claude Code platform (auto-managed) | Legion workflows (explicit store/recall calls) |
    | **Scope** | Platform-level — user preferences, project conventions, cross-session context | Agent orchestration-specific — performance data, error fixes, decision signals |
    | **Audience** | Claude Code itself (informs Claude's responses across all tools) | Legion commands (informs agent selection, pattern recall, error lookup) |
    | **Lifecycle** | Auto-populated by platform; persists across sessions | Created on first Legion store operation; grows via build/review cycles |
    | **Git-tracked** | No (lives outside project directory) | Yes (lives in `.planning/memory/`, committed with project) |
    | **Branch-aware** | No (single global file per project) | Yes (branches and merges with git operations) |

    ### Why They Coexist

    These systems are complementary, not competing:

    - **Claude Code memory** captures general user preferences and project patterns that apply across ALL tools and workflows. It is the platform's ambient understanding of the user and project.
    - **Legion memory** captures structured agent orchestration data — which agents performed well on which task types, what error fixes work, what patterns to reuse. This data is specific to Legion's agent routing and has no value to the platform at large.

    Merging them would conflate two different audiences. Claude Code memory informs Claude's general responses; Legion memory informs agent selection and workflow decisions. Keeping them separate ensures each system operates with high signal-to-noise ratio.

    ### Integration Rules

    ```
    Rule 1: Legion MAY READ from Claude Code memory
      When: During /legion:plan agent recommendation or /legion:quick task routing
      How: Check if ~/.claude/projects/{project}/memory/MEMORY.md exists
           If yes: read it for user preferences that may inform agent selection
           (e.g., "user prefers minimal code", "user dislikes verbose output")
           If no: skip silently (standard graceful degradation)
      Use: As a soft signal — same role as memory_boost in agent-registry scoring

    Rule 2: Legion MUST NOT WRITE to Claude Code memory
      Why: Claude Code memory is platform-managed. Writing to it from Legion
           would create confusing entries that the platform didn't generate.
           Let the platform manage its own memory lifecycle.

    Rule 3: Legion MUST NOT DUPLICATE into Claude Code memory
      Why: Legion's four-file structure (OUTCOMES, PATTERNS, ERRORS, PREFERENCES)
           is optimized for agent routing. Duplicating into a different format
           adds maintenance burden with no benefit.

    Rule 4: Absence of either system is non-blocking
      Behavior: If Claude Code memory doesn't exist — skip silently.
                If Legion memory doesn't exist — skip silently.
                Both systems follow identical graceful degradation (Section 6).
    ```

    ### Reading Claude Code Memory (Optional Enhancement)

    When a workflow already reads Legion memory for agent scoring, it may optionally also check Claude Code memory for additional context:

    ```
    Enhanced Agent Recommendation (during /legion:plan):

    Step 1: Standard recall from .planning/memory/ (existing behavior)
      - Recall agent scores from OUTCOMES.md
      - Recall patterns from PATTERNS.md
      - Recall preferences from PREFERENCES.md

    Step 2: Optional Claude Code memory read (enhancement)
      - Check: does ~/.claude/projects/{project}/memory/MEMORY.md exist?
      - If no: skip to Step 3
      - If yes: read the file and scan for:
        a. User coding preferences (style, verbosity, framework preferences)
        b. Project-specific patterns the user has established
        c. Any preferences that map to agent capabilities
      - Extract as: claude_code_context (free text, max ~500 tokens)

    Step 3: Combine signals
      - Legion memory provides: agent scores, pattern matches, preference boosts
      - Claude Code memory provides: user context that may refine agent selection
      - Claude Code context is advisory only — it does not modify numeric scores
      - Present as a note: "Claude Code memory suggests: {relevant preference}"
    ```

    ### Boundaries Summary

    | Action | Allowed? | Why |
    |--------|----------|-----|
    | Read Claude Code memory during planning | Yes | Soft signal for agent selection |
    | Write to Claude Code memory from Legion | No | Platform manages its own memory |
    | Duplicate Legion data to Claude Code memory | No | Different format, different audience |
    | Block on Claude Code memory absence | No | Graceful degradation always applies |
    | Read Legion memory (standard behavior) | Yes | Core functionality of this skill |
    | Store to Legion memory (standard behavior) | Yes | Core functionality of this skill |
    ```

    Do NOT modify any existing sections (1-13). Do NOT change the file's frontmatter. Only append the new Section 14 at the end.
  </action>
  <verify>
    1. `grep "Section 14: Claude Code Memory Alignment" skills/memory-manager/SKILL.md` returns a match
    2. `grep "complementary, not competing" skills/memory-manager/SKILL.md` returns a match
    3. `grep "MUST NOT WRITE to Claude Code memory" skills/memory-manager/SKILL.md` returns a match
    4. `grep "MUST NOT DUPLICATE" skills/memory-manager/SKILL.md` returns a match
    5. `grep "~/.claude/projects" skills/memory-manager/SKILL.md` returns a match
    6. `grep "Graceful degradation always applies" skills/memory-manager/SKILL.md` returns a match
    7. `grep "Section 13" skills/memory-manager/SKILL.md` returns a match (existing section preserved)
  </verify>
  <done>
    memory-manager/SKILL.md has a comprehensive Section 14 documenting the relationship between Legion memory and Claude Code memory. The boundary is clear: Legion reads from Claude Code memory when available but never writes to it. Both systems coexist as complementary layers serving different audiences. Integration rules, reading enhancement, and boundaries summary are all documented.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "Agent Team Conventions" skills/workflow-common/SKILL.md` returns a match
2. `grep "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS" skills/workflow-common/SKILL.md` returns a match
3. `grep "Section 14: Claude Code Memory Alignment" skills/memory-manager/SKILL.md` returns a match
4. `grep "Claude Code Memory Integration" skills/workflow-common/SKILL.md` returns a match
5. `grep "MUST NOT WRITE" skills/memory-manager/SKILL.md` returns a match
6. `grep "No nested teams" skills/workflow-common/SKILL.md` returns a match
7. Both files are syntactically valid markdown (no broken formatting)
8. Existing content in both files is preserved (no sections removed or altered)
</verification>

<success_criteria>
Finding 8 (Agent Teams Migration) is resolved: workflow-common/SKILL.md has a dedicated Agent Team Conventions section documenting the full lifecycle, experimental flag requirement, no-nested-teams constraint, and command-to-team mapping. The existing Agent Teams implementations in wave-executor, review-loop, build, and review are referenced and aligned.

Finding 9 (Memory System Alignment) is resolved: memory-manager/SKILL.md has a Section 14 documenting the two-system relationship, boundary rules (read-only integration), and graceful degradation. workflow-common/SKILL.md Memory Conventions section mentions Claude Code memory integration.
</success_criteria>

<output>
After completion, create `.planning/phases/35-consolidation-audit/35-03-SUMMARY.md`
</output>
