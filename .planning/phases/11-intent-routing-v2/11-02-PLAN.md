---
phase: 11-intent-routing-v2
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - skills/intent-router/SKILL.md
  - commands/status.md
  - .planning/config/intent-teams.yaml
files_forbidden:
  - agents/
  - commands/build.md
  - commands/review.md
  - commands/plan.md
  - commands/start.md
  - .planning/ROADMAP.md
sequential_files: []
expected_artifacts:
  - path: "skills/intent-router/SKILL.md"
    provides: "Updated intent-router with Section 8: Context-Aware Suggestions"
    required: true
  - path: "commands/status.md"
    provides: "Updated status command with context-aware next-action suggestions"
    required: true
autonomous: false
agents: ["engineering-senior-developer"]
requirements: ["INTENT-08"]
user_setup: []
verification_commands:
  - "grep -q 'Section 8' skills/intent-router/SKILL.md"
  - "grep -q 'getContextSuggestions' skills/intent-router/SKILL.md"
  - "grep -q 'context_rules' .planning/config/intent-teams.yaml"
  - "grep -q 'Context-Aware' commands/status.md"
  - "grep -q 'suggestions' commands/status.md"

must_haves:
  truths:
    - "Intent-router SKILL.md has a Section 8 covering context-aware suggestions with STATE.md position detection and lifecycle-based recommendations"
    - "intent-teams.yaml includes context_rules section mapping project lifecycle positions to recommended actions"
    - "status.md displays 2-3 context-aware next-action suggestions based on current project state"
    - "Suggestions degrade gracefully when STATE.md is missing or malformed"
    - "All existing intent-router sections (1-7) remain unchanged and functional"
  artifacts:
    - path: "skills/intent-router/SKILL.md"
      provides: "Intent router with context-aware suggestions section"
      min_lines: 800
      contains: "getContextSuggestions"
    - path: "commands/status.md"
      provides: "Status command with context-aware suggestions"
      min_lines: 50
      contains: "Context-Aware"
  key_links:
    - from: "skills/intent-router/SKILL.md"
      to: ".planning/STATE.md"
      via: "Context engine reads STATE.md for position detection"
      pattern: "STATE.md"
    - from: "commands/status.md"
      to: "skills/intent-router/SKILL.md"
      via: "Status command calls getContextSuggestions"
      pattern: "intent-router"
    - from: ".planning/config/intent-teams.yaml"
      to: "skills/intent-router/SKILL.md"
      via: "Context rules loaded from config"
      pattern: "context_rules"
---

<objective>
Add context-aware intent suggestions to the intent-router skill, enabling `/legion:status` to recommend next actions based on current project lifecycle position (e.g., "phase planned but not built" suggests `/legion:build`).

Purpose: Make Legion proactively helpful — instead of requiring users to know what command to run next, the system reads project state and suggests the most relevant actions with natural language descriptions.
Output: Updated `skills/intent-router/SKILL.md` (Section 8), extended `intent-teams.yaml` with context rules, updated `commands/status.md`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-intent-routing-v2/11-CONTEXT.md
@.planning/phases/11-intent-routing-v2/11-01-SUMMARY.md

Relevant source files:
@skills/intent-router/SKILL.md
@.planning/config/intent-teams.yaml
@commands/status.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Section 8 — Context-Aware Suggestions to intent-router SKILL.md</name>
  <files>skills/intent-router/SKILL.md</files>
  <action>
Add a new `## Section 8: Context-Aware Suggestions` to `skills/intent-router/SKILL.md` after the Section 7 added in Plan 11-01. This section defines how project state maps to recommended next actions.

The section must include:

**8.1 getContextSuggestions(statePath)**
A function that reads STATE.md and returns 2-3 ranked suggestions:
```javascript
/**
 * @param {string} statePath - Path to STATE.md (default: '.planning/STATE.md')
 * @returns {Object} {
 *   currentPosition: { phase, status, lastActivity },
 *   suggestions: [
 *     { command: '/legion:build', description: 'Execute Phase 5 plans', priority: 1, reason: 'Phase 5 is planned but not yet built' },
 *     { command: '/legion:status', description: 'Check progress dashboard', priority: 2, reason: 'Good starting point for session orientation' }
 *   ],
 *   rawState: { ... }
 * }
 */
```

**8.2 Project Lifecycle Position Detection**
Parse STATE.md to determine where the project is in its lifecycle. Define position categories:

| Position | Detection Rule | Description |
|----------|---------------|-------------|
| `no_project` | No STATE.md or no phase info | Project not initialized |
| `just_started` | Phase 1, status contains "initialized" | Just ran /legion:start |
| `needs_planning` | Current phase status = "complete" and next phase not planned | Ready for next phase planning |
| `planned_not_built` | Current phase status contains "planned" | Plans exist, need execution |
| `building` | Current phase status contains "executing" or "in progress" | Build in progress |
| `needs_review` | Current phase status contains "built" or "executed" | Build done, needs review |
| `review_in_progress` | Current phase status contains "reviewing" | Review cycle active |
| `review_failed` | Current phase status contains "needs work" or "rework" | Review found issues |
| `phase_complete` | Current phase status = "complete", not last phase | Phase done, next phase available |
| `milestone_complete` | All phases complete | Ready for milestone wrap-up |

**8.3 Position-to-Suggestion Mapping**
Map each lifecycle position to 2-3 ranked action suggestions. Load mappings from `intent-teams.yaml` `context_rules` section (created in Task 2).

```javascript
function mapPositionToSuggestions(position, stateData) {
  const rules = loadContextRules();  // from intent-teams.yaml
  const positionRules = rules[position];

  return positionRules.suggestions.map((suggestion, index) => ({
    command: suggestion.command,
    description: interpolate(suggestion.description, stateData),  // e.g., "Execute Phase {phase} plans"
    priority: index + 1,
    reason: interpolate(suggestion.reason, stateData)
  }));
}
```

**8.4 Graceful Degradation**
- If STATE.md doesn't exist: return `position: 'no_project'` with suggestion to run `/legion:start`
- If STATE.md is malformed: return generic suggestions (`/legion:status`, `/legion:start`)
- If position can't be determined: return `position: 'unknown'` with safe defaults
- Never throw errors — always return a valid suggestions array

**8.5 Integration with NL Parser**
When `parseNaturalLanguage()` (Section 7) returns LOW confidence, augment the fallback suggestions with context-aware suggestions. This gives users better guidance when their input is ambiguous:
```javascript
if (nlResult.confidence < 0.5) {
  const contextSuggestions = getContextSuggestions();
  nlResult.fallbackSuggestion = mergeWithContext(nlResult.suggestions, contextSuggestions);
}
```

Include implementation pseudocode for all functions following the existing code style.

> verification: grep -q "Section 8" skills/intent-router/SKILL.md
> verification: grep -q "getContextSuggestions" skills/intent-router/SKILL.md
> verification: grep -q "Lifecycle Position" skills/intent-router/SKILL.md
> verification: grep -q "Graceful Degradation" skills/intent-router/SKILL.md
> verification: grep -q "no_project" skills/intent-router/SKILL.md
  </action>
  <verify>
grep -q "Section 8" skills/intent-router/SKILL.md
grep -q "getContextSuggestions" skills/intent-router/SKILL.md
grep -q "Lifecycle Position" skills/intent-router/SKILL.md
grep -q "Graceful Degradation" skills/intent-router/SKILL.md
grep -q "no_project" skills/intent-router/SKILL.md
  </verify>
  <done>Intent-router SKILL.md has Section 8 with complete context-aware suggestion specification including getContextSuggestions, lifecycle position detection, position-to-suggestion mapping, and graceful degradation.</done>
</task>

<task type="auto">
  <name>Task 2: Extend intent-teams.yaml with context rules</name>
  <files>.planning/config/intent-teams.yaml</files>
  <action>
Add a `context_rules` top-level section to `.planning/config/intent-teams.yaml` that maps project lifecycle positions to suggested actions.

```yaml
context_rules:
  # Maps lifecycle positions to ranked action suggestions
  # {phase} and {phase_name} are interpolated from STATE.md at runtime

  no_project:
    suggestions:
      - command: "/legion:start"
        description: "Initialize a new project"
        reason: "No project state found — start here"
      - command: "/legion:explore"
        description: "Explore and crystallize an idea first"
        reason: "Pre-flight alignment before formal project setup"

  just_started:
    suggestions:
      - command: "/legion:plan 1"
        description: "Plan Phase 1: {phase_name}"
        reason: "Project initialized, ready for first phase planning"
      - command: "/legion:status"
        description: "View project dashboard"
        reason: "Orient yourself with the project overview"

  needs_planning:
    suggestions:
      - command: "/legion:plan {next_phase}"
        description: "Plan Phase {next_phase}: {phase_name}"
        reason: "Phase {phase} complete, next phase ready for planning"
      - command: "/legion:status"
        description: "Check overall progress"
        reason: "Review completed work before planning next phase"
      - command: "/legion:milestone"
        description: "Check milestone status"
        reason: "See how close you are to the milestone goal"

  planned_not_built:
    suggestions:
      - command: "/legion:build"
        description: "Execute Phase {phase} plans"
        reason: "Phase {phase} is planned and ready for execution"
      - command: "/legion:status"
        description: "Review plan breakdown before building"
        reason: "Verify plan structure looks correct"

  building:
    suggestions:
      - command: "/legion:build"
        description: "Resume Phase {phase} execution"
        reason: "Build is in progress — continue where you left off"
      - command: "/legion:status"
        description: "Check build progress"
        reason: "See which plans are complete and which remain"

  needs_review:
    suggestions:
      - command: "/legion:review"
        description: "Review Phase {phase} output"
        reason: "Build complete, quality review is the next step"
      - command: "/legion:status"
        description: "Check what was built before reviewing"
        reason: "Orient on build outputs before starting review"

  review_in_progress:
    suggestions:
      - command: "/legion:review"
        description: "Continue Phase {phase} review"
        reason: "Review cycle is active — continue the process"
      - command: "/legion:status"
        description: "Check review findings so far"
        reason: "See current review status and findings"

  review_failed:
    suggestions:
      - command: "/legion:build"
        description: "Fix review findings for Phase {phase}"
        reason: "Review found issues that need to be addressed"
      - command: "/legion:review"
        description: "Re-run review after fixes"
        reason: "Verify fixes resolve the review findings"
      - command: "/legion:status"
        description: "See what needs fixing"
        reason: "Review the specific issues found"

  phase_complete:
    suggestions:
      - command: "/legion:plan {next_phase}"
        description: "Plan Phase {next_phase}: {phase_name}"
        reason: "Phase {phase} complete and reviewed — move to next phase"
      - command: "/legion:status"
        description: "View overall progress"
        reason: "See milestone completion percentage"

  milestone_complete:
    suggestions:
      - command: "/legion:milestone"
        description: "Complete and archive the milestone"
        reason: "All phases done — wrap up the milestone"
      - command: "/legion:status"
        description: "View final milestone dashboard"
        reason: "Review everything accomplished in this milestone"
      - command: "/legion:explore"
        description: "Explore ideas for the next milestone"
        reason: "Start thinking about what comes next"
```

Add `INTENT-08` to the Requirements Validated header line. Preserve all existing content.

> verification: grep -q "context_rules" .planning/config/intent-teams.yaml
> verification: grep -q "INTENT-08" .planning/config/intent-teams.yaml
> verification: grep -q "no_project" .planning/config/intent-teams.yaml
> verification: grep -q "planned_not_built" .planning/config/intent-teams.yaml
> verification: grep -q "milestone_complete" .planning/config/intent-teams.yaml
  </action>
  <verify>
grep -q "context_rules" .planning/config/intent-teams.yaml
grep -q "INTENT-08" .planning/config/intent-teams.yaml
grep -q "no_project" .planning/config/intent-teams.yaml
grep -q "planned_not_built" .planning/config/intent-teams.yaml
grep -q "milestone_complete" .planning/config/intent-teams.yaml
  </verify>
  <done>intent-teams.yaml extended with context_rules mapping 10 lifecycle positions to ranked action suggestions with interpolation templates.</done>
</task>

<task type="auto">
  <name>Task 3: Update status.md to display context-aware suggestions</name>
  <files>commands/status.md</files>
  <action>
Update `commands/status.md` to display context-aware next-action suggestions at the end of the status dashboard output.

Read the current `commands/status.md` first. Add the following:

1. Add `skills/intent-router/SKILL.md` to the `<execution_context>` list

2. Add a new section "## Context-Aware Suggestions" to the process, positioned after the main dashboard display but before state updates. This section:

   a. Call `getContextSuggestions()` from intent-router Section 8
   b. Receive the current lifecycle position and 2-3 ranked suggestions
   c. Display suggestions in the dashboard output:

   ```
   ## Suggested Next Actions

   Based on your current position (Phase {N} {status}):

   1. **`/legion:build`** — Execute Phase {N} plans
      _Phase {N} is planned and ready for execution_

   2. **`/legion:status`** — Review plan breakdown
      _Verify plan structure looks correct_
   ```

   d. Format rules:
      - Show command in backtick code format
      - Show description on same line after em-dash
      - Show reason in italics on next line
      - Number suggestions by priority (1, 2, 3)
      - Maximum 3 suggestions displayed
      - If no suggestions available (degraded state), show: "Run `/legion:status` for orientation or `/legion:start` to begin a new project."

3. The suggestions section should be the last thing displayed in the status output, providing a natural "what to do next" ending to the dashboard.

4. Add graceful degradation: if intent-router skill loading fails or STATE.md is unparseable, skip suggestions silently (no error output, just omit the section).

> verification: grep -q "Context-Aware" commands/status.md
> verification: grep -q "suggestions" commands/status.md
> verification: grep -q "getContextSuggestions" commands/status.md
> verification: grep -q "intent-router" commands/status.md
> verification: grep -q "Suggested Next Actions" commands/status.md
  </action>
  <verify>
grep -q "Context-Aware" commands/status.md
grep -q "suggestions" commands/status.md
grep -q "getContextSuggestions" commands/status.md
grep -q "intent-router" commands/status.md
grep -q "Suggested Next Actions" commands/status.md
  </verify>
  <done>status.md updated with Context-Aware Suggestions section that displays 2-3 ranked next-action suggestions based on project lifecycle position, with graceful degradation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Intent-router SKILL.md has Section 8 with getContextSuggestions, lifecycle detection, position mapping
- [ ] intent-teams.yaml has context_rules section with 10 lifecycle positions and ranked suggestions
- [ ] status.md displays "Suggested Next Actions" section with context-aware recommendations
- [ ] Graceful degradation documented and implemented for missing/malformed STATE.md
- [ ] Context suggestions integrate with NL parser fallback (Section 7 → Section 8 bridge)
- [ ] All existing intent-router sections (1-7) remain unchanged
- [ ] Suggestions use interpolation templates ({phase}, {phase_name}, {next_phase})
</verification>

<success_criteria>
- Intent-router SKILL.md Section 8 fully specifies context-aware suggestions with getContextSuggestions function
- intent-teams.yaml context_rules covers all 10 lifecycle positions with appropriate suggestions
- status.md shows ranked next-action suggestions as the final dashboard section
- Graceful degradation ensures no errors when STATE.md is missing or malformed
- Context suggestions merge with NL parser output for low-confidence fallbacks
</success_criteria>

<output>
After completion, create `.planning/phases/11-intent-routing-v2/11-02-SUMMARY.md`

Update STATE.md progress to reflect Phase 11 execution complete.
</output>
