---
phase: 08-codebase-mapper-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/codebase-mapper/SKILL.md
  - tests/codebase-mapper-enrichment.test.js
files_forbidden:
  - commands/
  - agents/
  - adapters/
  - .planning/
sequential_files:
  - skills/codebase-mapper/SKILL.md
expected_artifacts:
  - path: "skills/codebase-mapper/SKILL.md"
    provides: "New Sections 9.4 and 9.5 (coverage tool integration + critical file correlation) and enriched Test Coverage Map template"
    required: true
  - path: "tests/codebase-mapper-enrichment.test.js"
    provides: "Tests for enriched test coverage map format, critical file correlation, and graceful degradation"
    required: true
autonomous: false
agents: ["testing-evidence-collector"]
requirements: ["MAP-02"]
user_setup: []
verification_commands:
  - "grep -q 'Section 9.4' skills/codebase-mapper/SKILL.md"
  - "grep -q 'Section 9.5' skills/codebase-mapper/SKILL.md"
  - "grep -q 'Coverage Tool Integration' skills/codebase-mapper/SKILL.md"
  - "grep -q 'Critical File Coverage' skills/codebase-mapper/SKILL.md"
  - "node --test tests/codebase-mapper-enrichment.test.js 2>&1 | tail -5"

must_haves:
  truths:
    - "Section 9.4 defines coverage tool integration for reading existing coverage reports"
    - "Section 9.4 covers nyc/istanbul (Node.js), pytest-cov (Python), go test -cover (Go)"
    - "Section 9.5 cross-references Section 8 fan-in data with untested files"
    - "Section 9.5 identifies critical untested files (high fan-in + no tests)"
    - "Enriched Test Coverage Map template includes coverage percentage from tooling"
    - "Enriched Test Coverage Map template includes Critical Untested Files table"
    - "Coverage tool integration is read-only (reads reports, never runs test suites)"
  artifacts:
    - path: "skills/codebase-mapper/SKILL.md"
      provides: "Enriched codebase-mapper with test coverage analysis"
      contains: "Section 9.4"
    - path: "tests/codebase-mapper-enrichment.test.js"
      provides: "Tests for MAP-02 enrichment"
      contains: "Test Coverage"
  key_links:
    - from: "skills/codebase-mapper/SKILL.md"
      to: "tests/codebase-mapper-enrichment.test.js"
      via: "test coverage"
      pattern: "Coverage Tool"
---

<objective>
Enrich the codebase-mapper's test coverage analysis (MAP-02) with coverage tool integration and critical file correlation. This enhances the existing Section 9 (sample-based test file matching) with actual coverage percentages from tooling and cross-references untested files with fan-in data to identify the highest-risk gaps.

Purpose: Move beyond "does a test file exist?" to "what's the actual coverage, and which untested files are the most dangerous?" — giving agents and planners actionable coverage intelligence.
Output: New Sections 9.4 and 9.5 in SKILL.md, enriched Test Coverage Map template, tests validating format and degradation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-codebase-mapper-enrichment/08-CONTEXT.md
@.planning/specs/08-codebase-mapper-enrichment-spec.md
@skills/codebase-mapper/SKILL.md

Relevant test patterns:
@tests/adapter-conformance.test.js (test structure reference)
@tests/directory-mappings.test.js (domain logic testing reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Section 9.4 — Coverage Tool Integration</name>
  <files>skills/codebase-mapper/SKILL.md</files>
  <action>
Read `skills/codebase-mapper/SKILL.md`. After Section 9.3 (Output Format) and before Section 10 (API Surface Detection), add a new subsection `### 9.4: Coverage Tool Integration (MAP-02)`.

The section should define:

**Purpose:** Read existing coverage reports to extract actual code coverage percentages. This is READ-ONLY — never run test suites or coverage tools (that's invasive and could fail).

**9.4.1: Coverage Report Detection**
Search for existing coverage report files in standard locations:

| Tool | Report Locations | Format |
|------|-----------------|--------|
| nyc/istanbul (Node.js) | `coverage/coverage-summary.json`, `coverage/lcov.info`, `.nyc_output/` | JSON summary or LCOV |
| jest (Node.js) | `coverage/coverage-summary.json` | JSON summary |
| pytest-cov (Python) | `htmlcov/`, `.coverage`, `coverage.xml` | XML (Cobertura) or SQLite |
| go test (Go) | `coverage.out`, `cover.out` | Go cover profile |
| SimpleCov (Ruby) | `coverage/.last_run.json`, `coverage/.resultset.json` | JSON |
| cargo-tarpaulin (Rust) | `tarpaulin-report.json`, `cobertura.xml` | JSON or Cobertura XML |

**Detection protocol:**
```
Step 1: Glob for each report location pattern above
Step 2: If any found, read the most recent report
Step 3: Extract aggregate coverage percentage:
  - JSON summary: pct field from statements/lines/branches
  - LCOV: parse LF/LH lines to compute line coverage
  - Cobertura XML: parse line-rate attribute
  - Go cover: compute from "ok" vs total lines
Step 4: Report source, date (file mtime), and percentage
```

**9.4.2: Coverage Percentage Extraction**
For each detected report type, define the extraction logic:

**JSON summary (nyc/jest):**
```
Read coverage-summary.json
Extract total.lines.pct (or total.statements.pct)
Report: "{pct}% line coverage (from coverage-summary.json, {date})"
```

**LCOV format:**
```
Grep for "^LF:" and "^LH:" lines
total_lines = sum of all LF values
hit_lines = sum of all LH values
pct = (hit_lines / total_lines) * 100
Report: "{pct}% line coverage (from lcov.info, {date})"
```

**Cobertura XML:**
```
Grep for 'line-rate="' in first 10 lines
Extract the decimal value, multiply by 100
Report: "{pct}% line coverage (from coverage.xml, {date})"
```

**Go cover profile:**
```
Count lines with "1" suffix (covered) vs "0" suffix (not covered)
pct = (covered / total) * 100
Report: "{pct}% statement coverage (from coverage.out, {date})"
```

**9.4.3: Coverage Quality Classification**
Classify the extracted coverage percentage:

| Coverage | Quality | Interpretation |
|----------|---------|---------------|
| >= 80% | HIGH | Well-tested codebase |
| 50-79% | MEDIUM | Partial coverage — gaps may exist in critical paths |
| < 50% | LOW | Significant testing gaps — risk of undetected regressions |

**9.4.4: Graceful Degradation**
- If no coverage reports found: fall back to Section 9.2 sample-based ratio (existing behavior)
- Report: "No coverage reports found. Coverage estimated from test file matching: {ratio}% ({HIGH|MEDIUM|LOW})"
- If report exists but is >30 days old: use but warn "Coverage report is {N} days old"
- If report parsing fails: skip and use sample-based ratio
- Never run test suites or coverage tools

> verification: grep -q 'Section 9.4' skills/codebase-mapper/SKILL.md
> verification: grep -q 'Coverage Tool Integration' skills/codebase-mapper/SKILL.md
  </action>
  <verify>
grep -q 'Section 9.4' skills/codebase-mapper/SKILL.md
grep -q 'Coverage Tool Integration' skills/codebase-mapper/SKILL.md
  </verify>
  <done>Section 9.4 added with coverage report detection for 6 ecosystems, extraction logic for 4 report formats, quality classification, and graceful degradation to sample-based ratio.</done>
</task>

<task type="auto">
  <name>Task 2: Add Section 9.5 — Critical File Coverage Correlation</name>
  <files>skills/codebase-mapper/SKILL.md</files>
  <action>
In `skills/codebase-mapper/SKILL.md`, after the new Section 9.4, add `### 9.5: Critical File Coverage Correlation (MAP-02)`.

The section should define:

**Purpose:** Cross-reference untested files (from Section 9.2) with dependency importance (from Section 8 fan-in) and complexity (from Section 4.1) to identify the highest-risk coverage gaps.

**9.5.1: Critical File Identification**
For each file identified as untested in Section 9.2 Step 3:
```
Step 1: Check if the file appears in Section 8 fan-in list
  - If yes: fan_in_score = import count (higher = more critical)
  - If no: fan_in_score = 0

Step 2: Check if the file appears in Section 4.1 large files list
  - If yes: complexity_score = line count
  - If no: complexity_score = 0

Step 3: Compute risk score
  risk = (fan_in_score * 10) + (complexity_score / 100)

Step 4: Classify risk
  - risk >= 30: CRITICAL (high fan-in + large file, no tests)
  - risk >= 10: HIGH (significant fan-in or large file, no tests)
  - risk > 0: MEDIUM (some coupling or complexity, no tests)
  - risk == 0: LOW (isolated small file, no tests)
```

**9.5.2: Output**
Produce a ranked table of critical untested files:

| File | Lines | Fan-in | Risk Score | Risk Level | Recommendation |
|------|-------|--------|-----------|-----------|----------------|
| {file} | {lines} | {imported_by_count} | {score} | {CRITICAL/HIGH/MEDIUM/LOW} | {e.g., "Add unit tests — 5 files depend on this"} |

Sort by risk score descending. Report top 5 only.

**9.5.3: Graceful Degradation**
- If Section 8 fan-in data not available (no import analysis ran): use complexity_score only
- If Section 4.1 data not available: use fan_in_score only
- If neither available: skip correlation, use basic untested files list from Section 9.2
- Never block on missing cross-reference data

> verification: grep -q 'Section 9.5' skills/codebase-mapper/SKILL.md
> verification: grep -q 'Critical File Coverage' skills/codebase-mapper/SKILL.md
  </action>
  <verify>
grep -q 'Section 9.5' skills/codebase-mapper/SKILL.md
grep -q 'Critical File Coverage' skills/codebase-mapper/SKILL.md
  </verify>
  <done>Section 9.5 added with critical file identification protocol, risk scoring formula, ranked output table, and graceful degradation for missing cross-reference data.</done>
</task>

<task type="auto">
  <name>Task 3: Update Test Coverage Map template and write tests</name>
  <files>skills/codebase-mapper/SKILL.md, tests/codebase-mapper-enrichment.test.js</files>
  <action>
**Part A: Update CODEBASE.md template (Section 5)**

In Section 5 of `skills/codebase-mapper/SKILL.md`, update the `## Test Coverage Map` template section to include enriched fields:

```markdown
## Test Coverage Map

**Test convention**: {dominant convention, e.g., "co-located .test.ts files"}
**Coverage**: {pct}% — {HIGH|MEDIUM|LOW}
**Source**: {e.g., "coverage-summary.json (2026-03-01)" or "Estimated from test file matching"}

### Files Without Tests
| Source File | Lines | Risk Note |
|-------------|-------|-----------|
| {file} | {lines} | {e.g., "Large file, high fan-in"} |

### Critical Untested Files
| File | Lines | Fan-in | Risk Score | Risk Level | Recommendation |
|------|-------|--------|-----------|-----------|----------------|
| {file} | {lines} | {count} | {score} | {CRITICAL/HIGH/MEDIUM/LOW} | {recommendation} |
```

Keep the existing graceful degradation placeholder:
```markdown
## Test Coverage Map
No test convention detected. No files matching common test patterns (.test., .spec., __tests__/, test/, _test.go, Test*.java) were found.
```

Also update Section 7.2 (Re-Analysis Protocol) Step 4 to include "Section 9.4 (Coverage Tool Integration)" and "Section 9.5 (Critical File Coverage Correlation)".

**Part B: Add MAP-02 tests to codebase-mapper-enrichment.test.js**

IMPORTANT: Plan 08-01 creates this test file. Append MAP-02 tests to it (do not overwrite). If the file does not exist yet (sequential execution), create it with both MAP-01 and MAP-02 test sections.

Add these test sections:

**describe('Test Coverage Enrichment — SKILL.md specification')**:
- test: Section 9.4 exists (Coverage Tool Integration)
- test: Section 9.4 defines coverage report detection for nyc, jest, pytest-cov, go test, SimpleCov, cargo-tarpaulin
- test: Section 9.4 includes extraction logic for JSON summary, LCOV, Cobertura XML, Go cover formats
- test: Section 9.4 includes quality classification thresholds (>=80 HIGH, 50-79 MEDIUM, <50 LOW)
- test: Section 9.4 includes graceful degradation to sample-based ratio
- test: Section 9.5 exists (Critical File Coverage Correlation)
- test: Section 9.5 defines risk scoring formula using fan-in and complexity
- test: Section 9.5 includes ranked output table format
- test: Section 9.5 includes graceful degradation for missing cross-reference data

**describe('Test Coverage Enrichment — CODEBASE.md template')**:
- test: Template Test Coverage Map includes Source field
- test: Template includes Critical Untested Files subsection
- test: Critical Untested Files table has required columns (File, Lines, Fan-in, Risk Score, Risk Level, Recommendation)
- test: Template preserves graceful degradation placeholder

> verification: node --test tests/codebase-mapper-enrichment.test.js 2>&1 | tail -5
  </action>
  <verify>
node --test tests/codebase-mapper-enrichment.test.js 2>&1 | tail -5
  </verify>
  <done>CODEBASE.md template enriched with coverage source field and Critical Untested Files table. Tests pass for all MAP-02 specification conformance checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Section 9.4 exists in skills/codebase-mapper/SKILL.md (Coverage Tool Integration)
- [ ] Section 9.5 exists in skills/codebase-mapper/SKILL.md (Critical File Coverage Correlation)
- [ ] CODEBASE.md template `## Test Coverage Map` includes Source field and Critical Untested Files table
- [ ] All tests pass: `node --test tests/codebase-mapper-enrichment.test.js`
- [ ] No existing Section 9.1, 9.2, or 9.3 content was modified (only new subsections added)
- [ ] Coverage tool integration is explicitly read-only (never runs test suites)
- [ ] Critical file risk scoring uses both fan-in and complexity data
</verification>

<success_criteria>
- Test Coverage Map in CODEBASE.md includes coverage percentage from tooling with source attribution
- Critical untested files identified by cross-referencing fan-in and complexity with test coverage
- Coverage tool integration reads existing reports only (never runs test suites)
- Graceful degradation at every level (no reports → sample-based, no fan-in → complexity only)
- Tests validate specification conformance
- No regressions in existing Section 9 content
</success_criteria>

<output>
After completion, create `.planning/phases/08-codebase-mapper-enrichment/08-02-SUMMARY.md`

Update STATE.md with plan completion status.
</output>
