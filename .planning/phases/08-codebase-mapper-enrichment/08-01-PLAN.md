---
phase: 08-codebase-mapper-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/codebase-mapper/SKILL.md
  - tests/codebase-mapper-enrichment.test.js
  - tests/fixtures/codebase-mapper/sample-npm-outdated.json
files_forbidden:
  - commands/
  - agents/
  - adapters/
  - .planning/
sequential_files:
  - skills/codebase-mapper/SKILL.md
expected_artifacts:
  - path: "skills/codebase-mapper/SKILL.md"
    provides: "New Section 4.6 (Package-Level Dependency Risk) and updated CODEBASE.md template with Dependency Risk section"
    required: true
  - path: "tests/codebase-mapper-enrichment.test.js"
    provides: "Tests for dependency risk section format, calibration, and graceful degradation"
    required: true
autonomous: false
agents: ["engineering-senior-developer"]
requirements: ["MAP-01"]
user_setup: []
verification_commands:
  - "grep -q 'Section 4.6' skills/codebase-mapper/SKILL.md"
  - "grep -q 'Package-Level Dependency Risk' skills/codebase-mapper/SKILL.md"
  - "grep -q '## Dependency Risk' skills/codebase-mapper/SKILL.md"
  - "grep -q 'npm outdated' skills/codebase-mapper/SKILL.md"
  - "grep -q 'graceful degradation' skills/codebase-mapper/SKILL.md | head -1"
  - "node --test tests/codebase-mapper-enrichment.test.js 2>&1 | tail -5"

must_haves:
  truths:
    - "Section 4.6 defines ecosystem-specific outdated detection commands (npm, pip, bundle, cargo)"
    - "Section 4.6 includes unmaintained package detection heuristic"
    - "Section 4.6 includes heavy dependency detection with calibrated thresholds"
    - "Section 4.6 includes skip conditions for graceful degradation per ecosystem"
    - "CODEBASE.md template includes ## Dependency Risk section with required fields"
    - "Dependency risk scoring is calibrated relative to total dependency count"
  artifacts:
    - path: "skills/codebase-mapper/SKILL.md"
      provides: "Enriched codebase-mapper with dependency risk analysis"
      contains: "Section 4.6"
    - path: "tests/codebase-mapper-enrichment.test.js"
      provides: "Tests for MAP-01 enrichment"
      contains: "Dependency Risk"
  key_links:
    - from: "skills/codebase-mapper/SKILL.md"
      to: "tests/codebase-mapper-enrichment.test.js"
      via: "test coverage"
      pattern: "Dependency Risk"
---

<objective>
Add package-level dependency risk assessment to the codebase-mapper skill (MAP-01). This enriches the existing config-level checks (Section 4.4) with deeper analysis: outdated packages, unmaintained dependencies, and heavy transitive dependency chains.

Purpose: Enable codebase-mapper to flag dependency health risks beyond just "is there a lockfile?" — agents need to know if the project's dependencies are outdated, abandoned, or bloated.
Output: New Section 4.6 in SKILL.md, updated CODEBASE.md template, tests validating format and degradation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-codebase-mapper-enrichment/08-CONTEXT.md
@.planning/specs/08-codebase-mapper-enrichment-spec.md
@skills/codebase-mapper/SKILL.md

Relevant test patterns:
@tests/adapter-conformance.test.js (test structure reference)
@tests/directory-mappings.test.js (domain logic testing reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Section 4.6 — Package-Level Dependency Risk</name>
  <files>skills/codebase-mapper/SKILL.md</files>
  <action>
Read `skills/codebase-mapper/SKILL.md`. After Section 4.5 (Risk Summary) and before Section 5 (CODEBASE.md Format), add a new `## Section 4.6: Package-Level Dependency Risk (MAP-01)`.

The section should define:

**4.6.1: Ecosystem Detection**
Use the framework detection from Section 3.1 to determine which package manager commands to run:

| Ecosystem | Package Manager | Outdated Command | Output Format |
|-----------|----------------|-----------------|---------------|
| Node.js | npm | `npm outdated --json 2>/dev/null` | JSON with current/wanted/latest |
| Node.js | yarn | `yarn outdated --json 2>/dev/null` | JSON lines format |
| Python | pip | `pip list --outdated --format=json 2>/dev/null` | JSON array |
| Ruby | bundler | `bundle outdated --parseable 2>/dev/null` | Parseable text |
| Rust | cargo | `cargo outdated --format json 2>/dev/null` | JSON (requires cargo-outdated) |
| Go | go | `go list -m -u -json all 2>/dev/null` | JSON per module |

**4.6.2: Outdated Package Detection**
Run the appropriate outdated command. Parse the output:
- Count packages with available updates
- Categorize: major version behind (HIGH), minor version behind (MEDIUM), patch only (LOW)
- Report top 5 most outdated packages with current → latest version

**Skip condition:** If package manager not available or command fails, output: "Package manager not available or no lockfile found. Dependency currency check skipped."

**4.6.3: Heavy Dependency Detection**
For Node.js projects, check transitive dependency count:
```bash
npm ls --all --json 2>/dev/null | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); const count=(o)=>o.dependencies?Object.keys(o.dependencies).reduce((s,k)=>s+1+count(o.dependencies[k]),0):0; console.log(count(d))"
```

Thresholds (calibrated to direct dep count):
- Ratio > 50 transitive per direct dep: HIGH (bloated dependency tree)
- Ratio 20-50: MEDIUM (normal growth)
- Ratio < 20: LOW (lean tree)

For other ecosystems: skip heavy dependency check (no reliable cross-ecosystem tool).

**Skip condition:** If `npm ls` fails or not a Node.js project, output: "Heavy dependency analysis requires Node.js/npm. Skipped for {ecosystem}."

**4.6.4: Unmaintained Package Heuristic**
For Node.js projects, check the `time` field in `package-lock.json` for packages not updated in >2 years:
```
Read package-lock.json
For each direct dependency in package.json:
  Find its entry in lockfile
  Check resolved URL date or version publish date if available
  Flag packages where latest version is >2 years old
```

For non-Node.js: Check lockfile modification dates as a rough proxy. If lockfile is >2 years old and manifest has been updated recently, flag as potential staleness.

**Skip condition:** If no lockfile or parsing fails, output: "Lockfile unavailable for unmaintained package detection. Skipped."

**4.6.5: Dependency Risk Summary**
Produce a consolidated dependency risk assessment:

| Metric | Value | Risk Level |
|--------|-------|-----------|
| Outdated packages | {N}/{total} ({pct}%) | {HIGH/MEDIUM/LOW} |
| Major version behind | {N} packages | {HIGH if >0} |
| Heavy dependencies | {transitive_count} transitive ({ratio}x) | {HIGH/MEDIUM/LOW} |
| Potentially unmaintained | {N} packages | {MEDIUM if >0} |

Calibration: Risk levels relative to total dependency count, not absolute numbers. A project with 5 deps and 3 outdated is HIGH; a project with 200 deps and 3 outdated is LOW.

**4.6.6: Graceful Degradation**
- If no package manifest detected: skip entire section silently
- If package manager command fails: report which check was skipped and why
- If lockfile is absent: some checks degrade (noted per subsection)
- Never error, never block analysis completion

> verification: grep -q 'Section 4.6' skills/codebase-mapper/SKILL.md
> verification: grep -q 'Package-Level Dependency Risk' skills/codebase-mapper/SKILL.md
> verification: grep -q 'npm outdated' skills/codebase-mapper/SKILL.md
  </action>
  <verify>
grep -q 'Section 4.6' skills/codebase-mapper/SKILL.md
grep -q 'Package-Level Dependency Risk' skills/codebase-mapper/SKILL.md
grep -q 'npm outdated' skills/codebase-mapper/SKILL.md
  </verify>
  <done>Section 4.6 added to codebase-mapper SKILL.md with ecosystem-specific outdated detection, heavy dependency analysis, unmaintained heuristic, and graceful degradation.</done>
</task>

<task type="auto">
  <name>Task 2: Update CODEBASE.md template with Dependency Risk section</name>
  <files>skills/codebase-mapper/SKILL.md</files>
  <action>
In Section 5 of `skills/codebase-mapper/SKILL.md` (CODEBASE.md Format), add a new `## Dependency Risk` section to the template. Place it after `## Technical Debt Signals` and before `## Agent Guidance`.

Template content:

```markdown
## Dependency Risk

**Ecosystem**: {ecosystem, e.g., "Node.js (npm)"}
**Direct dependencies**: {count} | **Outdated**: {count} ({pct}%)

### Outdated Packages
| Package | Current | Latest | Severity |
|---------|---------|--------|----------|
| {name} | {current_version} | {latest_version} | {major/minor/patch} |

### Heavy Dependencies
**Transitive count**: {count} ({ratio}x direct count) — {HIGH|MEDIUM|LOW}

### Potentially Unmaintained
| Package | Last Updated | Risk Note |
|---------|-------------|-----------|
| {name} | {date or "unknown"} | {e.g., "No updates in 3 years"} |

### Dependency Risk Summary
| Metric | Value | Risk Level |
|--------|-------|-----------|
| Outdated packages | {N}/{total} ({pct}%) | {HIGH/MEDIUM/LOW} |
| Major version behind | {N} packages | {risk} |
| Heavy dependencies | {transitive_count} ({ratio}x) | {risk} |
| Potentially unmaintained | {N} packages | {risk} |
```

Also add the graceful degradation placeholder:
```markdown
## Dependency Risk
No package manifest detected (package.json, requirements.txt, Gemfile, Cargo.toml, go.mod). Dependency risk analysis requires a recognized package ecosystem.
```

Update the Section 7.2 (Re-Analysis Protocol) Step 4 list to include "Section 4.6 (Package-Level Dependency Risk)" in the execution sequence.

> verification: grep -q '## Dependency Risk' skills/codebase-mapper/SKILL.md
  </action>
  <verify>
grep -c '## Dependency Risk' skills/codebase-mapper/SKILL.md | xargs test 2 -le
  </verify>
  <done>CODEBASE.md template in Section 5 includes Dependency Risk section with full format and graceful degradation placeholder.</done>
</task>

<task type="auto">
  <name>Task 3: Write tests for dependency risk enrichment</name>
  <files>tests/codebase-mapper-enrichment.test.js, tests/fixtures/codebase-mapper/sample-npm-outdated.json</files>
  <action>
Create `tests/fixtures/codebase-mapper/` directory and `tests/fixtures/codebase-mapper/sample-npm-outdated.json` with sample npm outdated output.

Create `tests/codebase-mapper-enrichment.test.js` using `node:test` + `node:assert/strict`. Follow the pattern from `tests/adapter-conformance.test.js` and `tests/directory-mappings.test.js`.

Test sections to include:

**describe('Dependency Risk — SKILL.md specification')**:
- test: Section 4.6 exists in codebase-mapper SKILL.md
- test: Section 4.6 defines ecosystem detection table with npm, pip, bundle, cargo, go
- test: Section 4.6 includes outdated detection protocol
- test: Section 4.6 includes heavy dependency detection with thresholds
- test: Section 4.6 includes unmaintained package heuristic
- test: Section 4.6 includes graceful degradation for each subsection

**describe('Dependency Risk — CODEBASE.md template')**:
- test: Template includes `## Dependency Risk` section
- test: Template includes Outdated Packages table with required columns (Package, Current, Latest, Severity)
- test: Template includes Heavy Dependencies subsection
- test: Template includes Potentially Unmaintained subsection
- test: Template includes Dependency Risk Summary table
- test: Template includes graceful degradation placeholder for missing ecosystem

**describe('Dependency Risk — calibration logic')**:
- test: Helper function validates risk calibration thresholds are relative to dependency count
- test: Outdated risk levels use major/minor/patch severity categories

The tests should read `skills/codebase-mapper/SKILL.md` and validate its content structure — they are specification conformance tests, not runtime execution tests.

> verification: node --test tests/codebase-mapper-enrichment.test.js 2>&1 | tail -5
  </action>
  <verify>
node --test tests/codebase-mapper-enrichment.test.js 2>&1 | tail -5
  </verify>
  <done>Tests pass for dependency risk section format, calibration, template structure, and graceful degradation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Section 4.6 exists in skills/codebase-mapper/SKILL.md with all 6 subsections (4.6.1-4.6.6)
- [ ] CODEBASE.md template includes `## Dependency Risk` section with Outdated, Heavy, Unmaintained subsections
- [ ] Template includes graceful degradation placeholder for missing ecosystem
- [ ] All tests pass: `node --test tests/codebase-mapper-enrichment.test.js`
- [ ] No existing Section 4.4 or 4.5 content was modified (only new section added)
- [ ] Section 7.2 re-analysis protocol references Section 4.6
</verification>

<success_criteria>
- CODEBASE.md template includes `## Dependency Risk` section with outdated/unmaintained/heavy analysis
- Section 4.6 covers Node.js, Python, Ruby, Rust, Go ecosystems
- Graceful degradation documented for every skip condition
- Tests validate specification conformance
- No regressions in existing codebase-mapper sections
</success_criteria>

<output>
After completion, create `.planning/phases/08-codebase-mapper-enrichment/08-01-SUMMARY.md`

Update STATE.md with plan completion status.
</output>
