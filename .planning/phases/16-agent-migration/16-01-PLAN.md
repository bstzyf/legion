---
phase: 16-agent-migration
plan: 01
type: execute
wave: 1
depends_on:
  - 15-01
files_modified:
  - agents/*.md (51 files created)
  - agency-agents/ (entire directory removed)
autonomous: true
requirements:
  - PLUG-04
  - AGENT-01
  - AGENT-02
  - AGENT-03
user_setup: []

must_haves:
  truths:
    - "All 51 agent .md files exist under agents/ in the plugin root (PLUG-04, AGENT-01)"
    - "Every agent file has name and description frontmatter fields with human-readable values — no kebab-case IDs, no escaped newlines (AGENT-02)"
    - "Every agent file has a division frontmatter field preserving the original grouping: Engineering, Design, Marketing, Product, Project Management, Spatial Computing, Specialized, Support, Testing (AGENT-03)"
    - "No agent files remain in the old agency-agents/ directory — migration is complete, not duplicated"
  artifacts:
    - path: "agents/engineering-senior-developer.md"
      provides: "Representative migrated agent with normalized frontmatter"
      contains: "division: Engineering"
    - path: "agents/design-ui-designer.md"
      provides: "Design division agent with division field added"
      contains: "division: Design"
    - path: "agents/testing-evidence-collector.md"
      provides: "Agent with previously non-standard name (EvidenceQA) now normalized"
      contains: "name: Evidence Collector"
  key_links:
    - from: "agents/*.md"
      to: ".claude/skills/agency/agent-registry.md"
      via: "Agent filenames are the IDs used in the registry — Phase 19 updates registry paths"
      pattern: "agents/{filename}"
---

<objective>
Migrate all 51 agent personality files from `agency-agents/{division}/` to the plugin `agents/` directory with normalized, schema-compliant frontmatter.

Purpose: Move agents into the standard Claude Code plugin directory structure so they can be auto-discovered by the plugin system. Normalize inconsistent frontmatter across all 51 files.
Output: 51 agent .md files in `agents/` with `name`, `description`, `division`, and `color` frontmatter fields. Old `agency-agents/` directory removed.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-agent-migration/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all 51 agent files to agents/ with normalized frontmatter</name>
  <files>agents/*.md (51 files)</files>
  <action>
For each of the 51 agent .md files in `agency-agents/{division}/`, copy to `agents/` at the plugin root with these frontmatter transformations:

**1. Add `division` field** based on source directory:
| Source Directory | `division` Value |
|------------------|-----------------|
| `design/` | Design |
| `engineering/` | Engineering |
| `marketing/` | Marketing |
| `product/` | Product |
| `project-management/` | Project Management |
| `spatial-computing/` | Spatial Computing |
| `specialized/` | Specialized |
| `support/` | Support |
| `testing/` | Testing |

**2. Normalize `name` field** — the following 21 agents need their `name` changed from kebab-case or non-standard to human-readable:

| Filename | Current `name` | New `name` |
|----------|---------------|------------|
| engineering-senior-developer.md | `engineering-senior-developer` | `Senior Developer` |
| engineering-ai-engineer.md | `engineering-ai-engineer` | `AI Engineer` |
| marketing-instagram-curator.md | `marketing-instagram-curator` | `Instagram Curator` |
| marketing-twitter-engager.md | `marketing-twitter-engager` | `Twitter Engager` |
| marketing-growth-hacker.md | `marketing-growth-hacker` | `Growth Hacker` |
| marketing-tiktok-strategist.md | `marketing-tiktok-strategist` | `TikTok Strategist` |
| marketing-content-creator.md | `marketing-content-creator` | `Content Creator` |
| marketing-social-media-strategist.md | `marketing-social-media-strategist` | `Social Media Strategist` |
| marketing-reddit-community-builder.md | `marketing-reddit-community-builder` | `Reddit Community Builder` |
| product-trend-researcher.md | `product-trend-researcher` | `Trend Researcher` |
| product-sprint-prioritizer.md | `product-sprint-prioritizer` | `Sprint Prioritizer` |
| product-feedback-synthesizer.md | `product-feedback-synthesizer` | `Feedback Synthesizer` |
| project-manager-senior.md | `project-manager-senior` | `Senior Project Manager` |
| agents-orchestrator.md | `agents-orchestrator` | `Agents Orchestrator` |
| data-analytics-reporter.md | `data-analytics-reporter` | `Data Analytics Reporter` |
| testing-reality-checker.md | `testing-reality-checker` | `Reality Checker` |
| design-visual-storyteller.md | `design-visual-storyteller` | `Visual Storyteller` |
| testing-evidence-collector.md | `EvidenceQA` | `Evidence Collector` |
| design-ux-architect.md | `ArchitectUX` | `UX Architect` |

The remaining 32 agents already have human-readable names — leave them as-is.

**3. Clean `description` field** — the following 4 agents have escaped `\n` in descriptions. Replace with clean single-line descriptions:

| Filename | New `description` |
|----------|------------------|
| engineering-senior-developer.md | `Premium implementation specialist mastering Laravel/Livewire/FluxUI, advanced CSS, and Three.js integration` |
| project-manager-senior.md | `Converts specs to tasks with focus on realistic scope, no background processes, and exact spec requirements` |
| testing-reality-checker.md | `Stops fantasy approvals with evidence-based certification, defaults to NEEDS WORK and requires overwhelming proof for production readiness` |
| testing-evidence-collector.md | `Screenshot-obsessed, fantasy-allergic QA specialist requiring visual proof for everything and defaults to finding 3-5 issues` |

**4. Preserve everything else** — the `color` field and the entire body content (everything after the frontmatter `---` closing) must be copied verbatim.

**5. Remove `.gitkeep`** from `agents/` — no longer needed once populated.

**Implementation approach**: Process all 51 files in a single pass. For each file:
1. Read the source file from `agency-agents/{division}/{filename}.md`
2. Parse the YAML frontmatter (between `---` markers)
3. Apply transformations (add `division`, normalize `name` if needed, clean `description` if needed)
4. Write the transformed file to `agents/{filename}.md`
5. After all 51 files are written, remove `agents/.gitkeep`

Frontmatter field order in output: `name`, `description`, `division`, `color` (consistent across all files).
  </action>
  <verify>
Verify the migration produced 51 files:
```bash
ls agents/*.md | wc -l
# Expected: 51

# Spot-check frontmatter on a few files
head -6 agents/engineering-senior-developer.md
# Should show: name: Senior Developer, description: ..., division: Engineering, color: green

head -6 agents/testing-evidence-collector.md
# Should show: name: Evidence Collector, description: ..., division: Testing

head -6 agents/design-ux-architect.md
# Should show: name: UX Architect, description: ..., division: Design

# Verify no .gitkeep remains
test ! -f agents/.gitkeep && echo "gitkeep removed" || echo "ERROR: gitkeep still exists"
```
  </verify>
  <done>All 51 agent .md files written to agents/ with normalized name, clean description, added division field, and preserved color and body content.</done>
</task>

<task type="auto">
  <name>Task 2: Validate migration completeness and schema compliance</name>
  <files>agents/*.md (read-only validation)</files>
  <action>
Run a comprehensive validation across all 51 migrated agent files:

**Check 1: File count**
Verify exactly 51 `.md` files exist in `agents/`. List them and compare against the agent-registry catalog.

**Check 2: Required frontmatter fields**
For every file in `agents/`, verify the YAML frontmatter contains:
- `name` — present and non-empty
- `description` — present, non-empty, no `\n` characters
- `division` — present, value is one of: Design, Engineering, Marketing, Product, Project Management, Spatial Computing, Specialized, Support, Testing

**Check 3: Name normalization**
Verify NO agent has a kebab-case `name` (no hyphens in name field). All names should be human-readable.

**Check 4: Division distribution**
Count agents per division and verify:
- Design: 6
- Engineering: 7
- Marketing: 8
- Product: 3
- Project Management: 5
- Spatial Computing: 6
- Specialized: 3
- Support: 6
- Testing: 7

**Check 5: Content preservation**
Spot-check 5 random agents to verify the body content (everything after frontmatter) matches the original file exactly.

If any check fails, report the specific failures. Do NOT proceed to Task 3 until all checks pass.
  </action>
  <verify>
All 5 checks pass with zero failures. Output a summary:
```
Migration Validation: PASS
- Files: 51/51
- Frontmatter: 51/51 schema-compliant
- Names: 0 kebab-case remaining
- Divisions: all 9 divisions correct counts
- Content: preserved (5/5 spot checks passed)
```
  </verify>
  <done>All 51 agent files validated. Schema-compliant frontmatter confirmed. Content preserved. Ready for cleanup.</done>
</task>

<task type="auto">
  <name>Task 3: Remove old agency-agents/ directory</name>
  <files>agency-agents/ (entire directory removed)</files>
  <action>
Remove the old `agency-agents/` directory and all its contents:

```bash
rm -rf agency-agents/
```

This removes:
- 9 division subdirectories with 51 agent .md files (now in `agents/`)
- `agency-agents/README.md` (describes old structure — no longer relevant)
- `agency-agents/CONTRIBUTING.md` (describes old agent creation — superseded by `/agency:agent` command)

**Why remove entirely**: The success criteria require "No agent files remain in the old `agency-agents/` directory." The README and CONTRIBUTING files describe the old structure and would be confusing to keep alongside the new plugin structure.

IMPORTANT: Verify the `agents/` directory has 51 files BEFORE removing `agency-agents/`. This is a destructive operation — confirm the migration is validated (Task 2 passed) before proceeding.
  </action>
  <verify>
Verify old directory is gone and new directory is intact:
```bash
# Old directory should not exist
test ! -d agency-agents && echo "Old directory removed" || echo "ERROR: agency-agents/ still exists"

# New directory should have 51 files
ls agents/*.md | wc -l
# Expected: 51
```
  </verify>
  <done>Old agency-agents/ directory removed. All 51 agents now exclusively in agents/. Migration complete — no duplication.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Exactly 51 `.md` files exist in `agents/`
- [ ] Every file has `name`, `description`, `division`, `color` in frontmatter
- [ ] All `name` values are human-readable (no kebab-case, no compressed names)
- [ ] All `description` values are clean single-line strings (no `\n` artifacts)
- [ ] All `division` values match one of the 9 expected divisions
- [ ] Division counts match: Design(6), Engineering(7), Marketing(8), Product(3), PM(5), Spatial(6), Specialized(3), Support(6), Testing(7)
- [ ] `agency-agents/` directory no longer exists
- [ ] `agents/.gitkeep` no longer exists
- [ ] Body content of all agent files preserved exactly from originals
</verification>

<success_criteria>
- All 3 tasks completed
- All verification checks pass
- PLUG-04 satisfied: agents/ has all 51 .md files
- AGENT-01 satisfied: flat directory, plugin-compatible frontmatter
- AGENT-02 satisfied: name and description fields present and schema-compliant
- AGENT-03 satisfied: division field preserves original grouping
- No agent files in old location
</success_criteria>

<output>
After completion, create `.planning/phases/16-agent-migration/16-01-SUMMARY.md`
</output>
