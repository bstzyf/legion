---
phase: 11-github-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/agency/github-sync.md
  - .claude/skills/agency/workflow-common.md
autonomous: true
requirements:
  - GH-01
  - GH-02
  - GH-03

must_haves:
  truths:
    - "github-sync skill provides prerequisite checks (gh auth, remote detection) that silently skip all operations when GitHub is unavailable"
    - "Issue creation produces one GitHub issue per phase with structured body (goal, plans checklist, requirements, success criteria)"
    - "PR creation produces one PR per phase with aggregate plan summaries and requirement references"
    - "Milestone sync maps ROADMAP.md milestones to GitHub milestones via gh api"
    - "All GitHub metadata (issue numbers, PR numbers, milestone numbers) stored in STATE.md ## GitHub section"
  artifacts:
    - path: ".claude/skills/agency/github-sync.md"
      provides: "Complete GitHub operations engine — prerequisites, issues, PRs, milestones, status readback, state linking, error handling, graceful degradation"
      min_lines: 350
    - path: ".claude/skills/agency/workflow-common.md"
      provides: "Updated with GitHub Conventions section"
      contains: "GitHub Conventions"
  key_links:
    - from: ".claude/skills/agency/github-sync.md"
      to: ".planning/STATE.md"
      via: "GitHub metadata table in ## GitHub section"
      pattern: "STATE\\.md"
    - from: ".claude/skills/agency/github-sync.md"
      to: ".planning/ROADMAP.md"
      via: "milestone reading and phase goal extraction"
      pattern: "ROADMAP\\.md"
---

<objective>
Create the github-sync skill — the complete GitHub operations engine that handles issue creation, PR creation, milestone sync, status readback, and graceful degradation when no GitHub remote exists.

Purpose: This skill is the core implementation for all three requirements (GH-01 issue tracking, GH-02 PR creation, GH-03 status sync). It follows the established skill pattern from memory-manager.md and agent-creator.md.
Output: `.claude/skills/agency/github-sync.md` (new skill), updated `workflow-common.md`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-github-integration/11-RESEARCH.md
@.planning/phases/11-github-integration/11-CONTEXT.md

@.claude/skills/agency/workflow-common.md
@.claude/skills/agency/memory-manager.md
@.claude/skills/agency/execution-tracker.md
@.claude/skills/agency/milestone-tracker.md

<interfaces>
<!-- Existing skill structure pattern (from memory-manager.md): -->
<!-- YAML frontmatter with name + description -->
<!-- Intro paragraph with references to other skills -->
<!-- Numbered sections with specific responsibilities -->
<!-- Each section contains rules, formats, examples, and error handling -->

From .claude/skills/agency/workflow-common.md:
- State File Locations table (add GitHub metadata info)
- Memory Conventions section (model for graceful degradation pattern)
- Milestone Conventions section (model for GitHub milestone sync integration)
- State Update Pattern (how STATE.md is read → updated → written)

From .claude/skills/agency/execution-tracker.md:
- Section 2: Plan Completion Tracking (where PR creation step would be referenced)
- Section 4: Phase Completion Tracking (where PR creation fits in the lifecycle)
- Section 6: Commit Message Convention (model for PR description format)

From .claude/skills/agency/milestone-tracker.md:
- Milestone definition format in ROADMAP.md (milestone names, phase ranges)
- Milestone lifecycle states (Pending → In Progress → Complete → Archived)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create github-sync.md skill</name>
  <files>.claude/skills/agency/github-sync.md</files>
  <action>
Create `.claude/skills/agency/github-sync.md` — a new skill file following the established pattern (YAML frontmatter, intro, numbered sections). Target 350-450 lines.

**Frontmatter:**
```yaml
---
name: agency:github-sync
description: GitHub integration — issue tracking, PR creation, milestone sync, and status readback via gh CLI
---
```

**Intro paragraph:** Reference workflow-common.md (state paths, degradation pattern), execution-tracker.md (commit conventions, phase lifecycle), and milestone-tracker.md (milestone format). State that all operations require `gh` CLI and a GitHub remote, and all operations degrade gracefully when unavailable.

**Section 1: Prerequisites & Detection** — How to check if GitHub operations are available:

```
GitHub Availability Check (run before ANY GitHub operation):

Step 1: Check gh CLI authentication
  Command: gh auth status 2>&1
  - If exit code 0: gh is authenticated, proceed
  - If non-zero: GitHub unavailable, skip all operations silently

Step 2: Check for GitHub remote
  Command: git remote get-url origin 2>/dev/null
  - If exit code 0: remote exists, proceed
  - If non-zero: no remote, skip all operations silently

Step 3: Get repo metadata (cache for session)
  Command: gh repo view --json nameWithOwner,defaultBranch -q '.nameWithOwner + " " + .defaultBranch'
  - Parse: REPO_SLUG = first token, DEFAULT_BRANCH = second token
  - Cache these values — do not re-query within the same command run

The result of this check is a boolean: github_available = true/false
All subsequent sections assume this check has been performed.
If github_available is false, the calling workflow skips the GitHub section entirely.
```

Define constants:
```
AGENCY_LABEL = "agency"
AGENCY_LABEL_COLOR = "7B68EE"
AGENCY_LABEL_DESCRIPTION = "Created by Agency Workflows"
BRANCH_PREFIX = "agency/phase-"
```

**Section 2: Issue Management (GH-01)** — Creating and managing issues linked to phases:

Subsection 2.1: Ensure Label Exists
```
Before first issue creation in a session, ensure the "agency" label exists:
  Command: gh label create agency --description "Created by Agency Workflows" --color 7B68EE 2>/dev/null
  (This is a no-op if the label already exists — gh returns non-zero but creates nothing)
```

Subsection 2.2: Create Phase Issue
```
Input: phase_number, phase_name, phase_goal, plans_list, requirements, success_criteria
Output: issue_number (integer)

Command:
  gh issue create \
    --title "Phase {phase_number}: {phase_name}" \
    --body "{ISSUE_BODY}" \
    --label "agency" \
    {--milestone "MILESTONE_TITLE" if a GitHub milestone exists for the current ROADMAP milestone}

ISSUE_BODY format:
  ## Goal
  {phase_goal}

  ## Plans
  - [ ] Plan {NN}-01: {plan_1_name}
  - [ ] Plan {NN}-02: {plan_2_name}
  ...

  ## Requirements
  {requirement_ids, comma-separated}

  ## Success Criteria
  {success_criteria as bullet list}

  ---
  *Created by Agency Workflows*

Capture issue number:
  Parse the URL returned by gh issue create — the last path segment is the issue number.
  Or use: gh issue create ... --json number -q .number (if supported)
  Fallback: gh issue list --label agency --state open --json number,title -q '.[0].number'
```

Subsection 2.3: Close Phase Issue
```
Input: issue_number, comment_text
Command:
  gh issue close {issue_number} --comment "{comment_text}"
```

Subsection 2.4: Update Issue Checklist
```
After a plan completes during /agency:build, update the issue body:
- Read current issue body: gh issue view {issue_number} --json body -q .body
- Find the "- [ ] Plan {NN}-{PP}" line and replace with "- [x] Plan {NN}-{PP}"
- Update: gh issue edit {issue_number} --body "{updated_body}"
```

**Section 3: PR Creation (GH-02)** — Creating PRs after phase execution:

Subsection 3.1: Branch Management
```
Step 1: Check current branch
  Command: git branch --show-current
  - If result equals DEFAULT_BRANCH (from Section 1):
    Create feature branch: git checkout -b {BRANCH_PREFIX}{NN}-{slug}
    (e.g., agency/phase-03-phase-planning)
  - If result is NOT the default branch:
    Use the current branch as-is

Step 2: Check for unpushed commits
  Command: git log origin/{current_branch}..HEAD --oneline 2>/dev/null
  - If the remote tracking branch doesn't exist: all local commits are unpushed
  - Count unpushed commits for the PR description

Step 3: Push to remote
  Command: git push -u origin {current_branch}
  - If push fails: report error, do NOT create PR
```

Subsection 3.2: Create Phase PR
```
Input: phase_number, phase_name, phase_goal, plan_summaries[], files_modified[], requirements[], issue_number (optional)
Output: pr_number, pr_url

Command:
  gh pr create \
    --title "Phase {phase_number}: {phase_name}" \
    --body "{PR_BODY}" \
    --base {DEFAULT_BRANCH}

PR_BODY format:
  ## Summary
  {phase_goal}

  ## Changes
  {files_modified as bullet list, grouped by type}

  ## Plans Executed
  | Plan | Wave | Status | Summary |
  |------|------|--------|---------|
  | {NN}-{PP} | {W} | Pass | {one-line from SUMMARY.md} |
  ...

  ## Requirements Satisfied
  {requirement_ids as bullet list}

  {If issue_number: "Closes #{issue_number}"}

  ---
  *Created by Agency Workflows*

Capture PR number:
  Parse from the gh pr create output URL, or use --json number if available.
```

**Section 4: Milestone Sync (GH-03)** — Mapping ROADMAP milestones to GitHub milestones:

Subsection 4.1: Create GitHub Milestone
```
Input: milestone_name, milestone_description, phase_range
Output: github_milestone_number

Step 1: Check if milestone already exists
  Command: gh api "repos/{REPO_SLUG}/milestones" --jq '.[].title'
  - If milestone_name is in the list: return its number, do not create duplicate

Step 2: Create milestone
  Command:
    gh api "repos/{REPO_SLUG}/milestones" \
      --method POST \
      -f title="{milestone_name}" \
      -f description="Phases {start}-{end}: {milestone_description}"

  Parse number from JSON response: .number
```

Subsection 4.2: Close GitHub Milestone
```
Input: github_milestone_number
Command:
  gh api "repos/{REPO_SLUG}/milestones/{github_milestone_number}" \
    --method PATCH \
    -f state=closed
```

**Section 5: Status Readback (GH-03)** — Fetching live GitHub status for the dashboard:

```
Purpose: Used by /agency:status to display current GitHub state.
Do NOT rely on STATE.md values for status — always fetch live from GitHub.
STATE.md stores the LINKING information (which issue/PR belongs to which phase).

Step 1: Read STATE.md ## GitHub section to get issue/PR numbers per phase

Step 2: Batch-fetch issue status
  Command: gh issue list --label agency --state all --json number,title,state --limit 100
  Parse into a map: {number: state}

Step 3: Batch-fetch PR status
  Command: gh pr list --state all --json number,title,state,mergedAt --limit 100
  Parse into a map: {number: {state, merged}}

Step 4: Batch-fetch milestone status
  Command: gh api "repos/{REPO_SLUG}/milestones?state=all" --jq '.[] | {number, title, state, open_issues, closed_issues}'

Step 5: Return structured data for the dashboard:
  {
    issues: [{phase, number, title, state}],
    prs: [{phase, number, title, state, merged}],
    milestones: [{name, number, state, open_issues, closed_issues}]
  }

If any gh command fails during readback: return partial data with a note.
Never block the status dashboard because GitHub is unreachable.
```

**Section 6: State Linking** — How GitHub metadata is stored in STATE.md:

```
GitHub metadata is stored in a ## GitHub section at the END of STATE.md (after Next Action):

## GitHub

| Phase | Issue | PR | Status |
|-------|-------|----|--------|
| Phase 1: Plugin Foundation | #1 | #10 | Merged |
| Phase 2: Project Initialization | #2 | #11 | Merged |
| Phase 3: Phase Planning | #3 | — | Open |

### Milestones

| Milestone | GitHub # | Status |
|-----------|----------|--------|
| v1.0: Core Workflows | #1 | Open |

Update rules:
- Add a row when an issue or PR is created
- Update PR column when PR is created (replace "—" with PR number)
- Do NOT update Status column in STATE.md — status is always fetched live
- Milestone rows added when synced to GitHub
```

**Section 7: Error Handling** — Cover these scenarios:

```
e1: gh not installed
  Detection: `which gh` returns non-zero
  Action: Skip all GitHub operations. No warning, no suggestion.

e2: gh not authenticated
  Detection: gh auth status returns non-zero
  Action: Skip all operations. Suggest: "Run `gh auth login` to enable GitHub integration."
  Note: Only suggest ONCE per session, not on every skipped operation.

e3: No GitHub remote
  Detection: git remote get-url origin returns non-zero
  Action: Skip all operations silently. Do not suggest adding a remote.

e4: Issue creation fails
  Detection: gh issue create returns non-zero
  Action: Log the error text. Continue workflow — issue creation is optional.
  Workflow should NOT abort because GitHub issue creation failed.

e5: PR creation fails
  Detection: gh pr create returns non-zero
  Action: Report the error to the user. The phase execution is still valid.
  Common causes: branch already has a PR, no commits to diff, permission denied.

e6: Milestone API fails
  Detection: gh api returns non-zero
  Action: Skip milestone operations. Issues can still be created without milestone assignment.

e7: Rate limiting (HTTP 403/429)
  Detection: gh api returns rate limit error
  Action: Log the warning. Return partial data. Do not retry automatically.
```

**Section 8: Graceful Degradation** — Same pattern as Memory Conventions:

```
All GitHub integration follows this pattern:
1. Run Prerequisites check (Section 1)
2. If github_available is true: perform the GitHub operation
3. If github_available is false: skip silently, proceed with default behavior
4. Never error, never block, never require GitHub for workflow completion
5. If a GitHub operation fails mid-way: log the error, continue the workflow
6. The system works identically with or without GitHub integration

Caller pattern (used by plan.md, build.md, status.md, review.md):
  # Check GitHub availability
  # Run: gh auth status 2>&1 && git remote get-url origin 2>/dev/null
  # If both succeed: github_available = true
  # If either fails: github_available = false, skip GitHub steps
```
  </action>
  <verify>
    <automated>wc -l < .claude/skills/agency/github-sync.md | awk '{print ($1 >= 350) ? "PASS: "$1" lines" : "FAIL: "$1" lines (need 350+)"}'</automated>
    <automated>head -1 .claude/skills/agency/github-sync.md | grep -q "^---$" && echo "PASS: frontmatter present" || echo "FAIL: missing frontmatter"</automated>
    <automated>grep -c "^## Section [0-9]" .claude/skills/agency/github-sync.md | awk '{print ($1 >= 8) ? "PASS: "$1" sections" : "FAIL: "$1" sections (need 8)"}'</automated>
    <automated>grep -q "Prerequisites" .claude/skills/agency/github-sync.md && grep -q "Graceful Degradation" .claude/skills/agency/github-sync.md && echo "PASS: key sections present" || echo "FAIL: missing key sections"</automated>
  </verify>
  <done>github-sync.md exists with 8 sections (Prerequisites, Issue Management, PR Creation, Milestone Sync, Status Readback, State Linking, Error Handling, Graceful Degradation), 350+ lines, YAML frontmatter present, follows established skill file structure</done>
</task>

<task type="auto">
  <name>Task 2: Update workflow-common.md with GitHub Conventions</name>
  <files>.claude/skills/agency/workflow-common.md</files>
  <action>
Update `.claude/skills/agency/workflow-common.md` with a new ## GitHub Conventions section. Use the Edit tool for surgical edits.

**Change 1: Add GitHub Conventions section after Memory Conventions**

After the Memory Conventions section (which ends with the graceful degradation pattern), add:

```markdown
## GitHub Conventions

### GitHub Purpose
Optional integration that connects Agency Workflows to GitHub — phases link to issues, completed work produces PRs, and milestones sync. All operations use the `gh` CLI and are entirely opt-in.

### GitHub Prerequisites
```
1. gh CLI installed (checked via `which gh`)
2. gh authenticated (checked via `gh auth status`)
3. Git remote pointing to GitHub (checked via `git remote get-url origin`)
All three must pass for GitHub operations to be available.
```

### GitHub Lifecycle
```
Unavailable → Available (prerequisites pass) → Active (first issue created) → Synced (milestones linked)
```
- **Unavailable**: No gh CLI, no auth, or no remote. All GitHub operations skip silently.
- **Available**: Prerequisites pass. Operations can be performed.
- **Active**: At least one GitHub issue has been created. STATE.md has a ## GitHub section.
- **Synced**: ROADMAP milestones have corresponding GitHub milestones.

### GitHub Paths
| Artifact | Location | When Created |
|----------|----------|-------------|
| GitHub metadata | `.planning/STATE.md ## GitHub` section | On first issue or PR creation |
| Agency label | GitHub repo labels | On first issue creation |
| Phase issues | GitHub issues with "agency" label | During `/agency:plan` |
| Phase PRs | GitHub pull requests | During `/agency:build` |
| GitHub milestones | GitHub API milestones | During `/agency:plan` (if ROADMAP milestones defined) |

### GitHub Integration Points
| Workflow | Operation | When |
|----------|-----------|------|
| `/agency:plan` | Create phase issue, sync milestone | After plan files generated |
| `/agency:build` | Update issue checklist, create PR | After plans execute and after phase completes |
| `/agency:status` | Read-back live GitHub status | During dashboard display |
| `/agency:review` | Close phase issue | After review passes |
| `/agency:milestone` | Close GitHub milestone | After milestone completion |

### Graceful Degradation Rule
All GitHub integration follows this pattern:
1. Run github-sync Section 1 prerequisites check
2. If github_available is true: perform the operation
3. If github_available is false: skip silently, proceed with default behavior
4. Never error, never block, never require GitHub for workflow completion
```

Use the Edit tool — insert the new section after the Memory Conventions section.
  </action>
  <verify>
    <automated>grep -c "GitHub Conventions" .claude/skills/agency/workflow-common.md</automated>
  </verify>
  <done>workflow-common.md contains GitHub Conventions section with prerequisites, lifecycle, paths, integration points, and graceful degradation rule</done>
</task>

</tasks>

<verification>
- github-sync.md exists at `.claude/skills/agency/github-sync.md`
- github-sync.md has YAML frontmatter with `name: agency:github-sync`
- github-sync.md has 8 numbered sections
- github-sync.md is 350+ lines
- github-sync.md Section 1 checks gh auth and remote
- github-sync.md Section 8 defines graceful degradation identical to memory pattern
- workflow-common.md has a ## GitHub Conventions section
- workflow-common.md GitHub section includes Prerequisites, Lifecycle, Paths, Integration Points, Degradation Rule
</verification>

<success_criteria>
- The github-sync skill provides a complete, self-contained GitHub operations engine
- All operations degrade gracefully when GitHub is unavailable
- workflow-common.md reflects the new GitHub integration paths and conventions
- Both files follow established project conventions (markdown skills, no external dependencies)
</success_criteria>

<output>
After completion, create `.planning/phases/11-github-integration/11-01-SUMMARY.md`
</output>
