---
phase: 24-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/workflow-common/SKILL.md
autonomous: true
requirements:
  - SKL-01

must_haves:
  truths:
    - "Zero case-insensitive occurrences of 'agency' remain in skills/workflow-common/SKILL.md"
    - "Frontmatter name is 'legion:workflow-common' and description is 'Shared constants, paths, and patterns for all /legion: commands'"
    - "Main heading reads '# Legion Workflow Common'"
    - "Portfolio path constant is '~/.claude/legion/portfolio.md'"
    - "GitHub label constant is 'legion' (not 'agency')"
    - "All 40 /agency: command references are now /legion: command references"
    - "All brand text reads 'Legion' not 'Agency'"
  artifacts:
    - path: "skills/workflow-common/SKILL.md"
      provides: "Shared constants layer with /legion: namespace — foundation for all subsequent phases"
      contains: "legion:workflow-common"
  key_links:
    - from: "skills/workflow-common/SKILL.md"
      to: "commands/*.md"
      via: "shared constants consumed by all 10 commands"
      pattern: "/legion:"
    - from: "skills/workflow-common/SKILL.md"
      to: "skills/*/SKILL.md"
      via: "shared conventions consumed by all 17 skills"
      pattern: "/legion:"
---

<objective>
Rebrand skills/workflow-common/SKILL.md from /agency: namespace to /legion: namespace — the foundation layer that all commands and skills consume.

Purpose: Establish /legion: as the canonical namespace in the shared constants file (requirement SKL-01). This must land before Phase 25 (commands) and Phase 26 (skills) can proceed.
Output: Updated `skills/workflow-common/SKILL.md` with zero "agency" references.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-foundation/24-CONTEXT.md
@.planning/phases/24-foundation/24-RESEARCH.md
@.planning/REQUIREMENTS.md

@skills/workflow-common/SKILL.md

<interfaces>
From 24-CONTEXT.md — 4 locked decisions:
1. Portfolio path: clean break to ~/.claude/legion/portfolio.md
2. GitHub label: "agency" → "legion"
3. Skill identity: specific locked text for frontmatter, heading, intro
4. General rename: every "Agency" → "Legion", every /agency: → /legion:, zero remnants

From 24-RESEARCH.md — Substitution map:
- Pattern A: 40 /agency: → /legion: command references
- Pattern B: 3 frontmatter/heading replacements (locked text, NOT simple find-replace)
- Pattern C: 2 filesystem path replacements (~/.claude/agency/ → ~/.claude/legion/)
- Pattern D: ~2 brand text replacements ("Agency" → "Legion" in prose)
- Total: 47 case-insensitive occurrences
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read current file and verify baseline count</name>
  <files>skills/workflow-common/SKILL.md</files>
  <action>
Read the full content of `skills/workflow-common/SKILL.md` (374 lines).

Run verification: `grep -in "agency" skills/workflow-common/SKILL.md | wc -l`
Expected result: 47 occurrences. If the count differs from 47, stop and reconcile against the RESEARCH.md substitution map before proceeding.

This establishes the pre-condition baseline.
  </action>
  <verify>
    <automated>grep -ic "agency" skills/workflow-common/SKILL.md</automated>
  </verify>
  <done>Baseline count confirmed at 47 (or reconciled if different). Full file content is in context.</done>
</task>

<task type="auto">
  <name>Task 2: Apply complete substitution and write transformed file</name>
  <files>skills/workflow-common/SKILL.md</files>
  <action>
Apply ALL substitutions to the file content in a single Write operation. Do NOT use incremental Edit calls — a single atomic Write prevents partial state.

**Pattern B — Frontmatter and heading (use locked text exactly):**

Line 2: `name: agency:workflow-common` → `name: legion:workflow-common`

Line 3: `description: Shared workflow patterns and conventions for The Agency plugin` → `description: Shared constants, paths, and patterns for all /legion: commands`

Line 6: `# Agency Workflow Common` → `# Legion Workflow Common`

Line 8: `Shared constants, paths, and patterns used across all /agency: commands.` → `Shared constants, paths, and patterns used across all /legion: commands.`

**Pattern C — Filesystem paths:**

Line 19: `~/.claude/agency/portfolio.md` → `~/.claude/legion/portfolio.md` AND `all Agency projects` → `all Legion projects`

Line 140: `~/.claude/agency/portfolio.md` → `~/.claude/legion/portfolio.md` AND `all Agency projects` → `all Legion projects`

**Pattern D — Brand text:**

Line 217: `Agency Workflows` → `Legion Workflows`

Line 240: `Agency label` → `Legion label`

Line 241: `"agency" label` → `"legion" label`

**Pattern A — All 40 command references:**

Global: every `/agency:` → `/legion:` across lines 23, 40, 103-105, 116, 143, 151, 156, 177, 202-205, 241-243, 248-252, 271-272, 277, 282-283, 302-303, 310, 322-324, 345, 351-352, 364-366.

Write the fully transformed content back to `skills/workflow-common/SKILL.md` in one operation.
  </action>
  <verify>
    <automated>grep -ic "agency" skills/workflow-common/SKILL.md</automated>
  </verify>
  <done>File written. grep -ic "agency" returns 0. All 47 occurrences transformed.</done>
</task>

<task type="auto">
  <name>Task 3: Post-write verification</name>
  <files>skills/workflow-common/SKILL.md</files>
  <action>
Run three verification checks:

1. **Zero agency remnants:** `grep -in "agency" skills/workflow-common/SKILL.md`
   Expected: empty output (zero matches). Any match = incomplete substitution.

2. **Correct legion count:** `grep -ic "legion" skills/workflow-common/SKILL.md`
   Expected: 47 (every former "agency" is now "legion"). If lower, a substitution was dropped. If higher, an unrelated insertion happened.

3. **Spot-check locked text:** Read the file and verify these exact strings exist:
   - Line 2: `name: legion:workflow-common`
   - Line 3: `description: Shared constants, paths, and patterns for all /legion: commands`
   - Line 6: `# Legion Workflow Common`
   - Line 19 contains: `~/.claude/legion/portfolio.md`
   - Line 140 contains: `~/.claude/legion/portfolio.md`
   - Line 240 contains: `Legion label`
   - Line 241 contains: `"legion" label`

If any check fails, identify which substitution was missed and apply a targeted Edit to fix it, then re-run all three checks.
  </action>
  <verify>
    <automated>grep -ic "agency" skills/workflow-common/SKILL.md</automated>
  </verify>
  <done>All three verification checks pass: zero "agency" hits, correct "legion" count, locked text spot-checks confirmed.</done>
</task>

</tasks>

<verification>
- `grep -ic "agency" skills/workflow-common/SKILL.md` returns 0
- `grep -ic "legion" skills/workflow-common/SKILL.md` returns 47
- Frontmatter name is `legion:workflow-common`
- Frontmatter description matches locked text exactly
- Main heading is `# Legion Workflow Common`
- Portfolio path is `~/.claude/legion/portfolio.md` (both occurrences)
- GitHub label is `"legion"` (not `"agency"`)
- File line count is still ~374 (no lines added or removed)
</verification>

<success_criteria>
Phase 24 success criteria (from ROADMAP.md):
1. workflow-common skill file contains no /agency: references ✓ (verified by grep)
2. All shared constants, command routing strings, and documentation use /legion: ✓ (47 substitutions applied)
3. A developer reading workflow-common sees Legion as the identity with no Agency remnants ✓ (zero "agency" hits)
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundation/24-01-SUMMARY.md`
</output>
