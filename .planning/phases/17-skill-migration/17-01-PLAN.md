---
phase: 17-skill-migration
plan: 01
type: execute
wave: 1
depends_on:
  - 15-01
  - 16-01
files_modified:
  - skills/*/SKILL.md (15 directories created, 15 SKILL.md files)
  - skills/questioning-flow/templates/*.md (3 template files)
  - skills/.gitkeep (removed)
autonomous: true
requirements:
  - PLUG-03
  - SKILL-01
  - SKILL-02
  - SKILL-03
user_setup: []

must_haves:
  truths:
    - "All 15 skills exist as skills/{name}/SKILL.md directories under the plugin root (PLUG-03, SKILL-01)"
    - "Every SKILL.md has name and description frontmatter fields — no changes needed, existing values already match schema (SKILL-02)"
    - "Template files from .planning/templates/ are co-located at skills/questioning-flow/templates/ (SKILL-03)"
    - "No skill files remain in the old .claude/skills/agency/ location"
  artifacts:
    - path: "skills/agent-registry/SKILL.md"
      provides: "Representative migrated skill with preserved frontmatter"
      contains: "name: agency:agent-registry"
    - path: "skills/questioning-flow/SKILL.md"
      provides: "Skill with co-located template supporting files"
      contains: "name: agency:questioning-flow"
    - path: "skills/questioning-flow/templates/project-template.md"
      provides: "Template file bundled with its parent skill"
      contains: "Template: PROJECT.md"
    - path: "skills/workflow-common/SKILL.md"
      provides: "Shared skill with updated template path reference"
      contains: "skills/questioning-flow/templates/"
  key_links:
    - from: "skills/*/SKILL.md"
      to: "commands/*.md"
      via: "Commands reference skills via @skills/{name}/SKILL.md — Phase 18 updates these paths"
      pattern: "skills/{name}/SKILL.md"
---

<objective>
Migrate all 15 skills from `.claude/skills/agency/{name}.md` to plugin `skills/{name}/SKILL.md` directory structure. Co-locate template files with the questioning-flow skill. Update the template path reference in workflow-common. Remove old files.

Purpose: Convert skills into the standard Claude Code plugin directory format so they're auto-discovered by the plugin system and distributable via `claude plugin add`.
Output: 15 skill directories each with a SKILL.md, templates bundled in questioning-flow, old `.claude/skills/agency/` removed.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-skill-migration/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill directories and migrate all 15 SKILL.md files</name>
  <files>skills/*/SKILL.md (15 directories, 15 files)</files>
  <action>
For each of the 15 skill `.md` files in `.claude/skills/agency/`, create a directory under `skills/` and copy the file as `SKILL.md`:

**Directory mapping** (source → target):

| Source File | Target Directory | Target File |
|-------------|-----------------|-------------|
| `.claude/skills/agency/agent-creator.md` | `skills/agent-creator/` | `SKILL.md` |
| `.claude/skills/agency/agent-registry.md` | `skills/agent-registry/` | `SKILL.md` |
| `.claude/skills/agency/codebase-mapper.md` | `skills/codebase-mapper/` | `SKILL.md` |
| `.claude/skills/agency/design-workflows.md` | `skills/design-workflows/` | `SKILL.md` |
| `.claude/skills/agency/execution-tracker.md` | `skills/execution-tracker/` | `SKILL.md` |
| `.claude/skills/agency/github-sync.md` | `skills/github-sync/` | `SKILL.md` |
| `.claude/skills/agency/marketing-workflows.md` | `skills/marketing-workflows/` | `SKILL.md` |
| `.claude/skills/agency/memory-manager.md` | `skills/memory-manager/` | `SKILL.md` |
| `.claude/skills/agency/milestone-tracker.md` | `skills/milestone-tracker/` | `SKILL.md` |
| `.claude/skills/agency/phase-decomposer.md` | `skills/phase-decomposer/` | `SKILL.md` |
| `.claude/skills/agency/portfolio-manager.md` | `skills/portfolio-manager/` | `SKILL.md` |
| `.claude/skills/agency/questioning-flow.md` | `skills/questioning-flow/` | `SKILL.md` |
| `.claude/skills/agency/review-loop.md` | `skills/review-loop/` | `SKILL.md` |
| `.claude/skills/agency/wave-executor.md` | `skills/wave-executor/` | `SKILL.md` |
| `.claude/skills/agency/workflow-common.md` | `skills/workflow-common/` | `SKILL.md` |

**For each file:**
1. Create the target directory: `mkdir -p skills/{name}/`
2. Copy the source file verbatim: `cp .claude/skills/agency/{name}.md skills/{name}/SKILL.md`
3. Content is copied exactly as-is — no frontmatter changes, no body modifications

**Frontmatter preservation**: Every skill already has `name` and `description` fields that match the Claude Code skill schema. Do not modify them.

**Remove `.gitkeep`**: Delete `skills/.gitkeep` after creating the directories.

**Implementation approach**: Process all 15 in a single batch using a shell loop:
```bash
for skill in agent-creator agent-registry codebase-mapper design-workflows execution-tracker github-sync marketing-workflows memory-manager milestone-tracker phase-decomposer portfolio-manager questioning-flow review-loop wave-executor workflow-common; do
  mkdir -p "skills/$skill"
  cp ".claude/skills/agency/$skill.md" "skills/$skill/SKILL.md"
done
rm -f skills/.gitkeep
```
  </action>
  <verify>
Verify 15 directories exist, each with a SKILL.md:
```bash
# Count skill directories
ls -d skills/*/  | wc -l
# Expected: 15

# Count SKILL.md files
ls skills/*/SKILL.md | wc -l
# Expected: 15

# Spot-check frontmatter
head -4 skills/agent-registry/SKILL.md
# Should show: name: agency:agent-registry

head -4 skills/wave-executor/SKILL.md
# Should show: name: agency:wave-executor

# Verify .gitkeep removed
test ! -f skills/.gitkeep && echo "gitkeep removed" || echo "ERROR: gitkeep still exists"
```
  </verify>
  <done>All 15 skills migrated to skills/{name}/SKILL.md directories with frontmatter and content preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Co-locate template files with questioning-flow skill and update reference</name>
  <files>
    skills/questioning-flow/templates/*.md (3 files created)
    skills/workflow-common/SKILL.md (1 path reference updated)
  </files>
  <action>
**Step 1: Move templates to questioning-flow skill directory**

Copy the 3 template files from `.planning/templates/` to `skills/questioning-flow/templates/`:

```bash
mkdir -p skills/questioning-flow/templates
cp .planning/templates/project-template.md skills/questioning-flow/templates/
cp .planning/templates/roadmap-template.md skills/questioning-flow/templates/
cp .planning/templates/state-template.md skills/questioning-flow/templates/
```

These templates are plugin assets used by `/agency:start` (via the questioning-flow skill) to generate PROJECT.md, ROADMAP.md, and STATE.md. They belong with the skill, not in project-level `.planning/`.

**Step 2: Update template path reference in workflow-common**

In `skills/workflow-common/SKILL.md`, find the line:
```
| Templates | `.planning/templates/` | Schema files for generating PROJECT/ROADMAP/STATE |
```

Replace with:
```
| Templates | `skills/questioning-flow/templates/` | Schema files for generating PROJECT/ROADMAP/STATE |
```

This is the ONLY cross-reference update in this phase. All other path updates (command references, skill cross-references) are Phase 18's responsibility.

**Do NOT remove `.planning/templates/`** — the templates in `.planning/` are still used by the current `.claude/` commands until Phase 18 migrates those commands. Removing them now would break the working plugin. Phase 18 will clean up the old location.
  </action>
  <verify>
```bash
# Verify templates exist in new location
ls skills/questioning-flow/templates/
# Expected: project-template.md  roadmap-template.md  state-template.md

# Verify content preserved
head -5 skills/questioning-flow/templates/project-template.md
# Should show the HTML comment and # {project_name}

# Verify workflow-common reference updated
grep "templates/" skills/workflow-common/SKILL.md
# Should show: skills/questioning-flow/templates/
# Should NOT show: .planning/templates/
```
  </verify>
  <done>3 template files co-located in skills/questioning-flow/templates/. Workflow-common path reference updated.</done>
</task>

<task type="auto">
  <name>Task 3: Validate migration and remove old skill files</name>
  <files>
    .claude/skills/agency/ (entire directory removed)
    skills/*/SKILL.md (read-only validation)
  </files>
  <action>
**Step 1: Comprehensive validation**

Run all validation checks before removing old files:

**Check 1: Directory count**
Verify exactly 15 skill directories exist under `skills/`:
```bash
ls -d skills/*/ | wc -l
# Expected: 15
```

**Check 2: SKILL.md presence**
Verify every directory has a SKILL.md:
```bash
for dir in skills/*/; do
  test -f "$dir/SKILL.md" || echo "MISSING: $dir/SKILL.md"
done
# Expected: no output (all present)
```

**Check 3: Frontmatter compliance**
For every SKILL.md, verify `name:` and `description:` fields exist in frontmatter:
```bash
for f in skills/*/SKILL.md; do
  name=$(head -20 "$f" | grep "^name:" | head -1)
  desc=$(head -20 "$f" | grep "^description:" | head -1)
  if [ -z "$name" ] || [ -z "$desc" ]; then
    echo "FAIL: $f — name='$name' desc='$desc'"
  fi
done
# Expected: no output (all pass)
```

**Check 4: Template co-location**
Verify 3 template files exist in `skills/questioning-flow/templates/`:
```bash
ls skills/questioning-flow/templates/*.md | wc -l
# Expected: 3
```

**Check 5: Content spot-check**
Compare file sizes of old and new to ensure no truncation:
```bash
for skill in agent-registry wave-executor review-loop; do
  old_size=$(wc -c < ".claude/skills/agency/$skill.md")
  new_size=$(wc -c < "skills/$skill/SKILL.md")
  if [ "$old_size" -ne "$new_size" ]; then
    echo "SIZE MISMATCH: $skill (old=$old_size, new=$new_size)"
  fi
done
# Expected: no output (sizes match)
```

**Step 2: Remove old skill files**

ONLY proceed if ALL checks pass:

```bash
rm -rf .claude/skills/agency/
```

This removes the 15 skill `.md` files from the old location. The `.claude/skills/` directory itself may remain if other non-Agency skills exist there.

**Step 3: Verify removal**
```bash
test ! -d .claude/skills/agency && echo "Old directory removed" || echo "ERROR: .claude/skills/agency/ still exists"
```
  </action>
  <verify>
All validation checks pass and old directory is removed:
```
Skill Migration Validation: PASS
- Directories: 15/15
- SKILL.md files: 15/15 present
- Frontmatter: 15/15 have name + description
- Templates: 3/3 co-located in questioning-flow
- Content: preserved (3/3 size checks match)
- Old location: .claude/skills/agency/ removed
```
  </verify>
  <done>All 15 skills validated schema-compliant. Templates co-located. Old .claude/skills/agency/ removed. Migration complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Exactly 15 skill directories exist under `skills/`
- [ ] Every directory contains a `SKILL.md` file
- [ ] Every `SKILL.md` has `name` and `description` frontmatter fields
- [ ] `skills/questioning-flow/templates/` contains 3 template files (project, roadmap, state)
- [ ] `skills/workflow-common/SKILL.md` references `skills/questioning-flow/templates/` (not `.planning/templates/`)
- [ ] No skill files remain in `.claude/skills/agency/`
- [ ] `skills/.gitkeep` no longer exists
- [ ] Body content of all skill files preserved exactly from originals
</verification>

<success_criteria>
- All 3 tasks completed
- All verification checks pass
- PLUG-03 satisfied: skills/ has 15 skill directories in {name}/SKILL.md format
- SKILL-01 satisfied: all 15 skills converted to directory structure
- SKILL-02 satisfied: frontmatter has name and description matching schema
- SKILL-03 satisfied: template files co-located with questioning-flow skill
- No skill files in old location
</success_criteria>

<output>
After completion, create `.planning/phases/17-skill-migration/17-01-SUMMARY.md`
</output>
