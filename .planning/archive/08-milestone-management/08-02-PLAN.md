---
phase: 08-milestone-management
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .claude/commands/agency/milestone.md
  - .claude/commands/agency/status.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - CLAUDE.md
autonomous: true
requirements: [MILE-01, MILE-02]
user_setup: []

must_haves:
  truths:
    - "/agency:milestone is a new command that handles milestone status, definition, completion, and archiving"
    - "The command auto-detects the appropriate action based on project state and presents options via AskUserQuestion"
    - "Milestone status displays all milestones with progress, phase count, and health indicators"
    - "Milestone definition proposes logical phase groupings from ROADMAP.md and gets user confirmation"
    - "Milestone completion validates all phases are Complete, generates a summary, and marks the milestone done"
    - "Milestone archiving moves phase directories to .planning/archive/ and condenses STATE.md"
    - "The command uses Haiku for status display and Sonnet for summary generation"
    - "/agency:status is updated to show milestone progress in the dashboard"
    - "MILE-01 and MILE-02 are marked complete in REQUIREMENTS.md"
    - "CLAUDE.md is updated to document the new /agency:milestone command"
    - "ROADMAP.md progress table is updated with Phase 8 plan count"
  artifacts:
    - path: ".claude/commands/agency/milestone.md"
      provides: "New milestone command with status, definition, completion, and archiving operations"
      min_lines: 180
      contains: "agency:milestone"
    - path: ".claude/commands/agency/status.md"
      provides: "Updated status command showing milestone progress in the dashboard"
      min_lines: 140
      contains: "Milestone"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements with MILE-01 and MILE-02 marked complete"
      contains: "MILE-01"
    - path: "CLAUDE.md"
      provides: "Updated CLAUDE.md with /agency:milestone documented in the commands table"
      contains: "agency:milestone"
  key_links:
    - from: ".claude/commands/agency/milestone.md"
      to: ".claude/skills/agency/milestone-tracker.md"
      via: "execution_context reference — command follows milestone-tracker for all operations"
      pattern: "milestone-tracker"
    - from: ".claude/commands/agency/milestone.md"
      to: ".claude/skills/agency/workflow-common.md"
      via: "execution_context reference for milestone paths, state update pattern, cost profiles"
      pattern: "workflow-common"
    - from: ".claude/commands/agency/milestone.md"
      to: ".claude/skills/agency/execution-tracker.md"
      via: "execution_context reference for progress calculation and commit convention"
      pattern: "execution-tracker"
    - from: ".claude/commands/agency/status.md"
      to: ".claude/skills/agency/milestone-tracker.md"
      via: "Reads milestone status from ROADMAP.md to display in dashboard"
      pattern: "Milestone"
---

<objective>
Create the `/agency:milestone` command that handles milestone status, definition, completion, and archiving. Update `/agency:status` to show milestone progress. Update documentation and state files.

Purpose: Give users a single command for all milestone lifecycle operations.
Output: New `.claude/commands/agency/milestone.md`, updated `status.md`, updated `REQUIREMENTS.md`, `CLAUDE.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-milestone-management/08-CONTEXT.md

Relevant source files:
@.claude/skills/agency/milestone-tracker.md
@.claude/skills/agency/workflow-common.md
@.claude/skills/agency/execution-tracker.md
@.claude/commands/agency/status.md

Format reference (established commands for style/structure):
@.claude/commands/agency/portfolio.md
@.claude/commands/agency/status.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /agency:milestone command</name>
  <files>.claude/commands/agency/milestone.md</files>
  <action>
Create `.claude/commands/agency/milestone.md` — the command entry point for milestone lifecycle management.

**Frontmatter**:
```yaml
---
name: agency:milestone
description: Milestone management — status, definition, completion, and archiving
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, AskUserQuestion]
---
```

Note: Read/Grep/Glob for state reading, Write/Edit for state updates, Bash for directory operations (mkdir, mv, test -d) and git commits, AskUserQuestion for user choices.

**execution_context** — Load required skills:
```
@./.claude/skills/agency/workflow-common.md
@./.claude/skills/agency/milestone-tracker.md
@./.claude/skills/agency/execution-tracker.md
```

**context** — Project state files:
```
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
```

**process** — Fully specified steps:

```
1. CHECK PROJECT EXISTS
   - Attempt to read .planning/PROJECT.md
   - If not found:
     Display:
     "No Agency project found in this directory.
      Run `/agency:start` to initialize a new project."
   - Exit — do not proceed to step 2

2. READ PROJECT STATE
   Read these files:
   a. .planning/PROJECT.md — extract project name
   b. .planning/ROADMAP.md — extract:
      - Phase list with status from Progress table
      - Milestones section (if it exists)
   c. .planning/STATE.md — extract current phase and status

3. CHECK MILESTONES DEFINED
   Look for `## Milestones` section in ROADMAP.md.

   If NOT found:
     Display:
     "# {project_name} — Milestones

      No milestones defined yet.
      Milestones group phases into major deliverables for tracking and archiving."

     Present via AskUserQuestion:
     "Would you like to define milestones for this project?"
     Options:
     - "Define milestones" — "Analyze phases and propose logical milestone groupings"
     - "Skip for now" — "Return without defining milestones"

     If "Define milestones": Go to Step 7 (DEFINE MILESTONES)
     If "Skip for now": Display "Run `/agency:milestone` anytime to set up milestones." → Exit

   If found: proceed to Step 4

4. DISPLAY MILESTONE STATUS
   For each milestone in the ## Milestones section:
   a. Extract: name, phase range (start-end), goal, status
   b. For each phase in the range:
      - Read its status from the ROADMAP.md Progress table
      - Count completed vs total plans
   c. Calculate milestone progress using milestone-tracker Section 5 formulas
   d. Determine display status

   Output:

   # {project_name} — Milestones

   | # | Milestone | Phases | Progress | Status |
   |---|-----------|--------|----------|--------|
   | 1 | {name} | {start}-{end} | [{bar}] {pct}% | {status} |
   | 2 | {name} | {start}-{end} | [{bar}] {pct}% | {status} |
   ...

   Progress bar: 10 characters wide per milestone-tracker Section 5.
   Status display:
   - Pending: "Pending"
   - In Progress: "In Progress"
   - Complete: "Complete ({date})"
   - Archived: "Archived ({date})"

   Below the table, show the current milestone (the In Progress one, or next Pending if none In Progress):

   ## Current: Milestone {N} — {name}
   **Goal**: {goal}
   **Phases {start}-{end}**:
   | Phase | Name | Plans | Status |
   |-------|------|-------|--------|
   | {N} | {name} | {completed}/{total} | {status} |
   ...

5. PRESENT OPTIONS
   Based on milestone state, determine available actions:

   **Always available**:
   - "View milestone details" — "Deep dive into a specific milestone's phases and deliverables"
   - "Done" — "Return to normal operation"

   **If any milestone has Status = "In Progress" and ALL its phases are "Complete"**:
   - "Complete milestone {N}" — "Mark '{name}' done and generate summary with metrics"

   **If any milestone has Status = "Complete" (not yet Archived)**:
   - "Archive milestone {N}" — "Move '{name}' phase files to archive to reduce clutter"

   **If milestones need redefinition**:
   - "Redefine milestones" — "Re-analyze phase groupings and update milestone boundaries"

   Present options via AskUserQuestion:
   "What would you like to do?"

6. HANDLE USER CHOICE

   **Path A: View milestone details**
   - If multiple milestones: ask which one (AskUserQuestion with milestone names)
   - Display full details for the selected milestone:
     - Goal, phase range
     - Per-phase breakdown: plan count, key deliverables from SUMMARY.md files
     - Requirement coverage: which requirements are satisfied by this milestone's phases
     - If milestone is Complete or Archived: show the summary from .planning/milestones/MILESTONE-{N}.md
   - Return to Step 5

   **Path B: Complete milestone**
   Follow milestone-tracker Section 3 (Milestone Completion):
   a. Run pre-flight checks — all phases must be Complete
   b. If checks fail: report incomplete phases, return to Step 5
   c. Generate milestone summary:
      - Gather metrics (plans, requirements, files, agents)
      - Gather qualitative data (outcomes, decisions)
      - Write .planning/milestones/MILESTONE-{N}.md
   d. Mark milestone complete in ROADMAP.md
   e. Update STATE.md with milestone completion
   f. Create git commit following execution-tracker Section 6 milestone completion format:
      ```
      git add .planning/milestones/ .planning/ROADMAP.md .planning/STATE.md
      git commit -m "chore(agency): complete milestone {N} — {name}

      Phases {start}-{end}: {count} phases, {plans} plans
      Requirements: {req_count} satisfied
      Summary: .planning/milestones/MILESTONE-{N}.md

      Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
      ```
   g. Display:
      "Milestone {N}: {name} — Complete!
       Summary written to .planning/milestones/MILESTONE-{N}.md
       Run `/agency:milestone` to archive when ready."
   - Return to Step 5

   **Path C: Archive milestone**
   Follow milestone-tracker Section 4 (Milestone Archiving):
   a. Run pre-flight checks — milestone must be Complete, summary must exist
   b. If checks fail: report what's missing, return to Step 5
   c. Confirm with user:
      "Archive Milestone {N}: {name}?
       This will move {count} phase directories from .planning/phases/ to .planning/archive/milestone-{N}/.
       Files remain accessible in the archive location."
      Options: "Archive" / "Cancel"
   d. If Cancel: return to Step 5
   e. If Archive:
      - Create .planning/archive/milestone-{N}/ directory
      - Move each phase directory
      - Update ROADMAP.md: milestone Status → Archived, phase rows get "(Archived)" note
      - Update STATE.md: condense archived phase results, update Milestones section
      - Update milestone summary: add archive date
   f. Create git commit following execution-tracker Section 6 milestone archive format:
      ```
      git add -A
      git commit -m "chore(agency): archive milestone {N} — {name}

      Phases moved to .planning/archive/milestone-{N}/
      STATE.md and ROADMAP.md updated

      Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
      ```
   g. Display:
      "Milestone {N}: {name} — Archived!
       Phase directories moved to .planning/archive/milestone-{N}/
       Summary preserved at .planning/milestones/MILESTONE-{N}.md"
   - Return to Step 5

   **Path D: Redefine milestones**
   - Go to Step 7 (DEFINE MILESTONES) — overwrites existing milestone definitions

   **Path E: Done**
   - Display: "Milestone view closed. Run `/agency:milestone` anytime for milestone management."
   - Exit

7. DEFINE MILESTONES
   Follow milestone-tracker Section 2 (Milestone Definition):
   a. Read ROADMAP.md phase list — all phases with names and goals
   b. Analyze for logical groupings:
      - Look for theme clusters (infrastructure, features, integrations, etc.)
      - Consider dependency chains
      - Aim for 2-4 milestones with 3-7 phases each
   c. Present proposed milestones:
      "Based on your {count} phases, here are proposed milestone groupings:"

      | # | Milestone | Phases | Goal |
      |---|-----------|--------|------|
      | 1 | {name} | {start}-{end} | {goal} |
      ...

   d. Ask via AskUserQuestion:
      "Accept these milestone groupings?"
      Options:
      - "Accept" — "Use these milestone definitions"
      - "Modify" — "Let me adjust the groupings"

   e. If "Modify": Ask for specific changes (which phases to regroup, new names, etc.)
   f. If "Accept" or after modifications:
      - Write ## Milestones section to ROADMAP.md per milestone-tracker Section 2
      - Derive initial status for each milestone
      - Display: "Milestones defined in ROADMAP.md. {count} milestones covering {total_phases} phases."
   g. Return to Step 4 to display the newly defined milestone status
```

IMPORTANT:
- The command always starts by checking if milestones are defined — definition is a prerequisite
- Completion requires ALL phases in the milestone to be Complete — no partial completions
- Archiving is separate from completion — users can keep completed milestones without archiving
- Archive confirmation asks the user explicitly (destructive-ish operation — moving files)
- Git commits use execution-tracker Section 6 conventions
- The action loop (Steps 5-6) keeps the user in milestone context until they choose "Done"
  </action>
  <verify>
Verify the command file was created:
```bash
wc -l .claude/commands/agency/milestone.md
```
Should be 180+ lines.

Verify frontmatter is correct:
```bash
grep "name: agency:milestone" .claude/commands/agency/milestone.md
```
Should match.

Verify it references milestone-tracker skill:
```bash
grep "milestone-tracker" .claude/commands/agency/milestone.md
```
Should match.

Verify it references workflow-common:
```bash
grep "workflow-common" .claude/commands/agency/milestone.md
```
Should match.

Verify it references execution-tracker:
```bash
grep "execution-tracker" .claude/commands/agency/milestone.md
```
Should match.

Verify all 7 steps exist:
```bash
grep -c "^[0-9]\+\.\|^   [0-9]\." .claude/commands/agency/milestone.md
```
Should return 7+ matches.

Verify key operations:
```bash
grep -c "Complete milestone\|Archive milestone\|Define milestones\|View milestone\|DISPLAY MILESTONE STATUS" .claude/commands/agency/milestone.md
```
Should return 5+ matches.

Verify git commit integration:
```bash
grep -c "git commit\|chore(agency)" .claude/commands/agency/milestone.md
```
Should return 2+ matches.

Verify it handles missing milestones:
```bash
grep "No milestones defined" .claude/commands/agency/milestone.md
```
Should match.
  </verify>
  <done>/agency:milestone command created with 7-step process: project check, state reading, milestone check (with define flow), status display, action routing, operation handling (view/complete/archive/redefine), and milestone definition.</done>
</task>

<task type="auto">
  <name>Task 2: Update /agency:status to show milestone progress</name>
  <files>.claude/commands/agency/status.md</files>
  <action>
Update `.claude/commands/agency/status.md` to include milestone information in the dashboard.

**Changes (3 additions, no modifications to existing steps):**

1. **Add milestone-tracker to execution_context**:
   Update the execution_context block to include the milestone-tracker skill:
   ```
   @./.claude/skills/agency/workflow-common.md
   @./.claude/skills/agency/execution-tracker.md
   @./.claude/skills/agency/milestone-tracker.md
   ```

2. **Add Step 2.e**: Read milestone data from ROADMAP.md
   After step 2.d, add:
   ```
   e. .planning/ROADMAP.md `## Milestones` section — if present, extract:
      - Each milestone: name, phase range, status
      - The current milestone (the one containing the current phase number)
   ```

3. **Add a "## Current Milestone" section to the dashboard in Step 4**, inserted between "## Current Phase" and "## Phase History":

   ```
   If milestones are defined in ROADMAP.md:

   ## Current Milestone
   **Milestone {N}: {name}** — {status}
   Phases {start}-{end} | {phases_complete}/{phases_total} phases complete

   If the current milestone is Complete but not Archived:
   Tip: Run `/agency:milestone` to generate a summary and optionally archive.
   ```

   If milestones are NOT defined in ROADMAP.md, omit this section entirely (do not show a placeholder).

4. **Extend the next-action routing in Step 5** with a new case, inserted between case (e) and case (f):

   ```
   e2. Current phase is "Complete" AND it's the last phase of the current milestone AND all phases in the milestone are Complete:
       Next: "Milestone {N}: {name} is complete! Run `/agency:milestone` to mark it done and generate a summary."
   ```

   This case takes priority over the generic "plan next phase" routing when a milestone boundary is reached.

Do not modify any other steps. Preserve all existing content in Steps 1-6 exactly except for the listed additions.
  </action>
  <verify>
Verify milestone-tracker is in execution_context:
```bash
grep "milestone-tracker" .claude/commands/agency/status.md
```
Should match.

Verify the Current Milestone section exists:
```bash
grep "Current Milestone" .claude/commands/agency/status.md
```
Should match.

Verify milestone routing case:
```bash
grep "agency:milestone" .claude/commands/agency/status.md
```
Should match.

Verify it handles missing milestones:
```bash
grep -i "milestones are defined\|milestones are NOT" .claude/commands/agency/status.md
```
Should match.

Verify existing steps are preserved:
```bash
grep -c "CHECK PROJECT EXISTS\|READ PROJECT STATE\|CALCULATE PROGRESS\|DISPLAY DASHBOARD\|DETERMINE NEXT ACTION\|DISPLAY NEXT ACTION" .claude/commands/agency/status.md
```
Should return 6 (all original steps present).

Verify total step count hasn't changed:
```bash
grep -cE "^[0-9]+\." .claude/commands/agency/status.md
```
Should still return 6 (no new top-level steps, only sub-steps added).
  </verify>
  <done>/agency:status updated with milestone-tracker in execution_context, "Current Milestone" dashboard section, and milestone-boundary routing in next-action logic.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md, CLAUDE.md, ROADMAP.md, and STATE.md</name>
  <files>.planning/REQUIREMENTS.md, CLAUDE.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
**Update `.planning/REQUIREMENTS.md`**:
Mark MILE-01 and MILE-02 as complete:
- Change `- [ ] MILE-01:` to `- [x] MILE-01:`
- Change `- [ ] MILE-02:` to `- [x] MILE-02:`

Update the Traceability table:
- Change `| MILE-01 through MILE-02 | TBD |` to `| MILE-01 through MILE-02 | Phase 8 |`

**Update `CLAUDE.md`** (project root):
Add `/agency:milestone` to the Available Commands table, after the `/agency:portfolio` row:
```
| `/agency:milestone` | Milestone completion, archiving, and metrics |
```

**Update `.planning/ROADMAP.md`**:
Update the progress table for Phase 8:
- Change `| 8. Milestone Management | TBD | 0 | Pending |` to `| 8. Milestone Management | 2 | 0 | Planned |`

**Update `.planning/STATE.md`**:
- Phase: 8 of 14 (planned)
- Status: Phase 8 planned — 2 plans in 2 waves
- Last Activity: Phase 8 planning (2026-03-01)
- Next Action: Run `/agency:build` to execute Phase 8: Milestone Management
  </action>
  <verify>
Verify MILE requirements are marked complete:
```bash
grep -c "\- \[x\] MILE" .planning/REQUIREMENTS.md
```
Should return 2.

Verify traceability is updated:
```bash
grep "MILE-01.*Phase 8" .planning/REQUIREMENTS.md
```
Should match.

Verify CLAUDE.md has the new command:
```bash
grep "agency:milestone" CLAUDE.md
```
Should match.

Verify ROADMAP.md progress table:
```bash
grep "Milestone Management.*2.*0.*Planned" .planning/ROADMAP.md
```
Should match.

Verify STATE.md is updated:
```bash
grep "Phase 8 planned" .planning/STATE.md
```
Should match.

Verify STATE.md next action:
```bash
grep "agency:build" .planning/STATE.md
```
Should match.
  </verify>
  <done>MILE-01 and MILE-02 marked complete in REQUIREMENTS.md. CLAUDE.md updated with /agency:milestone. ROADMAP.md shows Phase 8 as Planned with 2 plans. STATE.md updated to Phase 8 planned with next action to build.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.claude/commands/agency/milestone.md` is a complete command with 7-step process
- [ ] Command references milestone-tracker, workflow-common, and execution-tracker in execution_context
- [ ] Milestone status displays all milestones with progress bars and status
- [ ] Milestone definition proposes groupings from ROADMAP.md and gets user confirmation
- [ ] Milestone completion validates phases, generates summary, creates git commit
- [ ] Milestone archiving moves directories, condenses state, creates git commit
- [ ] Archive confirmation requires explicit user approval
- [ ] Action loop keeps user in milestone context until they choose "Done"
- [ ] `/agency:status` shows "Current Milestone" section when milestones are defined
- [ ] Status next-action routing detects milestone boundaries
- [ ] Status gracefully omits milestone section when milestones aren't defined
- [ ] MILE-01 and MILE-02 marked complete in REQUIREMENTS.md
- [ ] Traceability table updated with Phase 8
- [ ] CLAUDE.md documents /agency:milestone in the commands table
- [ ] ROADMAP.md shows Phase 8 with 2 plans, Planned status
- [ ] STATE.md shows Phase 8 planned, next action is /agency:build
- [ ] No placeholder text or scaffold remnants
</verification>

<success_criteria>
- /agency:milestone is a complete, self-routing command that handles the full milestone lifecycle
- Users can define, complete, and archive milestones through a single command
- Milestone completion generates meaningful summaries with metrics extracted from existing state files
- Archiving physically moves files to reduce clutter while maintaining accessibility
- /agency:status surfaces milestone progress naturally in the existing dashboard
- Requirements tracking and documentation are updated to reflect the new capability
- All state files are consistent and ready for /agency:build execution
</success_criteria>

<output>
After completion, create `.planning/phases/08-milestone-management/08-02-SUMMARY.md`

Then update `.planning/STATE.md`:
- Phase: 8 of 14 (planned)
- Status: Phase 8 planned — 2 plans in 2 waves
- Last Activity: Phase 8 planning (2026-03-01)
- Next Action: Run `/agency:build` to execute Phase 8: Milestone Management

And update `.planning/ROADMAP.md` progress table:
- Phase 8: Plans = 2, Completed = 0, Status = Planned
</output>
