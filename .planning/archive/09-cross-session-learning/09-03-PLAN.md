---
phase: 09-cross-session-learning
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .claude/skills/agency/phase-decomposer.md
  - .claude/commands/agency/plan.md
  - .claude/commands/agency/status.md
  - .planning/REQUIREMENTS.md
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [LEARN-03, LEARN-04, LEARN-05]
user_setup: []

must_haves:
  truths:
    - "phase-decomposer.md Section 4 has a memory-enhanced recommendation subsection before per-plan selection"
    - "phase-decomposer.md memory recall is guarded by file existence check and skips silently if unavailable"
    - "plan.md adds memory-manager to its execution_context"
    - "status.md has a Memory section in the dashboard showing recent outcomes and top agents"
    - "status.md Memory section is conditional — omitted entirely when memory is not available"
    - "status.md adds memory-manager to its execution_context"
    - "LEARN-01 through LEARN-05 are marked complete in REQUIREMENTS.md"
    - "Traceability table maps LEARN requirements to Phase 9"
    - "ROADMAP.md progress table shows Phase 9 with 3 plans, 0 completed, Planned status"
    - "STATE.md shows Phase 9 planned with next action to build"
  artifacts:
    - path: ".claude/skills/agency/phase-decomposer.md"
      provides: "Updated phase-decomposer with memory-enhanced agent recommendation"
      min_lines: 500
      contains: "memory"
    - path: ".claude/commands/agency/status.md"
      provides: "Updated status command with Memory section in dashboard"
      min_lines: 155
      contains: "Memory"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements with LEARN-01 through LEARN-05 marked complete"
      contains: "LEARN-01"
  key_links:
    - from: ".claude/skills/agency/phase-decomposer.md"
      to: ".claude/skills/agency/memory-manager.md"
      via: "Phase-decomposer calls memory-manager recall for agent recommendation"
      pattern: "memory-manager"
    - from: ".claude/commands/agency/status.md"
      to: ".claude/skills/agency/memory-manager.md"
      via: "Status calls memory-manager recall for session briefing"
      pattern: "memory-manager"
    - from: ".claude/commands/agency/plan.md"
      to: ".claude/skills/agency/memory-manager.md"
      via: "execution_context reference — plan loads memory-manager for recommendation"
      pattern: "memory-manager"
---

<objective>
Update phase-decomposer with memory-enhanced agent recommendation. Update plan.md and status.md with memory integration. Update project state files to reflect Phase 9 planning.

Purpose: Wire memory recall into the planning and status workflows, and update all documentation.
Output: Updated `phase-decomposer.md`, `plan.md`, `status.md`, `REQUIREMENTS.md`, `CLAUDE.md`, `ROADMAP.md`, `STATE.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-cross-session-learning/09-CONTEXT.md
@.planning/phases/09-cross-session-learning/09-01-PLAN.md

Relevant source files:
@.claude/skills/agency/phase-decomposer.md
@.claude/skills/agency/memory-manager.md
@.claude/commands/agency/plan.md
@.claude/commands/agency/status.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

Format reference:
@.claude/commands/agency/milestone.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update phase-decomposer and plan.md with memory-enhanced recommendation</name>
  <files>.claude/skills/agency/phase-decomposer.md, .claude/commands/agency/plan.md</files>
  <action>
**Update `.claude/skills/agency/phase-decomposer.md`**:

Add a memory-enhanced recommendation subsection to Section 4 (Agent Recommendation), inserted BEFORE the existing "### Per-Plan Selection" heading:

```markdown
### Memory-Enhanced Recommendation (Optional)

Before running the per-plan recommendation algorithm, check if cross-session memory can inform agent selection.

```
Step 0: Check memory availability
  - If .planning/memory/OUTCOMES.md exists:
    a. Extract task types from the current phase's requirements and deliverables
    b. Call memory-manager Section 4 "Recall Agent Scores" with those task types
    c. Receive agent_id → memory_score mapping
    d. Pass this mapping to the per-plan selection below — it will be consumed
       by agent-registry.md Step 4.5 (Memory Boost)
    e. Display memory context to inform the recommendation:
       "Memory: {count} past outcomes found for task types [{types}].
        Agents with track record: {agent_id} ({memory_score}), ..."
  - If .planning/memory/OUTCOMES.md does not exist:
    a. Skip memory recall entirely
    b. Proceed to Per-Plan Selection with no memory data
    c. Do NOT display any message about missing memory
```

This step enriches the recommendation but does not change the algorithm. If memory is absent, Section 4 works identically to how it worked before Phase 9.
```

Do not modify any other content in phase-decomposer.md. Preserve all 8 existing sections exactly.

**Update `.claude/commands/agency/plan.md`**:

Add memory-manager to the execution_context block. The updated block should be:
```
@./.claude/skills/agency/workflow-common.md
@./.claude/skills/agency/agent-registry.md
@./.claude/skills/agency/phase-decomposer.md
@./.claude/skills/agency/memory-manager.md
```

Do not modify any other content in plan.md. Preserve all 10 existing steps exactly.
  </action>
  <verify>
Verify phase-decomposer has memory-enhanced recommendation:
```bash
grep "Memory-Enhanced Recommendation" .claude/skills/agency/phase-decomposer.md
```
Should match.

Verify it references memory-manager:
```bash
grep "memory-manager" .claude/skills/agency/phase-decomposer.md
```
Should match.

Verify it handles missing memory:
```bash
grep "does not exist" .claude/skills/agency/phase-decomposer.md
```
Should match (in the memory context).

Verify existing sections are preserved:
```bash
grep -c "Section 1:\|Section 2:\|Section 3:\|Section 4:\|Section 5:\|Section 6:\|Section 7:\|Section 8:" .claude/skills/agency/phase-decomposer.md
```
Should return 8.

Verify plan.md has memory-manager in execution_context:
```bash
grep "memory-manager" .claude/commands/agency/plan.md
```
Should match.

Verify plan.md existing steps are preserved:
```bash
grep -c "PARSE PHASE NUMBER\|CHECK FOR EXISTING PLANS\|READ PHASE DETAILS\|DECOMPOSE INTO PLANS\|RECOMMEND AGENTS\|PRESENT TO USER\|GENERATE CONTEXT\|GENERATE PLAN\|UPDATE STATE\|DISPLAY SUMMARY" .claude/commands/agency/plan.md
```
Should return 10.
  </verify>
  <done>phase-decomposer.md updated with Memory-Enhanced Recommendation subsection (Step 0) in Section 4, guarded by file existence check. plan.md updated with memory-manager in execution_context.</done>
</task>

<task type="auto">
  <name>Task 2: Update status.md with memory section in dashboard</name>
  <files>.claude/commands/agency/status.md</files>
  <action>
Update `.claude/commands/agency/status.md` with memory integration for session briefing.

**Changes (3 additions, no modifications to existing steps):**

1. **Add memory-manager to execution_context** (it may already have milestone-tracker; add memory-manager after it):
   ```
   @./.claude/skills/agency/workflow-common.md
   @./.claude/skills/agency/execution-tracker.md
   @./.claude/skills/agency/milestone-tracker.md
   @./.claude/skills/agency/memory-manager.md
   ```

2. **Add Step 2.f**: Read memory data
   After the existing step 2.e (milestone data), add:
   ```
   f. .planning/memory/OUTCOMES.md — if present, follow memory-manager Section 4 "Recall Session Briefing":
      - recent_outcomes: last 5 outcomes with decay scores
      - top_agents: top 3 agents by task count and success rate
      - total_records: total count of all outcome records
      If .planning/memory/OUTCOMES.md does not exist: skip, set memory_available = false
   ```

3. **Add a "## Memory" section to the dashboard in Step 4**, inserted between "## Requirements Progress" and the conditional "## Known Issues" section:

   ```
   If memory_available (i.e., .planning/memory/OUTCOMES.md exists and has records):

   ## Memory
   **Outcomes**: {total_records} recorded | **Recent** (last 5):
   | Date | Agent | Outcome | Summary |
   |------|-------|---------|---------|
   | {date} | {agent} | {outcome} | {summary} |
   ...

   **Top Agents** (by experience):
   | Agent | Tasks | Success Rate |
   |-------|-------|-------------|
   | {agent_id} | {task_count} | {success_rate}% |
   ...

   If memory is not available (no OUTCOMES.md, or file exists but is empty):
   Omit this section entirely. Do NOT show a placeholder, suggestion, or "no memory" message.
   ```

Do not modify any other steps. Preserve all existing content in Steps 1-6 exactly except for the listed additions.
  </action>
  <verify>
Verify memory-manager is in execution_context:
```bash
grep "memory-manager" .claude/commands/agency/status.md
```
Should match.

Verify Memory section exists in dashboard:
```bash
grep "## Memory" .claude/commands/agency/status.md
```
Should match.

Verify it shows outcomes and top agents:
```bash
grep -c "Outcomes\|Top Agents\|Success Rate" .claude/commands/agency/status.md
```
Should return 3.

Verify it handles missing memory:
```bash
grep "Omit this section entirely" .claude/commands/agency/status.md
```
Should match.

Verify existing steps are preserved:
```bash
grep -c "CHECK PROJECT EXISTS\|READ PROJECT STATE\|CALCULATE PROGRESS\|DISPLAY DASHBOARD\|DETERMINE NEXT ACTION\|DISPLAY NEXT ACTION" .claude/commands/agency/status.md
```
Should return 6 (all original steps present).

Verify milestone integration is still present:
```bash
grep "Current Milestone" .claude/commands/agency/status.md
```
Should match.
  </verify>
  <done>status.md updated with memory-manager in execution_context, Step 2.f for memory data reading, and conditional Memory section in dashboard showing recent outcomes and top agents.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md, CLAUDE.md, ROADMAP.md, and STATE.md</name>
  <files>.planning/REQUIREMENTS.md, CLAUDE.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
**Update `.planning/REQUIREMENTS.md`**:
Mark LEARN-01 through LEARN-05 as complete:
- Change `- [ ] LEARN-01:` to `- [x] LEARN-01:`
- Change `- [ ] LEARN-02:` to `- [x] LEARN-02:`
- Change `- [ ] LEARN-03:` to `- [x] LEARN-03:`
- Change `- [ ] LEARN-04:` to `- [x] LEARN-04:`
- Change `- [ ] LEARN-05:` to `- [x] LEARN-05:`

Update the Traceability table:
- Change `| LEARN-01 through LEARN-05 | TBD |` to `| LEARN-01 through LEARN-05 | Phase 9 |`

**Update `CLAUDE.md`** (project root):
No command table change needed — Phase 9 adds no new `/agency:` commands. The memory layer is a skill consumed by existing commands.

However, add a brief note to the "## Agent Divisions (51 total)" section's surrounding context. After the "## Workflow" section's code block, add:

```markdown
## Memory Layer (Optional)

After build/review cycles, outcomes are recorded to `.planning/memory/OUTCOMES.md`. During planning, past outcomes boost agent recommendations. During status, recent outcomes enrich the session briefing. All memory features degrade gracefully — the system works identically without them.
```

**Update `.planning/ROADMAP.md`**:
Update the progress table for Phase 9:
- Change `| 9. Cross-Session Learning | TBD | 0 | Pending |` to `| 9. Cross-Session Learning | 3 | 0 | Planned |`

**Update `.planning/STATE.md`**:
- Current Position → Phase: 9 of 14 (planned)
- Status: Phase 9 planned — 3 plans in 2 waves
- Last Activity: Phase 9 planning (2026-03-01)
- Progress: recalculate — 19 complete out of 22 total plans (phases 1-8 = 19 plans) + 3 new from phase 9 = 19/25 = 76%
  Actually, phases 1-8 had 19 completed plans. With phase 9 adding 3 plans, total becomes 22 (existing) + 3 = wait, let me recalculate.

  From ROADMAP.md progress table:
  - Phases 1-8: 2+2+2+3+2+2+2+2 = 17 total plans, all completed (19 in STATE.md progress bar, but the progress table says different — use the table)

  Actually, read the current ROADMAP.md progress table and STATE.md carefully. The progress bar says "19/22 plans complete" but the table shows phases 1-8 as all complete. Calculate correctly:
  - Count total Plans column across all phases in the progress table
  - Phase 9 now adds 3 plans (0 completed)
  - Recalculate percentage

- Next Action: Run `/agency:build` to execute Phase 9: Cross-Session Learning
  </action>
  <verify>
Verify LEARN requirements are marked complete:
```bash
grep -c "\- \[x\] LEARN" .planning/REQUIREMENTS.md
```
Should return 5.

Verify traceability is updated:
```bash
grep "LEARN-01.*Phase 9" .planning/REQUIREMENTS.md
```
Should match.

Verify CLAUDE.md has the memory layer note:
```bash
grep "Memory Layer" CLAUDE.md
```
Should match.

Verify ROADMAP.md progress table:
```bash
grep "Cross-Session Learning.*3.*0.*Planned" .planning/ROADMAP.md
```
Should match.

Verify STATE.md is updated:
```bash
grep "Phase 9 planned" .planning/STATE.md
```
Should match.

Verify STATE.md next action:
```bash
grep "agency:build" .planning/STATE.md
```
Should match.
  </verify>
  <done>LEARN-01 through LEARN-05 marked complete in REQUIREMENTS.md with Phase 9 traceability. CLAUDE.md updated with Memory Layer section. ROADMAP.md shows Phase 9 as Planned with 3 plans. STATE.md updated to Phase 9 planned with next action to build.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `phase-decomposer.md` has Memory-Enhanced Recommendation subsection in Section 4
- [ ] Phase-decomposer memory recall is guarded by OUTCOMES.md existence check
- [ ] Phase-decomposer skips silently when memory is unavailable
- [ ] Phase-decomposer passes memory scores to agent-registry Step 4.5
- [ ] `plan.md` has memory-manager in execution_context
- [ ] `plan.md` existing 10 steps are unchanged
- [ ] `status.md` has memory-manager in execution_context
- [ ] `status.md` has Step 2.f for reading memory data
- [ ] `status.md` has conditional Memory section in dashboard
- [ ] `status.md` Memory section shows recent outcomes and top agents
- [ ] `status.md` omits Memory section entirely when memory unavailable
- [ ] LEARN-01 through LEARN-05 marked complete in REQUIREMENTS.md
- [ ] Traceability table maps LEARN requirements to Phase 9
- [ ] CLAUDE.md has Memory Layer section describing the feature
- [ ] ROADMAP.md shows Phase 9 with 3 plans, Planned status
- [ ] STATE.md shows Phase 9 planned, next action is /agency:build
- [ ] Progress percentage is correctly recalculated
- [ ] No placeholder text or scaffold remnants
</verification>

<success_criteria>
- Phase-decomposer's agent recommendation is enriched by memory when available
- Plan command loads memory-manager for recommendation access
- Status dashboard surfaces agent performance and recent outcomes for session context
- All memory integration degrades gracefully — zero change when memory files absent
- Requirements tracking and documentation reflect the completed planning
- All state files are consistent and ready for /agency:build execution
</success_criteria>

<output>
After completion, create `.planning/phases/09-cross-session-learning/09-03-SUMMARY.md`

Then update `.planning/STATE.md`:
- Phase: 9 of 14 (planned)
- Status: Phase 9 planned — 3 plans in 2 waves
- Last Activity: Phase 9 planning (2026-03-01)
- Next Action: Run `/agency:build` to execute Phase 9: Cross-Session Learning

And update `.planning/ROADMAP.md` progress table:
- Phase 9: Plans = 3, Completed = 0, Status = Planned
</output>
