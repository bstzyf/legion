---
phase: 09-cross-session-learning
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .claude/commands/agency/build.md
  - .claude/commands/agency/review.md
  - .claude/skills/agency/agent-registry.md
autonomous: true
requirements: [LEARN-02, LEARN-03, LEARN-05]
user_setup: []

must_haves:
  truths:
    - "build.md records an outcome in memory after each plan completes (success, partial, or failed)"
    - "build.md adds memory-manager to its execution_context"
    - "build.md memory recording follows graceful degradation — skip silently if memory unavailable"
    - "review.md records an outcome in memory after review passes or escalates"
    - "review.md adds memory-manager to its execution_context"
    - "review.md memory recording follows graceful degradation — skip silently if memory unavailable"
    - "agent-registry.md Section 3 has a new Step 4.5 that applies memory boost to agent scores"
    - "Memory boost in agent-registry supplements but cannot override mandatory roles or division alignment"
    - "All three modified files reference memory-manager.md"
  artifacts:
    - path: ".claude/commands/agency/build.md"
      provides: "Updated build command with outcome recording after plan execution"
      min_lines: 250
      contains: "memory"
    - path: ".claude/commands/agency/review.md"
      provides: "Updated review command with outcome recording after review cycle"
      min_lines: 310
      contains: "memory"
    - path: ".claude/skills/agency/agent-registry.md"
      provides: "Updated agent registry with memory boost in recommendation algorithm"
      min_lines: 280
      contains: "memory"
  key_links:
    - from: ".claude/commands/agency/build.md"
      to: ".claude/skills/agency/memory-manager.md"
      via: "execution_context reference — build follows memory-manager for outcome recording"
      pattern: "memory-manager"
    - from: ".claude/commands/agency/review.md"
      to: ".claude/skills/agency/memory-manager.md"
      via: "execution_context reference — review follows memory-manager for outcome recording"
      pattern: "memory-manager"
    - from: ".claude/skills/agency/agent-registry.md"
      to: ".claude/skills/agency/memory-manager.md"
      via: "Section 3 Step 4.5 references memory-manager recall for agent scoring"
      pattern: "memory-manager"
---

<objective>
Update build.md and review.md to record outcomes in memory after execution and review. Update agent-registry.md to apply memory boosts during agent recommendation.

Purpose: Wire the memory-manager skill (from Plan 09-01) into the execution and recommendation workflows.
Output: Updated `build.md`, `review.md`, `agent-registry.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-cross-session-learning/09-CONTEXT.md
@.planning/phases/09-cross-session-learning/09-01-PLAN.md

Relevant source files:
@.claude/commands/agency/build.md
@.claude/commands/agency/review.md
@.claude/skills/agency/agent-registry.md
@.claude/skills/agency/memory-manager.md
@.claude/skills/agency/execution-tracker.md

Format reference:
@.claude/commands/agency/milestone.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update build.md with outcome recording</name>
  <files>.claude/commands/agency/build.md</files>
  <action>
Update `.claude/commands/agency/build.md` with memory integration.

**Changes (2 additions, no modifications to existing steps):**

1. **Add memory-manager to execution_context**:
   Update the execution_context block to include the memory-manager skill:
   ```
   @./.claude/skills/agency/workflow-common.md
   @./.claude/skills/agency/agent-registry.md
   @./.claude/skills/agency/wave-executor.md
   @./.claude/skills/agency/execution-tracker.md
   @./.claude/skills/agency/memory-manager.md
   ```

2. **Add Step 4.g.2** (after the existing Step 4.g, which handles progress tracking and git commits):

   Insert after the existing step 4.g block (Track progress after each plan result), before step 4.h:

   ```
   g2. Record outcome in memory (optional — follows memory-manager Section 6)
       If .planning/memory/OUTCOMES.md exists or .planning/memory/ directory can be created:
         Follow memory-manager Section 3 (Store Outcome):
         - Agent: the agent that executed this plan (from plan's agent assignment, or "autonomous" if autonomous: true)
         - Task Type: primary task type from the plan (match plan's tasks against agent-registry task type tags)
         - Outcome: "success" if plan passed all verification, "failed" if plan failed, "partial" if completed with warnings
         - Importance: per memory-manager Section 2 importance scoring (base 2 for success, 5 for failure, 3 for partial)
         - Tags: phase slug (e.g., "cross-session-learning"), agent division (e.g., "engineering"), key file types modified
         - Summary: one-line from the agent's SendMessage completion summary (the Status field)
         NOTE: Memory write happens AFTER the plan's git commit (step 4.g). The memory file change
         will be included in the wave completion commit (step 4.h) via git add.
       If memory is not available: skip silently. Do not warn. Do not suggest setup.
   ```

Do not modify any other steps. Preserve all existing content in Steps 1-6 exactly except for the listed additions.
  </action>
  <verify>
Verify memory-manager is in execution_context:
```bash
grep "memory-manager" .claude/commands/agency/build.md
```
Should match.

Verify outcome recording step exists:
```bash
grep "Record outcome in memory" .claude/commands/agency/build.md
```
Should match.

Verify graceful degradation:
```bash
grep "skip silently" .claude/commands/agency/build.md
```
Should match.

Verify it references memory-manager Section 3:
```bash
grep "memory-manager Section 3" .claude/commands/agency/build.md
```
Should match.

Verify existing steps are preserved:
```bash
grep -c "DETERMINE TARGET PHASE\|DISCOVER PLANS\|PRE-EXECUTION CONFIRMATION\|EXECUTE WAVES\|COMPLETE PHASE EXECUTION\|ROUTE TO NEXT ACTION" .claude/commands/agency/build.md
```
Should return 6 (all original steps present).
  </verify>
  <done>build.md updated with memory-manager in execution_context and Step 4.g2 for recording outcomes after plan execution, following graceful degradation pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Update review.md with outcome recording</name>
  <files>.claude/commands/agency/review.md</files>
  <action>
Update `.claude/commands/agency/review.md` with memory integration.

**Changes (2 additions to the review command file):**

Note: review.md is the `/agency:review` command file. It references the review-loop skill but is itself a command. Read the current file to understand its structure before making changes.

1. **Add memory-manager to execution_context** (if it has an execution_context block):
   Add this line to the execution_context:
   ```
   @./.claude/skills/agency/memory-manager.md
   ```

2. **Add outcome recording after review completion**:

   The review command follows review-loop.md which has two terminal paths:
   - Section 7 (Review Passed): review approved, phase marked complete
   - Section 8 (Escalation): 3 cycles exhausted, blockers remain

   For BOTH paths, add a memory recording step. Insert this step into the command's process, after the review result is determined but before the final display/routing:

   **After review passes (Section 7 path)**:
   ```
   RECORD REVIEW OUTCOME (optional — follows memory-manager Section 6):
   If .planning/memory/OUTCOMES.md exists or .planning/memory/ directory can be created:
     Follow memory-manager Section 3 (Store Outcome):
     - Agent: comma-separated list of reviewer agent IDs (e.g., "testing-reality-checker, testing-evidence-collector")
     - Task Type: "quality-review"
     - Outcome: "success"
     - Importance: 2 if passed in cycle 1, 3 if passed in cycle 2+
     - Tags: phase slug, reviewer agent IDs, "review-passed", cycle count
     - Summary: "Phase {N} review passed in {cycles} cycle(s). {blocker_count} blockers fixed."
     NOTE: Memory write is included in the review completion git commit via git add.
   If memory is not available: skip silently.
   ```

   **After review escalates (Section 8 path)**:
   ```
   RECORD REVIEW OUTCOME (optional — follows memory-manager Section 6):
   If .planning/memory/OUTCOMES.md exists or .planning/memory/ directory can be created:
     Follow memory-manager Section 3 (Store Outcome):
     - Agent: comma-separated list of reviewer agent IDs
     - Task Type: "quality-review"
     - Outcome: "failed"
     - Importance: 5 (escalation is always high-signal)
     - Tags: phase slug, reviewer agent IDs, "review-escalated", "3-cycles", unresolved blocker files
     - Summary: "Phase {N} review escalated — {blocker_count} blocker(s) unresolved after 3 cycles."
   If memory is not available: skip silently.
   ```

Do not modify any other content. Preserve all existing review command structure exactly.
  </action>
  <verify>
Verify memory-manager is referenced:
```bash
grep "memory-manager" .claude/commands/agency/review.md
```
Should match.

Verify outcome recording for passed reviews:
```bash
grep "RECORD REVIEW OUTCOME" .claude/commands/agency/review.md
```
Should match.

Verify both paths are covered:
```bash
grep -c "review-passed\|review-escalated" .claude/commands/agency/review.md
```
Should return 2.

Verify graceful degradation:
```bash
grep -c "skip silently" .claude/commands/agency/review.md
```
Should return 2 (one for each path).

Verify existing review content is preserved:
```bash
grep -c "review-loop\|Review" .claude/commands/agency/review.md
```
Should return multiple matches (existing references intact).
  </verify>
  <done>review.md updated with memory-manager reference and outcome recording for both review-passed and review-escalated paths, following graceful degradation pattern.</done>
</task>

<task type="auto">
  <name>Task 3: Update agent-registry with memory boost step</name>
  <files>.claude/skills/agency/agent-registry.md</files>
  <action>
Update `.claude/skills/agency/agent-registry.md` Section 3 (Recommendation Algorithm) with a memory boost step.

**Change**: Insert a new Step 4.5 between existing Step 4 (Cap Team Size) and Step 5 (Mandatory Roles):

```markdown

### Step 4.5: Apply Memory Boost (Optional)

If `.planning/memory/OUTCOMES.md` exists:

1. Call memory-manager Section 4 "Recall Agent Scores" with the task types extracted in Step 1
2. Receive an `agent_id → memory_score` mapping (scores in range [0, 5])
3. For each agent in the current candidate list:
   - If the agent has a memory_score: add it to their recommendation score
   - Memory scores are additive — they boost existing scores, not replace them
4. Re-rank candidates with the updated scores
5. Display memory influence in the recommendation output:
   - For each agent with a memory boost: "(+{score} from {task_count} past outcomes)"
   - Example: "engineering-senior-developer — 8 points (+2.3 from 5 past outcomes)"

**Constraints**:
- Memory boost CANNOT override Step 5 (Mandatory Roles). A testing agent is still required for code execution teams regardless of memory scores.
- Memory boost CANNOT promote an agent from a completely unrelated division. An agent must have at least 1 point from Step 2 matching to receive a memory boost.
- If memory is unavailable (no OUTCOMES.md): skip this step entirely, proceed to Step 5. No warning, no placeholder.
- Minimum data threshold: agents with fewer than 2 recorded outcomes are excluded from memory boosting (prevents one-off noise from influencing recommendations).

If `.planning/memory/OUTCOMES.md` does not exist: skip this step entirely.
```

Do not modify Steps 1-4 or Steps 5-6. Do not modify Section 1 (Agent Catalog), Section 2 (Task Type Index), or Section 4 (Team Assembly Patterns). Preserve all existing content exactly.
  </action>
  <verify>
Verify Step 4.5 exists:
```bash
grep "Step 4.5" .claude/skills/agency/agent-registry.md
```
Should match.

Verify memory boost is documented:
```bash
grep "Memory Boost" .claude/skills/agency/agent-registry.md
```
Should match.

Verify it references memory-manager:
```bash
grep "memory-manager" .claude/skills/agency/agent-registry.md
```
Should match.

Verify constraints are documented:
```bash
grep -c "CANNOT override\|CANNOT promote\|unavailable\|Minimum data" .claude/skills/agency/agent-registry.md
```
Should return 4.

Verify existing steps are preserved:
```bash
grep -c "Step 1:\|Step 2:\|Step 3:\|Step 4:\|Step 5:\|Step 6:" .claude/skills/agency/agent-registry.md
```
Should return 6 (all original steps present).

Verify existing sections are preserved:
```bash
grep -c "Section 1:\|Section 2:\|Section 3:\|Section 4:" .claude/skills/agency/agent-registry.md
```
Should return 4 (all sections intact).
  </verify>
  <done>agent-registry.md updated with Step 4.5 Memory Boost in the recommendation algorithm — additive scoring from past outcomes, constrained by mandatory roles and division alignment, with graceful degradation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `build.md` has memory-manager in execution_context
- [ ] `build.md` has Step 4.g2 recording outcomes after each plan execution
- [ ] `build.md` outcome recording uses correct fields: agent, task type, outcome, importance, tags, summary
- [ ] `build.md` graceful degradation: skip silently if memory unavailable
- [ ] `build.md` memory write timing: after plan commit, included in wave commit
- [ ] `review.md` has memory-manager reference
- [ ] `review.md` records outcome on review-passed path (importance 2-3)
- [ ] `review.md` records outcome on review-escalated path (importance 5)
- [ ] `review.md` graceful degradation: skip silently on both paths
- [ ] `agent-registry.md` has Step 4.5 Memory Boost between Step 4 and Step 5
- [ ] Memory boost is additive to existing scores, not replacement
- [ ] Memory boost cannot override mandatory roles (Step 5)
- [ ] Memory boost requires minimum 1 point from Step 2 matching
- [ ] Memory boost requires minimum 2 recorded outcomes per agent
- [ ] Memory boost graceful degradation: skip if OUTCOMES.md missing
- [ ] All three files reference memory-manager.md
- [ ] All existing content in all three files is preserved exactly
- [ ] No scaffold remnants or placeholder text
</verification>

<success_criteria>
- Build command automatically records outcomes after each plan execution
- Review command records outcomes for both pass and escalation paths
- Agent registry recommendation algorithm includes memory as a scoring factor
- Memory boost improves agent selection based on proven track record
- All integration follows graceful degradation — zero breakage if memory absent
- Existing build/review/registry behavior is unchanged when memory is not present
</success_criteria>

<output>
After completion, create `.planning/phases/09-cross-session-learning/09-02-SUMMARY.md`
</output>
