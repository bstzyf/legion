---
phase: 01-plugin-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/agency/start.md
  - .claude/commands/agency/plan.md
  - .claude/commands/agency/build.md
  - .claude/commands/agency/review.md
  - .claude/commands/agency/status.md
  - .claude/commands/agency/quick.md
  - .claude/skills/agency/workflow-common.md
  - CLAUDE.md
autonomous: true
requirements: [INFRA-01, INFRA-02]
user_setup: []

must_haves:
  truths:
    - "Running /agency:start shows a meaningful entry point (not 'not implemented')"
    - "All 6 /agency: commands are discoverable via Claude Code skill/command listing"
    - "CLAUDE.md documents the /agency: namespace and available commands"
  artifacts:
    - path: ".claude/commands/agency/start.md"
      provides: "Entry point for project initialization"
      contains: "agency:start"
    - path: ".claude/commands/agency/plan.md"
      provides: "Entry point for phase planning"
      contains: "agency:plan"
    - path: ".claude/commands/agency/build.md"
      provides: "Entry point for parallel execution"
      contains: "agency:build"
    - path: ".claude/commands/agency/review.md"
      provides: "Entry point for quality review"
      contains: "agency:review"
    - path: ".claude/commands/agency/status.md"
      provides: "Entry point for status dashboard"
      contains: "agency:status"
    - path: ".claude/commands/agency/quick.md"
      provides: "Entry point for ad-hoc tasks"
      contains: "agency:quick"
    - path: ".claude/skills/agency/workflow-common.md"
      provides: "Shared workflow patterns and conventions"
      min_lines: 20
    - path: "CLAUDE.md"
      provides: "Project-level instructions declaring the plugin"
      contains: "/agency:"
  key_links:
    - from: ".claude/commands/agency/start.md"
      to: ".claude/skills/agency/"
      via: "execution_context references to skill files"
      pattern: "@./.claude/skills/agency/"
    - from: "CLAUDE.md"
      to: ".claude/commands/agency/"
      via: "documents available commands"
      pattern: "/agency:"
---

<objective>
Create the Claude Code plugin directory structure with all 6 /agency: command entry points and foundational skills.

Purpose: Establish the .claude/ plugin skeleton that all subsequent phases build upon. Commands are the user-facing entry points; skills provide reusable workflow logic.
Output: 6 command files, 1 foundational skill file, project CLAUDE.md
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

Relevant source files:
@agency-agents/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 /agency: command entry points</name>
  <files>.claude/commands/agency/start.md, .claude/commands/agency/plan.md, .claude/commands/agency/build.md, .claude/commands/agency/review.md, .claude/commands/agency/status.md, .claude/commands/agency/quick.md</files>
  <action>
Create `.claude/commands/agency/` directory and 6 command files. Each command uses the Claude Code command format:

```yaml
---
name: agency:{command}
description: {what it does}
argument-hint: {expected args if any}
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob]
---
```

Each command file should:
1. Have proper frontmatter with name, description, argument-hint, and allowed-tools
2. Include an `<objective>` section explaining what the command accomplishes
3. Include an `<execution_context>` section that references the skills it will use (point to `.claude/skills/agency/` files)
4. Include a `<context>` section referencing `.planning/` state files
5. Include a `<process>` section with numbered steps — these are PLACEHOLDER steps that describe what the command WILL do (implementation comes in later phases)

Specific commands:

**start.md** — `/agency:start` — Initialize a new project
- argument-hint: (none)
- Will trigger questioning flow, create PROJECT.md, ROADMAP.md, STATE.md
- Placeholder references: `@./.claude/skills/agency/questioning-flow.md` (Phase 2)

**plan.md** — `/agency:plan` — Plan a specific phase
- argument-hint: `<phase-number>`
- Will decompose phase into wave-structured plans with agent recommendations
- Placeholder references: `@./.claude/skills/agency/agent-registry.md`, `@./.claude/skills/agency/phase-decomposer.md` (Phase 3)

**build.md** — `/agency:build` — Execute current phase plans
- argument-hint: `[--phase N]`
- Will spawn agent teams in parallel waves
- Placeholder references: `@./.claude/skills/agency/wave-executor.md` (Phase 4)

**review.md** — `/agency:review` — Run quality review cycle
- argument-hint: `[--phase N]`
- Will select review agents and run dev-QA loop
- Placeholder references: `@./.claude/skills/agency/review-loop.md` (Phase 5)

**status.md** — `/agency:status` — Show progress and next action
- argument-hint: (none)
- Will read STATE.md and route to appropriate command
- Placeholder references: `@./.claude/skills/agency/workflow-common.md`

**quick.md** — `/agency:quick` — Run ad-hoc task with agent selection
- argument-hint: `<task-description>`
- Will select appropriate agent and execute single task
- Placeholder references: `@./.claude/skills/agency/agent-registry.md`

IMPORTANT: Commands should be functional scaffolds, not empty stubs. Each should clearly describe what it will do and which skills it depends on, so that later phases can implement the referenced skills and the commands will "just work."
  </action>
  <verify>
Verify all 6 files exist:
```bash
ls -la .claude/commands/agency/
```
Verify each has valid YAML frontmatter with `name: agency:*` pattern.
  </verify>
  <done>All 6 command files exist with proper frontmatter, objective, execution_context, context, and process sections. Each command clearly describes its workflow and skill dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create foundational workflow-common skill</name>
  <files>.claude/skills/agency/workflow-common.md</files>
  <action>
Create `.claude/skills/agency/` directory and the `workflow-common.md` skill file. This skill contains shared patterns used across all /agency: commands.

Format:
```yaml
---
name: agency:workflow-common
description: Shared workflow patterns and conventions for The Agency plugin
---
```

Content should include:

1. **State File Locations** — Where to find/create PROJECT.md, ROADMAP.md, STATE.md
2. **Agent Personality Path Convention** — How to locate agent .md files: `agency-agents/{division}/{filename}.md`
3. **Personality Injection Pattern** — How to load an agent personality and inject it into a spawned agent's prompt (read the .md file → pass as system instructions via Agent tool prompt)
4. **Plan File Convention** — How plans are structured: `.planning/phases/{NN-name}/{NN}-{PP}-PLAN.md`
5. **Wave Execution Pattern** — Execute wave 1 plans in parallel, wait for all to complete, then wave 2, etc.
6. **State Update Pattern** — How to update STATE.md after operations (read → modify → write)
7. **Cost Profile Convention** — Agent model selection: Opus for planning/decisions, Sonnet for execution/implementation, Haiku for lightweight checks
8. **Error Handling Pattern** — What to do when an agent fails: log error, mark task failed in STATE.md, escalate to user
9. **Division Constants** — The 9 division names: engineering, design, marketing, product, project-management, testing, support, spatial-computing, specialized

Keep it concise — this is a reference skill, not a tutorial. Use code-like precision for paths and patterns.
  </action>
  <verify>
```bash
cat .claude/skills/agency/workflow-common.md | head -5
```
Verify frontmatter is valid and file has substantive content.
  </verify>
  <done>workflow-common.md exists with all 9 sections, proper frontmatter, and serves as the shared reference for all /agency: commands.</done>
</task>

<task type="auto">
  <name>Task 3: Create project CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Create a project-level `CLAUDE.md` in the repository root that declares The Agency plugin and documents available commands.

Content:

1. **Project Identity** — "The Agency Workflows: A Claude Code plugin for orchestrating 51 AI specialist personalities"
2. **Available Commands** — List all 6 `/agency:` commands with one-line descriptions:
   - `/agency:start` — Initialize a new project with guided setup
   - `/agency:plan <N>` — Plan phase N with agent recommendations
   - `/agency:build` — Execute current phase plans with parallel agent teams
   - `/agency:review` — Run quality review cycle
   - `/agency:status` — Show progress dashboard and next action
   - `/agency:quick <task>` — Run ad-hoc task with agent selection
3. **Project Structure** — Document the `.claude/` directory layout:
   - `.claude/commands/agency/` — Command entry points
   - `.claude/skills/agency/` — Reusable workflow skills
   - `agency-agents/` — 51 agent personality files organized by division
   - `.planning/` — Project state (PROJECT.md, ROADMAP.md, STATE.md)
4. **Agent Divisions** — List the 9 divisions with agent count
5. **Workflow** — Brief description: start → plan → build → review → status
6. **Conventions**:
   - Agent personalities are the source of truth for agent behavior
   - Plans have max 3 tasks, organized into dependency waves
   - Full personality injection: agents are spawned with their .md as instructions
   - Cost profile: Opus planning, Sonnet execution, Haiku checks

Keep it under 80 lines. This is a working reference, not documentation.
  </action>
  <verify>
```bash
test -f CLAUDE.md && echo "exists" || echo "missing"
```
Verify it contains `/agency:` command references.
  </verify>
  <done>CLAUDE.md exists in project root, documents all 6 commands, project structure, and conventions. Under 80 lines.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 6 command files exist in `.claude/commands/agency/`
- [ ] Each command has valid frontmatter with `name: agency:{command}`
- [ ] `.claude/skills/agency/workflow-common.md` exists with all shared patterns
- [ ] `CLAUDE.md` exists and documents the full /agency: namespace
- [ ] No files reference non-existent dependencies (skills that don't exist yet should be noted as "Phase N" placeholders)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- `.claude/commands/agency/` contains 6 properly formatted command files
- `.claude/skills/agency/` contains the workflow-common skill
- `CLAUDE.md` serves as the project-level plugin declaration
- Commands clearly document their skill dependencies for future phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-foundation/01-01-SUMMARY.md`
</output>
