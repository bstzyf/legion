---
phase: 13-marketing-workflows
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .claude/skills/agency/phase-decomposer.md
  - .claude/commands/agency/plan.md
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - MKT-01
  - MKT-02
  - MKT-03

must_haves:
  truths:
    - "phase-decomposer.md has a marketing domain detection step that triggers marketing-specific wave patterns when MKT-* requirements or marketing keywords are present"
    - "phase-decomposer.md uses marketing-workflows skill patterns for campaign-focused decomposition (Strategy → Creation → Distribution waves)"
    - "plan.md references marketing-workflows skill in execution_context and triggers campaign questioning for marketing phases"
    - "All marketing integration uses the graceful degradation pattern — skip silently when phase is not marketing"
    - "REQUIREMENTS.md has MKT-01, MKT-02, MKT-03 checked with traceability updated to Phase 13"
  artifacts:
    - path: ".claude/skills/agency/phase-decomposer.md"
      provides: "Updated with marketing domain detection and marketing-specific wave decomposition patterns"
      contains: "marketing-workflows"
    - path: ".claude/commands/agency/plan.md"
      provides: "Updated with marketing-workflows skill reference and marketing phase awareness"
      contains: "marketing-workflows"
    - path: "CLAUDE.md"
      provides: "Updated to document marketing workflows feature"
      contains: "marketing"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MKT-01/02/03 checked, traceability updated"
      contains: "[x] MKT-01"
  key_links:
    - from: ".claude/skills/agency/phase-decomposer.md"
      to: ".claude/skills/agency/marketing-workflows.md"
      via: "Reference during marketing domain detection"
      pattern: "marketing-workflows"
    - from: ".claude/commands/agency/plan.md"
      to: ".claude/skills/agency/marketing-workflows.md"
      via: "execution_context reference"
      pattern: "marketing-workflows"
---

<objective>
Wire the marketing-workflows skill into existing Agency commands — phase-decomposer gets marketing domain detection with campaign-specific wave patterns, plan.md gets marketing phase awareness with campaign questioning. Update CLAUDE.md and REQUIREMENTS.md to reflect Phase 13 completion.

Purpose: Completes all three marketing requirements by integrating the marketing-workflows skill (from Plan 01) into the existing planning lifecycle. This is the integration layer — after this, marketing phases are automatically detected and decomposed with campaign-specific patterns.
Output: Updated phase-decomposer.md, plan.md, CLAUDE.md, REQUIREMENTS.md.
</objective>

<execution_context>
@./.claude/skills/agency/marketing-workflows.md
@./.claude/skills/agency/workflow-common.md
@./.claude/skills/agency/phase-decomposer.md
@./.claude/commands/agency/plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-marketing-workflows/13-RESEARCH.md
@.planning/phases/13-marketing-workflows/13-CONTEXT.md
@.planning/phases/13-marketing-workflows/13-01-SUMMARY.md

@.claude/skills/agency/marketing-workflows.md
@.claude/skills/agency/phase-decomposer.md
@.claude/commands/agency/plan.md

<interfaces>
<!-- Skill integration pattern: add reference + new process step -->
<!-- Follows the same pattern used when adding brownfield (Phase 12) and GitHub (Phase 11) integration -->

From .claude/skills/agency/phase-decomposer.md:
- Section 2: Phase Analysis — add marketing domain detection after reading ROADMAP phase details
- Section 3: Task Decomposition — add marketing-specific wave pattern as alternative to generic decomposition
- Section 4: Agent Recommendation — add marketing team assembly pattern reference
- Section 7: Context File Generation — add campaign document reference when marketing phase detected

From .claude/commands/agency/plan.md:
- execution_context: add @./.claude/skills/agency/marketing-workflows.md
- Step 3 (READ PHASE DETAILS): add marketing domain detection sub-step
- Step 4 (DECOMPOSE PHASE): reference marketing-specific wave patterns when marketing phase detected
- Step 5 (RECOMMEND AGENTS): reference marketing team assembly for marketing phases

From .planning/phases/13-marketing-workflows/13-RESEARCH.md:
- Pattern 3: Marketing domain detection heuristic
- Pattern 4: Marketing-specific wave structure
- Anti-patterns to avoid
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update phase-decomposer.md with marketing domain detection and wave patterns</name>
  <files>.claude/skills/agency/phase-decomposer.md</files>
  <action>
Three surgical edits to `.claude/skills/agency/phase-decomposer.md`. Use the Edit tool.

**Edit 1: Add marketing domain detection to Section 2 (Phase Analysis)**

At the end of Section 2, after the existing phase analysis steps (reading ROADMAP, REQUIREMENTS, PROJECT, STATE), add a new sub-section:

```markdown
### Marketing Domain Detection
After reading phase details, check if this is a marketing-focused phase:

1. **Requirement check**: Any MKT-* requirement IDs in the phase requirements?
2. **Keyword check**: Phase description contains marketing keywords?
   Keywords: "campaign", "content calendar", "social media", "cross-channel",
   "marketing", "brand awareness", "audience", "engagement strategy",
   "content strategy", "channel strategy"
3. **Agent signal**: Does agent-registry recommend majority marketing-division agents?

If ANY signal is positive:
  → Flag phase as marketing-focused
  → Read marketing-workflows skill for domain-specific patterns
  → Use marketing wave pattern in Section 3 decomposition
  → Trigger campaign questioning (marketing-workflows Section 2.1)
  → Generate campaign document (marketing-workflows Section 2.2)

If no signals:
  → Standard decomposition (no impact)
```

**Edit 2: Add marketing-specific wave pattern to Section 3 (Task Decomposition)**

In Section 3, after the existing wave pattern description (the step that maps dependency layers to waves), add:

```markdown
### Marketing-Specific Wave Pattern
When phase is flagged as marketing-focused (Section 2 detection):

Replace the generic wave mapping with marketing-specific waves:
- **Wave 1: Strategy & Planning**
  Tasks: Campaign strategy brief, audience analysis, success metrics definition
  Agents: marketing-social-media-strategist (lead), marketing-growth-hacker
  Output: Strategy brief in campaign document, core messaging (marketing-workflows Section 4.1)

- **Wave 2: Content Creation**
  Tasks: Core content production, channel-specific content adaptation
  Agents: marketing-content-creator + channel specialists based on campaign channels
  Input: Wave 1 strategy brief + core messaging as context
  Output: Content assets per channel, following adaptation guidelines

- **Wave 3 (optional — only if phase scope includes distribution/execution)**:
  Tasks: Content distribution, engagement, community management
  Agents: All assigned channel agents in parallel
  Input: Wave 2 content + campaign calendar
  Output: Published content, engagement metrics

Generate plan files following this wave structure instead of generic dependency-based waves.
Each plan's context section references the campaign document:
  @.planning/campaigns/{campaign-slug}.md
```

**Edit 3: Add marketing team assembly reference to Section 4 (Agent Recommendation)**

In Section 4, after the existing agent recommendation logic (where it queries agent-registry), add:

```markdown
### Marketing Team Assembly
When phase is marketing-focused, replace individual-agent-per-plan recommendation with team assembly:
- Read agent-registry Section 4 "Marketing Campaign" team pattern
- Read marketing-workflows Section 2.3 for role definitions
- Assemble team based on campaign's channel selection:
  Required: Strategy Lead (social-media-strategist) + Content Lead (content-creator)
  Per-channel: one specialist per selected channel (from marketing-workflows Section 1 Channel-Agent Mapping)
  Optional: growth-hacker (if acquisition/conversion focus), design-visual-storyteller (if visual-heavy)
- Present team to user for confirmation (not individual agent per plan)
```

All edits use the Edit tool. Do NOT renumber existing sections — these are additions within existing sections.
  </action>
  <verify>
    <automated>grep -c "marketing-workflows" .claude/skills/agency/phase-decomposer.md | awk '{print ($1 >= 2) ? "PASS: marketing-workflows referenced" : "FAIL: insufficient marketing-workflows refs"}'</automated>
    <automated>grep -c "Marketing Domain Detection" .claude/skills/agency/phase-decomposer.md | awk '{print ($1 >= 1) ? "PASS: domain detection present" : "FAIL: missing domain detection"}'</automated>
    <automated>grep -c "Marketing-Specific Wave" .claude/skills/agency/phase-decomposer.md | awk '{print ($1 >= 1) ? "PASS: marketing wave pattern present" : "FAIL: missing marketing wave pattern"}'</automated>
    <automated>grep -c "Marketing Team Assembly" .claude/skills/agency/phase-decomposer.md | awk '{print ($1 >= 1) ? "PASS: team assembly present" : "FAIL: missing team assembly"}'</automated>
  </verify>
  <done>phase-decomposer.md has Marketing Domain Detection in Section 2, Marketing-Specific Wave Pattern in Section 3, and Marketing Team Assembly in Section 4 — all marketing-focused phases automatically use campaign-specific decomposition patterns</done>
</task>

<task type="auto">
  <name>Task 2: Update plan.md with marketing-workflows skill reference and marketing phase awareness</name>
  <files>.claude/commands/agency/plan.md</files>
  <action>
Two surgical edits to `.claude/commands/agency/plan.md`. Use the Edit tool.

**Edit 1: Add marketing-workflows to execution_context**

Find the last skill reference in the execution_context block (should be codebase-mapper.md):
```
@./.claude/skills/agency/codebase-mapper.md
```

Add after it:
```
@./.claude/skills/agency/marketing-workflows.md
```

**Edit 2: Add MARKETING PHASE sub-step to step 3 (READ PHASE DETAILS)**

At the end of step 3, after the BROWNFIELD CONTEXT sub-step, add a new sub-step:

```
   MARKETING PHASE DETECTION (optional — follows marketing-workflows Section 1):
   - Run marketing domain detection on the current phase:
     a. Check if phase requirements include MKT-* IDs
     b. Check if phase description contains marketing keywords
        ("campaign", "content calendar", "social media", "cross-channel", "marketing",
         "brand awareness", "audience", "engagement strategy")
   - If marketing phase detected:
     a. Read marketing-workflows skill for domain-specific patterns
     b. In step 4 (decomposition), use marketing-specific wave pattern:
        Wave 1: Strategy & Planning (Social Media Strategist + Growth Hacker)
        Wave 2: Content Creation (Content Creator + Channel Specialists)
        Wave 3 (optional): Distribution (all channel agents)
     c. In step 5 (agent recommendation), use marketing team assembly pattern:
        Required: Strategy Lead + Content Lead
        Per-channel: one specialist per selected channel
     d. Before generating plan files, run campaign brief questioning
        (marketing-workflows Section 2.1) and generate campaign document
        at .planning/campaigns/{campaign-slug}.md
     e. All plan files reference the campaign document in their context section
   - If not marketing phase:
     Skip silently (standard decomposition applies)
```

No step renumbering needed — this is a sub-step within existing step 3.

Use the Edit tool for both changes.
  </action>
  <verify>
    <automated>grep -c "marketing-workflows" .claude/commands/agency/plan.md | awk '{print ($1 >= 2) ? "PASS: marketing-workflows referenced" : "FAIL: insufficient references"}'</automated>
    <automated>grep -c "MARKETING PHASE DETECTION" .claude/commands/agency/plan.md | awk '{print ($1 >= 1) ? "PASS: marketing detection step present" : "FAIL: missing marketing detection"}'</automated>
    <automated>grep -c "campaign" .claude/commands/agency/plan.md | awk '{print ($1 >= 2) ? "PASS: campaign references present" : "FAIL: insufficient campaign references"}'</automated>
  </verify>
  <done>plan.md references marketing-workflows.md in execution_context and has a MARKETING PHASE DETECTION sub-step in step 3 that triggers marketing-specific decomposition, team assembly, campaign questioning, and campaign document generation for marketing phases</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md and REQUIREMENTS.md</name>
  <files>CLAUDE.md, .planning/REQUIREMENTS.md</files>
  <action>
Two housekeeping files with targeted edits:

**File 1: CLAUDE.md — Document marketing workflows**

No new command to add to the Available Commands table (marketing workflows integrate into existing `/agency:plan` and `/agency:build`, not a standalone command).

Find the line about brownfield support:
```
Brownfield support is automatic — when `/agency:start` detects an existing codebase, it offers to analyze architecture, frameworks, and risks before planning. The analysis produces `.planning/CODEBASE.md`, which `/agency:plan` injects into agent task context.
```

Add after it:
```

Marketing workflows activate when `/agency:plan` detects a marketing-focused phase (MKT-* requirements or marketing keywords). Campaign planning produces structured documents at `.planning/campaigns/`, with content calendars and cross-channel coordination across the 8 marketing agents.
```

**File 2: REQUIREMENTS.md — Check MKT requirements and update traceability**

Edit 2a: Check MKT-01, MKT-02, MKT-03:
- `- [ ] MKT-01: Campaign planning workflow — structured campaign creation with marketing agents` becomes `- [x] MKT-01: Campaign planning workflow — structured campaign creation with marketing agents`
- `- [ ] MKT-02: Content calendar generation — time-based content planning with assignments` becomes `- [x] MKT-02: Content calendar generation — time-based content planning with assignments`
- `- [ ] MKT-03: Cross-channel coordination — align messaging across social, email, web` becomes `- [x] MKT-03: Cross-channel coordination — align messaging across social, email, web`

Edit 2b: Update traceability table:
- `| MKT-01 through MKT-03 | TBD |` becomes `| MKT-01 through MKT-03 | Phase 13 |`

Use the Edit tool for all changes.
  </action>
  <verify>
    <automated>grep "MKT-0[123]" .planning/REQUIREMENTS.md | grep -c "\[x\]" | awk '{print ($1 == 3) ? "PASS: all 3 MKT requirements checked" : "FAIL: "$1" of 3 checked"}'</automated>
    <automated>grep "MKT-01 through MKT-03" .planning/REQUIREMENTS.md | grep -c "Phase 13" | awk '{print ($1 >= 1) ? "PASS: traceability updated" : "FAIL: traceability not updated"}'</automated>
    <automated>grep -c "Marketing workflows" CLAUDE.md | awk '{print ($1 >= 1) ? "PASS: CLAUDE.md updated" : "FAIL: CLAUDE.md missing marketing note"}'</automated>
  </verify>
  <done>CLAUDE.md documents marketing workflows in the Workflow section, REQUIREMENTS.md has all 3 MKT requirements checked with traceability updated to Phase 13</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] phase-decomposer.md has Marketing Domain Detection in Section 2
- [ ] phase-decomposer.md has Marketing-Specific Wave Pattern in Section 3
- [ ] phase-decomposer.md has Marketing Team Assembly in Section 4
- [ ] phase-decomposer.md references marketing-workflows skill at least twice
- [ ] plan.md references marketing-workflows.md in execution_context
- [ ] plan.md has MARKETING PHASE DETECTION sub-step in step 3
- [ ] plan.md triggers campaign questioning and document generation for marketing phases
- [ ] CLAUDE.md documents marketing workflows feature
- [ ] REQUIREMENTS.md has MKT-01, MKT-02, MKT-03 checked
- [ ] REQUIREMENTS.md traceability maps MKT requirements to Phase 13
- [ ] All marketing integration uses graceful degradation — skip silently for non-marketing phases
</verification>

<success_criteria>
- phase-decomposer.md automatically detects marketing phases and applies domain-specific decomposition
- plan.md triggers campaign questioning and generates campaign documents for marketing phases
- Marketing-specific wave patterns (Strategy → Creation → Distribution) replace engineering patterns
- Team assembly ensures proper agent mix for marketing campaigns (strategy lead + content + channel specialists)
- All marketing operations use the same graceful degradation pattern as Memory, GitHub, and Brownfield integrations
- CLAUDE.md and REQUIREMENTS.md reflect Phase 13 completion
</success_criteria>

<output>
After completion, create `.planning/phases/13-marketing-workflows/13-02-SUMMARY.md`
</output>
