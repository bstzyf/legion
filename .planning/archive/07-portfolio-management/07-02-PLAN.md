---
phase: 07-portfolio-management
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .claude/commands/agency/portfolio.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - CLAUDE.md
autonomous: true
requirements: [PORT-01, PORT-02]
user_setup: []

must_haves:
  truths:
    - "/agency:portfolio is a new command that displays a multi-project dashboard from the global portfolio registry"
    - "The dashboard shows all registered projects with: name, status, current phase, progress %, health indicator, last activity"
    - "Cross-project dependencies are displayed with blocking status derived from live state checks"
    - "Agent allocation summary shows which agent personalities are used across multiple projects"
    - "Studio Producer can be invoked on-demand for strategic cross-project analysis and recommendations"
    - "The command works from any directory — it reads the global registry at ~/.claude/agency/portfolio.md"
    - "Without Studio Producer invocation, the dashboard is lightweight (read-only, Haiku-tier cost)"
    - "Studio Producer invocation uses Opus and full personality injection from the Studio Producer .md file"
    - "The command handles empty portfolio, missing projects, and stale registrations gracefully"
    - "PORT-01 and PORT-02 are marked complete in REQUIREMENTS.md"
    - "CLAUDE.md is updated to document the new /agency:portfolio command"
  artifacts:
    - path: ".claude/commands/agency/portfolio.md"
      provides: "New portfolio command with multi-project dashboard, dependency view, and Studio Producer integration"
      min_lines: 150
      contains: "agency:portfolio"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements with PORT-01 and PORT-02 marked complete"
      contains: "PORT-01"
    - path: "CLAUDE.md"
      provides: "Updated CLAUDE.md with /agency:portfolio documented in the commands table"
      contains: "agency:portfolio"
  key_links:
    - from: ".claude/commands/agency/portfolio.md"
      to: ".claude/skills/agency/portfolio-manager.md"
      via: "execution_context reference — command follows portfolio-manager for all registry operations"
      pattern: "portfolio-manager"
    - from: ".claude/commands/agency/portfolio.md"
      to: ".claude/skills/agency/workflow-common.md"
      via: "execution_context reference for portfolio paths, personality injection pattern, cost profiles"
      pattern: "workflow-common"
    - from: ".claude/commands/agency/portfolio.md"
      to: "agency-agents/project-management/project-management-studio-producer.md"
      via: "Personality injection — Studio Producer is invoked for strategic analysis when user opts in"
      pattern: "studio-producer"
---

<objective>
Create the `/agency:portfolio` command that provides a multi-project dashboard, cross-project dependency visualization, and on-demand Studio Producer strategic coordination.

Purpose: Give users cross-project visibility and strategic coordination from a single command.
Output: New `.claude/commands/agency/portfolio.md`, updated `REQUIREMENTS.md`, updated `CLAUDE.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-portfolio-management/07-CONTEXT.md

Relevant source files:
@.claude/skills/agency/portfolio-manager.md
@.claude/skills/agency/workflow-common.md
@.claude/skills/agency/agent-registry.md
@.claude/commands/agency/status.md

Format reference (established commands for style/structure):
@.claude/commands/agency/status.md
@.claude/commands/agency/start.md

Studio Producer personality:
@agency-agents/project-management/project-management-studio-producer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /agency:portfolio command</name>
  <files>.claude/commands/agency/portfolio.md</files>
  <action>
Create `.claude/commands/agency/portfolio.md` — the command entry point for multi-project portfolio management.

**Frontmatter**:
```yaml
---
name: agency:portfolio
description: Multi-project portfolio dashboard with cross-project dependency tracking
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, Agent, AskUserQuestion]
---
```

Note: Read/Grep/Glob for dashboard assembly, Write/Edit for portfolio file updates, Agent for Studio Producer invocation, AskUserQuestion for user choices, Bash for filesystem checks.

**execution_context** — Load required skills:
```
@./.claude/skills/agency/workflow-common.md
@./.claude/skills/agency/portfolio-manager.md
@./.claude/skills/agency/agent-registry.md
```

**context** — No project-specific context needed (portfolio is global):
```
Portfolio registry: ~/.claude/agency/portfolio.md
Studio Producer: agency-agents/project-management/project-management-studio-producer.md
```

**process** — Fully specified steps:

```
1. LOAD PORTFOLIO REGISTRY
   - Attempt to read ~/.claude/agency/portfolio.md
   - If not found:
     Display:
     "No portfolio registry found at ~/.claude/agency/portfolio.md
      Run `/agency:start` in a project to register it, or use Step 8 to register manually."
   - If found but empty (no ### project headings):
     Display:
     "Portfolio is empty — no projects registered.
      Run `/agency:start` in a project directory to add it to the portfolio."
   - If found with projects: proceed to Step 2

2. VALIDATE REGISTERED PROJECTS
   For each project in the registry (each ### heading):
   a. Extract the project path from "**Path**:" field
   b. Check if the directory exists (use Bash: test -d "{path}")
   c. If directory exists:
      - Read {path}/.planning/STATE.md — extract phase, status, last activity
      - Read {path}/.planning/ROADMAP.md — extract phase list, progress table
      - Calculate completion % using execution-tracker Section 5 formula
      - Assess health: Green/Yellow/Red per portfolio-manager Section 3 rules
   d. If directory doesn't exist:
      - Mark project status as Stale in registry
      - Set health to Red
      - Note: "Project directory not found at {path}"

3. DISPLAY PORTFOLIO DASHBOARD
   Output the dashboard:

   # Agency Portfolio

   **{active_count} active projects** | {total_count} registered | {at_risk_count} at risk

   ## Projects

   | Project | Phase | Progress | Health | Last Activity |
   |---------|-------|----------|--------|---------------|
   | {name} | {N}/{M}: {phase_name} | [{bar}] {pct}% | {health} | {date} — {activity} |
   ...

   Health indicators (text-based, no emoji):
   - [OK] = Green (on track)
   - [!!] = Yellow (at risk — stale activity, escalated, or failed)
   - [XX] = Red (blocking — project missing, unresolved blockers)

   Progress bar format (10 chars wide):
   - filled = floor(percentage / 10)
   - empty = 10 - filled
   - Format: [{"#" * filled}{"." * empty}]

   Sort projects: Red first (needs attention), then Yellow, then Green. Within each group, sort by last activity (most recent first).

4. DISPLAY CROSS-PROJECT DEPENDENCIES
   Read the Cross-Project Dependencies table from PORTFOLIO.md.

   If no dependencies:
     ## Dependencies
     No cross-project dependencies registered.
     Tip: Use the "Add dependency" option below to link project phases.

   If dependencies exist:
     ## Dependencies

     | # | From | To | Status | Impact |
     |---|------|----|--------|--------|
     | DEP-01 | {proj-a}: Phase {N} | {proj-b}: Phase {M} | {Resolved/Blocking} | {impact text} |

     For each dependency:
     a. Check if the "From" phase is complete (read From project's ROADMAP.md)
     b. If complete: Status = "Resolved"
     c. If not complete: Status = "Blocking"
     d. Impact text: "{To project} Phase {M} is waiting on {From project} Phase {N}"

     Blocking dependency count: {blocking_count} of {total_count}

5. DISPLAY AGENT ALLOCATION
   Follow portfolio-manager Section 5 to derive agent allocation:

   ## Agent Allocation

   If only 1 project: "Agent allocation tracking requires 2+ projects."

   If 2+ projects:
   **Shared agents** (used in 2+ projects):
   | Agent | Division | Projects |
   |-------|----------|----------|
   | {agent-id} | {division} | {project-a}, {project-b} |

   **Division coverage**:
   | Division | Projects Using | Agents Available |
   |----------|---------------|-----------------|
   | engineering | {count} | 7 |
   | design | {count} | 6 |
   ...

   If no agent overlap detected: "No agents are shared across projects."

6. DISPLAY NEXT ACTIONS
   Based on the portfolio state, suggest actions:

   ## Actions

   Present options via AskUserQuestion:
   "What would you like to do?"
   Options:
   - "View project details" — Show detailed status for a specific project
     Description: "Deep dive into one project's phase history and state"
   - "Add dependency" — Register a cross-project dependency
     Description: "Link phases across projects (e.g., Project A Phase 3 blocks Project B Phase 1)"
   - "Studio Producer analysis" — Get strategic coordination insights
     Description: "Invoke Studio Producer agent for cross-project strategy (uses Opus)"
   - "Done" — Exit portfolio view
     Description: "Return to normal operation"

7. HANDLE USER CHOICE

   **Path A: View project details**
   - If multiple projects: ask user to pick one (AskUserQuestion with project names)
   - Read the selected project's STATE.md and ROADMAP.md
   - Display the same dashboard that /agency:status would show, but prefixed with portfolio context
   - Include: current phase, plans breakdown, recent decisions, next action for that project
   - After display, return to Step 6

   **Path B: Add dependency**
   - Ask user via AskUserQuestion: "Which project is the source (blocker)?"
   - Present project names as options
   - Ask: "Which phase in {source_project} must complete first?"
   - Ask: "Which project depends on this?"
   - Ask: "Which phase in {target_project} is blocked?"
   - Ask: "Dependency type?" — Options: "blocks (hard)" or "informs (soft)"
   - Follow portfolio-manager Section 4 (Add Dependency):
     - Read PORTFOLIO.md
     - Assign next DEP-{NN} ID
     - Add row to table
     - Write PORTFOLIO.md
   - Display: "Dependency DEP-{NN} added: {source}:Phase {N} {type} {target}:Phase {M}"
   - Return to Step 6

   **Path C: Studio Producer analysis**
   - Read the Studio Producer personality file in full:
     agency-agents/project-management/project-management-studio-producer.md
   - Construct a prompt with full personality injection:
     """
     {full Studio Producer personality content}

     ---

     # Portfolio Analysis Task

     You are analyzing a portfolio of {count} Agency projects. Provide strategic coordination insights.

     ## Portfolio State

     {paste the full dashboard output from Steps 3-5 here}

     ## Instructions
     - Analyze the portfolio holistically — look for resource conflicts, dependency risks, and strategic alignment
     - Identify the highest-priority cross-project actions
     - Recommend sequencing if projects compete for similar agent specialties
     - Flag any projects that should be paused, accelerated, or deprioritized
     - Use your Strategic Portfolio Plan Template to structure recommendations
     - Keep the analysis concise (under 500 words) and actionable
     - Format as a brief executive summary, not a full portfolio plan
     """
   - Spawn via Agent tool:
     - subagent_type: "general-purpose"
     - model: "opus" (strategic decisions = Opus per cost profile)
     - name: "studio-producer-portfolio"
     - prompt: {constructed prompt}
   - Display the Studio Producer's analysis
   - Return to Step 6

   **Path D: Done**
   - Display: "Portfolio view closed. Run `/agency:portfolio` anytime for cross-project status."
   - Exit

8. MANUAL REGISTRATION (fallback for projects not created with /agency:start)
   This step is only reached if the user explicitly asks to register a project manually.

   - Confirm current directory has .planning/PROJECT.md
   - If not: "This directory doesn't have an Agency project. Run /agency:start first."
   - If yes: Follow portfolio-manager Section 2 (Register Project)
   - Display: "Registered {project_name} in portfolio."
```

IMPORTANT:
- The command works from ANY directory — it reads the global registry, not local state
- The dashboard is read-only and lightweight unless Studio Producer is invoked
- Studio Producer uses Opus because it's a strategic planning decision
- The action loop (Steps 6-7) keeps the user in portfolio context until they choose "Done"
- All operations handle missing/stale projects gracefully
- Dependencies are validated against live project state, not just registry entries
  </action>
  <verify>
Verify the command file was created:
```bash
wc -l .claude/commands/agency/portfolio.md
```
Should be 150+ lines.

Verify frontmatter is correct:
```bash
grep "name: agency:portfolio" .claude/commands/agency/portfolio.md
```
Should match.

Verify Agent is in allowed-tools (for Studio Producer):
```bash
grep "Agent" .claude/commands/agency/portfolio.md
```
Should match.

Verify it references portfolio-manager skill:
```bash
grep "portfolio-manager" .claude/commands/agency/portfolio.md
```
Should match.

Verify it references workflow-common:
```bash
grep "workflow-common" .claude/commands/agency/portfolio.md
```
Should match.

Verify it references Studio Producer:
```bash
grep "studio-producer" .claude/commands/agency/portfolio.md
```
Should match.

Verify the global registry path:
```bash
grep "~/.claude/agency/portfolio.md" .claude/commands/agency/portfolio.md
```
Should match.

Verify all 8 steps exist:
```bash
grep -c "^[0-9]\+\." .claude/commands/agency/portfolio.md
```
Should return 8.

Verify dashboard structure:
```bash
grep -c "Projects\|Dependencies\|Agent Allocation\|Actions" .claude/commands/agency/portfolio.md
```
Should return 4+ matches.

Verify Studio Producer analysis step:
```bash
grep -c "Studio Producer\|studio-producer\|opus" .claude/commands/agency/portfolio.md
```
Should return 5+ matches.

Verify it handles empty portfolio:
```bash
grep "No portfolio\|empty\|no projects" .claude/commands/agency/portfolio.md
```
Should match.
  </verify>
  <done>/agency:portfolio command created with 8-step process: load registry, validate projects, display dashboard, show dependencies, show agent allocation, present actions, handle user choice (with Studio Producer integration), and manual registration fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md and CLAUDE.md</name>
  <files>.planning/REQUIREMENTS.md, CLAUDE.md</files>
  <action>
**Update `.planning/REQUIREMENTS.md`**:
Mark PORT-01 and PORT-02 as complete:
- Change `- [ ] PORT-01:` to `- [x] PORT-01:`
- Change `- [ ] PORT-02:` to `- [x] PORT-02:`

Update the Traceability table:
- Change `| PORT-01 through PORT-02 | TBD |` to `| PORT-01 through PORT-02 | Phase 7 |`

**Update `CLAUDE.md`** (project root):
Add `/agency:portfolio` to the Available Commands table:
```
| `/agency:portfolio` | Multi-project dashboard with dependency tracking |
```

Insert it after the `/agency:quick` row in the commands table.
  </action>
  <verify>
Verify PORT requirements are marked complete:
```bash
grep -c "\- \[x\] PORT" .planning/REQUIREMENTS.md
```
Should return 2.

Verify traceability is updated:
```bash
grep "PORT-01.*Phase 7" .planning/REQUIREMENTS.md
```
Should match.

Verify CLAUDE.md has the new command:
```bash
grep "agency:portfolio" CLAUDE.md
```
Should match.
  </verify>
  <done>PORT-01 and PORT-02 marked complete in REQUIREMENTS.md. CLAUDE.md updated with /agency:portfolio in the commands table.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.claude/commands/agency/portfolio.md` is a complete command with 8-step process
- [ ] Command references portfolio-manager, workflow-common, and agent-registry in execution_context
- [ ] Dashboard shows all projects with name, phase, progress, health, last activity
- [ ] Health indicators use text-based [OK]/[!!]/[XX] format
- [ ] Cross-project dependencies are validated against live project state
- [ ] Agent allocation is derived at read-time from plan files across projects
- [ ] Studio Producer analysis is opt-in (user chooses it from the actions menu)
- [ ] Studio Producer uses Opus model and full personality injection
- [ ] Action loop keeps user in portfolio context until they choose "Done"
- [ ] Manual registration fallback exists for non-/agency:start projects
- [ ] Empty portfolio and stale projects are handled gracefully
- [ ] PORT-01 and PORT-02 marked complete in REQUIREMENTS.md
- [ ] CLAUDE.md documents /agency:portfolio in the commands table
- [ ] No placeholder text or scaffold remnants
</verification>

<success_criteria>
- /agency:portfolio is a complete, executable command that works from any directory
- The dashboard provides immediate cross-project visibility with health assessment
- Cross-project dependencies show blocking status derived from live state
- Studio Producer provides strategic analysis on demand (not by default)
- The command follows established patterns (personality injection, cost profiles, error handling)
- Requirements tracking and documentation are updated to reflect the new capability
</success_criteria>

<output>
After completion, create `.planning/phases/07-portfolio-management/07-02-SUMMARY.md`

Then update `.planning/STATE.md`:
- Phase: 7 of 14 (planned)
- Status: Phase 7 planned — 2 plans in 2 waves
- Last Activity: Phase 7 planning (2026-03-01)
- Next Action: Run `/agency:build` to execute Phase 7: Portfolio Management

And update `.planning/ROADMAP.md` progress table:
- Phase 7: Plans = 2, Completed = 0, Status = Planned
</output>
