---
phase: 14-design-workflows
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - .claude/skills/agency/phase-decomposer.md
  - .claude/commands/agency/plan.md
  - .claude/commands/agency/review.md
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DSN-01
  - DSN-02
  - DSN-03

must_haves:
  truths:
    - "phase-decomposer.md has a design domain detection step that triggers design-specific wave patterns when DSN-* requirements or design keywords are present"
    - "phase-decomposer.md uses design-workflows skill patterns for design-focused decomposition (Research → Creation → Polish waves)"
    - "plan.md references design-workflows skill in execution_context and triggers design questioning for design phases"
    - "review.md references design-workflows skill and triggers three-lens design review for design phases"
    - "All design integration uses the graceful degradation pattern — skip silently when phase is not design"
    - "REQUIREMENTS.md has DSN-01, DSN-02, DSN-03 checked with traceability updated to Phase 14"
  artifacts:
    - path: ".claude/skills/agency/phase-decomposer.md"
      provides: "Updated with design domain detection and design-specific wave decomposition patterns"
      contains: "design-workflows"
    - path: ".claude/commands/agency/plan.md"
      provides: "Updated with design-workflows skill reference and design phase awareness"
      contains: "design-workflows"
    - path: ".claude/commands/agency/review.md"
      provides: "Updated with three-lens design review reference"
      contains: "design-workflows"
    - path: "CLAUDE.md"
      provides: "Updated to document design workflows feature"
      contains: "design"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DSN-01/02/03 checked, traceability updated"
      contains: "[x] DSN-01"
  key_links:
    - from: ".claude/skills/agency/phase-decomposer.md"
      to: ".claude/skills/agency/design-workflows.md"
      via: "Reference during design domain detection"
      pattern: "design-workflows"
    - from: ".claude/commands/agency/plan.md"
      to: ".claude/skills/agency/design-workflows.md"
      via: "execution_context reference"
      pattern: "design-workflows"
    - from: ".claude/commands/agency/review.md"
      to: ".claude/skills/agency/design-workflows.md"
      via: "Three-lens design review reference"
      pattern: "design-workflows"
---

<objective>
Wire the design-workflows skill into existing Agency commands — phase-decomposer gets design domain detection with design-specific wave patterns, plan.md gets design phase awareness with design questioning, review.md gets three-lens design review. Update CLAUDE.md and REQUIREMENTS.md to reflect Phase 14 completion.

Purpose: Completes all three design requirements by integrating the design-workflows skill (from Plan 01) into the existing planning and review lifecycle. This is the integration layer — after this, design phases are automatically detected, decomposed with design-specific patterns, and reviewed with brand/accessibility/usability lenses.
Output: Updated phase-decomposer.md, plan.md, review.md, CLAUDE.md, REQUIREMENTS.md.
</objective>

<execution_context>
@./.claude/skills/agency/design-workflows.md
@./.claude/skills/agency/workflow-common.md
@./.claude/skills/agency/phase-decomposer.md
@./.claude/skills/agency/review-loop.md
@./.claude/commands/agency/plan.md
@./.claude/commands/agency/review.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-workflows/14-RESEARCH.md
@.planning/phases/14-design-workflows/14-CONTEXT.md
@.planning/phases/14-design-workflows/14-01-SUMMARY.md

@.claude/skills/agency/design-workflows.md
@.claude/skills/agency/phase-decomposer.md
@.claude/skills/agency/review-loop.md
@.claude/commands/agency/plan.md
@.claude/commands/agency/review.md

<interfaces>
<!-- Skill integration pattern: add reference + new process step -->
<!-- Follows the same pattern used when adding marketing (Phase 13), brownfield (Phase 12), and GitHub (Phase 11) integration -->

From .claude/skills/agency/phase-decomposer.md:
- Section 2: Phase Analysis — add design domain detection after the existing marketing domain detection
- Section 3: Task Decomposition — add design-specific wave pattern as alternative to generic decomposition
- Section 4: Agent Recommendation — add design team assembly pattern reference

From .claude/commands/agency/plan.md:
- execution_context: add @./.claude/skills/agency/design-workflows.md
- Step 3 (READ PHASE DETAILS): add design domain detection sub-step after MARKETING PHASE DETECTION

From .claude/commands/agency/review.md:
- execution_context: add @./.claude/skills/agency/design-workflows.md
- Review agent selection: add three-lens design review enhancement

From .planning/phases/14-design-workflows/14-RESEARCH.md:
- Design domain detection signals
- Design-specific wave structure
- Three-lens review specification
- Anti-patterns to avoid
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update phase-decomposer.md with design domain detection, wave patterns, and team assembly</name>
  <files>.claude/skills/agency/phase-decomposer.md</files>
  <action>
Three surgical edits to `.claude/skills/agency/phase-decomposer.md`. Use the Edit tool.

**Edit 1: Add design domain detection to Section 2 (Phase Analysis)**

At the end of Section 2, after the existing Marketing Domain Detection sub-section (which ends with "Standard decomposition (no impact)"), add a new sub-section:

```markdown

### Design Domain Detection

After reading phase details (and after marketing detection), check if this is a design-focused phase:

1. **Requirement check**: Any DSN-* requirement IDs in the phase requirements?
2. **Keyword check**: Phase description contains design keywords?
   Keywords: "design system", "component library", "UX research", "usability testing",
   "accessibility audit", "brand guidelines", "design tokens", "wireframes", "prototypes",
   "user testing", "design review", "user persona", "user journey", "information architecture",
   "visual design"
3. **Agent signal**: Does agent-registry recommend majority design-division agents?

If ANY signal is positive:
  → Flag phase as design-focused
  → Read design-workflows skill for domain-specific patterns
  → Use design wave pattern in Section 3 decomposition
  → Trigger design brief questioning (design-workflows Section 2.1)
  → Generate design documents (design-workflows Section 2.2)

If no signals:
  → Standard decomposition (no impact)
```

**Edit 2: Add design-specific wave pattern to Section 3 (Task Decomposition)**

In Section 3, after the existing Marketing-Specific Wave Pattern sub-section (which ends with the line about campaign document context reference), add:

```markdown

### Design-Specific Wave Pattern

When phase is flagged as design-focused (Section 2 detection):

Replace the generic wave mapping with design-specific waves:
- **Wave 1: Research & Foundation**
  Tasks: User research, brand audit, design principles definition, token foundation
  Agents: design-ux-researcher (research lead), design-brand-guardian (brand audit)
  Output: Research brief, user insights, brand foundation, design principles in design document

- **Wave 2: Design System & Creation**
  Tasks: Component library, token system, layout framework, visual assets
  Agents: design-ui-designer (design lead) + design-ux-architect + design-visual-storyteller
  Input: Wave 1 research + brand guidelines as context
  Output: Design system document, component specifications, visual language

- **Wave 3 (optional — only if phase scope includes polish/validation)**:
  Tasks: Micro-interactions, delight elements, accessibility audit, usability validation
  Agents: design-whimsy-injector + review agents
  Input: Wave 2 design system + components
  Output: Enhanced specifications with delight, audit reports

Generate plan files following this wave structure instead of generic dependency-based waves.
Each plan's context section references the design documents:
  @.planning/designs/{project-slug}-system.md
```

**Edit 3: Add design team assembly reference to Section 4 (Agent Recommendation)**

In Section 4, after the existing Marketing Team Assembly sub-section (which ends with "Present team to user for confirmation"), add:

```markdown

### Design Team Assembly

When phase is design-focused, replace individual-agent-per-plan recommendation with team assembly:
- Read agent-registry Section 4 "Design Sprint" team pattern
- Read design-workflows Section 2.3 for role definitions
- Assemble team based on project's design scope:
  Required: Design Lead (design-ui-designer) + Research Lead (design-ux-researcher)
  Per-discipline: one specialist per relevant discipline (from design-workflows Section 1 Discipline-Agent Mapping)
  Optional: design-brand-guardian (if established brand), design-ux-architect (if CSS/layout needed), engineering-frontend-developer (if design-to-code handoff)
- Present team to user for confirmation (not individual agent per plan)
```

All edits use the Edit tool. Do NOT renumber existing sections — these are additions within existing sections.
  </action>
  <verify>
    <automated>grep -c "design-workflows" .claude/skills/agency/phase-decomposer.md | awk '{print ($1 >= 2) ? "PASS: design-workflows referenced" : "FAIL: insufficient design-workflows refs"}'</automated>
    <automated>grep -c "Design Domain Detection" .claude/skills/agency/phase-decomposer.md | awk '{print ($1 >= 1) ? "PASS: domain detection present" : "FAIL: missing domain detection"}'</automated>
    <automated>grep -c "Design-Specific Wave" .claude/skills/agency/phase-decomposer.md | awk '{print ($1 >= 1) ? "PASS: design wave pattern present" : "FAIL: missing design wave pattern"}'</automated>
    <automated>grep -c "Design Team Assembly" .claude/skills/agency/phase-decomposer.md | awk '{print ($1 >= 1) ? "PASS: team assembly present" : "FAIL: missing team assembly"}'</automated>
  </verify>
  <done>phase-decomposer.md has Design Domain Detection in Section 2, Design-Specific Wave Pattern in Section 3, and Design Team Assembly in Section 4 — all design-focused phases automatically use design-specific decomposition patterns</done>
</task>

<task type="auto">
  <name>Task 2: Update plan.md and review.md with design-workflows skill references</name>
  <files>.claude/commands/agency/plan.md, .claude/commands/agency/review.md</files>
  <action>
Four surgical edits across two files. Use the Edit tool.

**File 1: plan.md — Two edits**

**Edit 1a: Add design-workflows to execution_context**

Find the last skill reference in the execution_context block (should be marketing-workflows.md):
```
@./.claude/skills/agency/marketing-workflows.md
```

Add after it:
```
@./.claude/skills/agency/design-workflows.md
```

**Edit 1b: Add DESIGN PHASE DETECTION sub-step to step 3 (READ PHASE DETAILS)**

At the end of step 3, after the MARKETING PHASE DETECTION sub-step (which ends with "Skip silently (standard decomposition applies)"), add:

```
   DESIGN PHASE DETECTION (optional — follows design-workflows Section 1):
   - Run design domain detection on the current phase:
     a. Check if phase requirements include DSN-* IDs
     b. Check if phase description contains design keywords
        ("design system", "component library", "UX research", "usability testing",
         "accessibility audit", "brand guidelines", "design tokens", "wireframes",
         "user persona", "user journey", "information architecture", "visual design")
   - If design phase detected:
     a. Read design-workflows skill for domain-specific patterns
     b. In step 4 (decomposition), use design-specific wave pattern:
        Wave 1: Research & Foundation (UX Researcher + Brand Guardian)
        Wave 2: Design System & Creation (UI Designer + UX Architect + Visual Storyteller)
        Wave 3 (optional): Polish & Validation (Whimsy Injector + review agents)
     c. In step 5 (agent recommendation), use design team assembly pattern:
        Required: Design Lead (design-ui-designer) + Research Lead (design-ux-researcher)
        Per-discipline: specialist per relevant discipline
     d. Before generating plan files, run design brief questioning
        (design-workflows Section 2.1) and generate design documents
        at .planning/designs/{project-slug}-system.md
     e. All plan files reference the design documents in their context section
   - If not design phase:
     Skip silently (standard decomposition applies)
```

**File 2: review.md — Two edits**

**Edit 2a: Add design-workflows to execution_context**

Find the execution_context block in review.md. After the last skill reference, add:
```
@./.claude/skills/agency/design-workflows.md
```

**Edit 2b: Add three-lens design review enhancement**

Find the review agent selection section in review.md (the step where review agents are selected based on phase type). After the existing review agent selection logic, add a note about design phase enhancement:

```
   DESIGN REVIEW ENHANCEMENT (optional — follows design-workflows Section 4):
   - If phase type includes "design" AND design documents exist at .planning/designs/:
     a. Use three-lens design review instead of default single-reviewer mapping
     b. Select three design reviewers (within the max 3 reviewer limit):
        - design-brand-guardian (brand lens — visual identity compliance, voice consistency)
        - design-ux-architect (accessibility lens — WCAG compliance, keyboard nav, contrast)
        - design-ux-researcher (usability lens — Nielsen's heuristics, IA, user flows)
     c. Each reviewer uses design-specific checklists from design-workflows Section 4.3
     d. Findings use design-specific categories (Brand Violation, Accessibility Failure, Usability Critical, etc.)
   - If not a design phase or no design documents:
     Use default review agent selection (no impact)
```

Use the Edit tool for all four changes.
  </action>
  <verify>
    <automated>grep -c "design-workflows" .claude/commands/agency/plan.md | awk '{print ($1 >= 2) ? "PASS: design-workflows referenced in plan.md" : "FAIL: insufficient references"}'</automated>
    <automated>grep -c "DESIGN PHASE DETECTION" .claude/commands/agency/plan.md | awk '{print ($1 >= 1) ? "PASS: design detection step present" : "FAIL: missing design detection"}'</automated>
    <automated>grep -c "design-workflows" .claude/commands/agency/review.md | awk '{print ($1 >= 1) ? "PASS: design-workflows referenced in review.md" : "FAIL: missing reference"}'</automated>
    <automated>grep -c "DESIGN REVIEW ENHANCEMENT" .claude/commands/agency/review.md || grep -c "three-lens" .claude/commands/agency/review.md | awk '{print ($1 >= 1) ? "PASS: three-lens review present" : "FAIL: missing three-lens review"}'</automated>
  </verify>
  <done>plan.md references design-workflows.md in execution_context and has a DESIGN PHASE DETECTION sub-step in step 3; review.md references design-workflows.md and has a DESIGN REVIEW ENHANCEMENT sub-step for three-lens design review</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md and REQUIREMENTS.md</name>
  <files>CLAUDE.md, .planning/REQUIREMENTS.md</files>
  <action>
Two housekeeping files with targeted edits:

**File 1: CLAUDE.md — Document design workflows**

No new command to add to the Available Commands table (design workflows integrate into existing `/agency:plan`, `/agency:build`, and `/agency:review`, not a standalone command).

Find the line about marketing workflows:
```
Marketing workflows activate when `/agency:plan` detects a marketing-focused phase (MKT-* requirements or marketing keywords). Campaign planning produces structured documents at `.planning/campaigns/`, with content calendars and cross-channel coordination across the 8 marketing agents.
```

Add after it:
```

Design workflows activate when `/agency:plan` detects a design-focused phase (DSN-* requirements or design keywords). Design system creation produces structured documents at `.planning/designs/`, with component specifications and three-lens review (brand, accessibility, usability) across the 6 design agents.
```

**File 2: REQUIREMENTS.md — Check DSN requirements and update traceability**

Edit 2a: Check DSN-01, DSN-02, DSN-03:
- `- [ ] DSN-01: Design system creation — structured design system workflow with design agents` becomes `- [x] DSN-01: Design system creation — structured design system workflow with design agents`
- `- [ ] DSN-02: UX research workflow — user research planning and synthesis` becomes `- [x] DSN-02: UX research workflow — user research planning and synthesis`
- `- [ ] DSN-03: Design review cycle — design-specific quality gates (brand, accessibility, usability)` becomes `- [x] DSN-03: Design review cycle — design-specific quality gates (brand, accessibility, usability)`

Edit 2b: Update traceability table:
- `| DSN-01 through DSN-03 | TBD |` becomes `| DSN-01 through DSN-03 | Phase 14 |`

Use the Edit tool for all changes.
  </action>
  <verify>
    <automated>grep "DSN-0[123]" .planning/REQUIREMENTS.md | grep -c "\[x\]" | awk '{print ($1 == 3) ? "PASS: all 3 DSN requirements checked" : "FAIL: "$1" of 3 checked"}'</automated>
    <automated>grep "DSN-01 through DSN-03" .planning/REQUIREMENTS.md | grep -c "Phase 14" | awk '{print ($1 >= 1) ? "PASS: traceability updated" : "FAIL: traceability not updated"}'</automated>
    <automated>grep -c "Design workflows" CLAUDE.md | awk '{print ($1 >= 1) ? "PASS: CLAUDE.md updated" : "FAIL: CLAUDE.md missing design note"}'</automated>
  </verify>
  <done>CLAUDE.md documents design workflows in the Workflow section, REQUIREMENTS.md has all 3 DSN requirements checked with traceability updated to Phase 14</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] phase-decomposer.md has Design Domain Detection in Section 2
- [ ] phase-decomposer.md has Design-Specific Wave Pattern in Section 3
- [ ] phase-decomposer.md has Design Team Assembly in Section 4
- [ ] phase-decomposer.md references design-workflows skill at least twice
- [ ] plan.md references design-workflows.md in execution_context
- [ ] plan.md has DESIGN PHASE DETECTION sub-step in step 3
- [ ] plan.md triggers design questioning and document generation for design phases
- [ ] review.md references design-workflows.md in execution_context
- [ ] review.md has DESIGN REVIEW ENHANCEMENT for three-lens design review
- [ ] CLAUDE.md documents design workflows feature
- [ ] REQUIREMENTS.md has DSN-01, DSN-02, DSN-03 checked
- [ ] REQUIREMENTS.md traceability maps DSN requirements to Phase 14
- [ ] All design integration uses graceful degradation — skip silently for non-design phases
</verification>

<success_criteria>
- phase-decomposer.md automatically detects design phases and applies domain-specific decomposition
- plan.md triggers design questioning and generates design documents for design phases
- review.md triggers three-lens review (brand + accessibility + usability) for design phases
- Design-specific wave patterns (Research → Creation → Polish) replace engineering patterns
- Team assembly ensures proper agent mix for design projects (design lead + research lead + discipline specialists)
- All design operations use the same graceful degradation pattern as Marketing, Memory, GitHub, and Brownfield integrations
- CLAUDE.md and REQUIREMENTS.md reflect Phase 14 completion
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-workflows/14-02-SUMMARY.md`
</output>
