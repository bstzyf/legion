---
phase: 11-github-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - .claude/commands/agency/plan.md
  - .claude/commands/agency/build.md
  - .claude/commands/agency/status.md
  - .claude/commands/agency/review.md
  - .claude/commands/agency/milestone.md
  - .claude/skills/agency/execution-tracker.md
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - GH-01
  - GH-02
  - GH-03

must_haves:
  truths:
    - "plan.md creates a GitHub issue for the phase after generating plan files"
    - "build.md creates a PR after phase execution completes successfully"
    - "status.md shows a GitHub section in the dashboard when GitHub metadata exists"
    - "review.md closes the linked GitHub issue after review passes"
    - "milestone.md closes the linked GitHub milestone after milestone completion"
    - "All GitHub operations use the graceful degradation pattern — skip silently when unavailable"
  artifacts:
    - path: ".claude/commands/agency/plan.md"
      provides: "Updated with GitHub issue creation step after plan generation"
      contains: "github-sync"
    - path: ".claude/commands/agency/build.md"
      provides: "Updated with PR creation step after phase execution"
      contains: "github-sync"
    - path: ".claude/commands/agency/status.md"
      provides: "Updated with GitHub section in dashboard display"
      contains: "GitHub"
    - path: ".claude/commands/agency/review.md"
      provides: "Updated with issue close after review passes"
      contains: "github-sync"
    - path: ".claude/commands/agency/milestone.md"
      provides: "Updated with GitHub milestone close after milestone completion"
      contains: "github-sync"
  key_links:
    - from: ".claude/commands/agency/plan.md"
      to: ".claude/skills/agency/github-sync.md"
      via: "execution_context reference"
      pattern: "github-sync"
    - from: ".claude/commands/agency/build.md"
      to: ".claude/skills/agency/github-sync.md"
      via: "execution_context reference"
      pattern: "github-sync"
    - from: ".claude/commands/agency/status.md"
      to: ".claude/skills/agency/github-sync.md"
      via: "execution_context reference"
      pattern: "github-sync"
    - from: ".claude/commands/agency/review.md"
      to: ".claude/skills/agency/github-sync.md"
      via: "execution_context reference"
      pattern: "github-sync"
    - from: ".claude/commands/agency/milestone.md"
      to: ".claude/skills/agency/github-sync.md"
      via: "execution_context reference"
      pattern: "github-sync"
---

<objective>
Wire the github-sync skill into existing Agency commands — plan, build, status, review, and milestone — so GitHub operations happen automatically at the right workflow points. Update execution-tracker with PR tracking conventions.

Purpose: Completes all three requirements by integrating the github-sync skill (from Plan 01) into the existing command lifecycle. This is the integration layer.
Output: Updated plan.md, build.md, status.md, review.md, milestone.md, execution-tracker.md, CLAUDE.md, REQUIREMENTS.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-github-integration/11-RESEARCH.md
@.planning/phases/11-github-integration/11-CONTEXT.md
@.planning/phases/11-github-integration/11-01-SUMMARY.md

@.claude/skills/agency/github-sync.md
@.claude/skills/agency/workflow-common.md
@.claude/skills/agency/execution-tracker.md
@.claude/commands/agency/plan.md
@.claude/commands/agency/build.md
@.claude/commands/agency/status.md
@.claude/commands/agency/review.md
@.claude/commands/agency/milestone.md

<interfaces>
<!-- Command update pattern: add execution_context reference + new process step -->
<!-- Follows the same pattern used when adding memory integration in Phase 9 -->

From .claude/commands/agency/plan.md:
- execution_context: add @./.claude/skills/agency/github-sync.md
- Step 8 generates plan files, Step 9 updates STATE.md, Step 10 displays summary
- New step 9: GitHub issue creation (between plan generation and state update)
- Renumber existing 9→10, 10→11. After renumbering, search for internal step references.

From .claude/commands/agency/build.md:
- execution_context: add @./.claude/skills/agency/github-sync.md
- Step 4.g tracks progress, Step 4.g2 records memory, Step 5 completes phase
- New step 4.g3: update issue checklist after each plan
- New step 5.e2: PR creation (after phase completion display, before routing)

From .claude/commands/agency/status.md:
- execution_context: add @./.claude/skills/agency/github-sync.md
- Step 2.f reads memory, Step 4 displays dashboard
- New step 2.g: read GitHub metadata from STATE.md
- New dashboard section after Memory section

From .claude/commands/agency/review.md:
- execution_context: add @./.claude/skills/agency/github-sync.md
- After review passes and phase is marked complete
- New step: close linked GitHub issue

From .claude/commands/agency/milestone.md:
- execution_context: add @./.claude/skills/agency/github-sync.md
- Path B (Complete milestone) step f creates git commit
- New step after f: close GitHub milestone (github-sync Section 4.2)

From .claude/skills/agency/execution-tracker.md:
- Section 6: Commit Message Convention
- Add PR creation commit convention
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update plan.md with GitHub issue creation</name>
  <files>.claude/commands/agency/plan.md</files>
  <action>
Two surgical edits to `.claude/commands/agency/plan.md`:

**Edit 1: Add github-sync to execution_context**

Find:
```
@./.claude/skills/agency/memory-manager.md
```

Add after it:
```
@./.claude/skills/agency/github-sync.md
```

**Edit 2: Add GitHub issue creation step between steps 8 and 9**

After step 8 (GENERATE PLAN FILES) and before step 9 (UPDATE STATE), insert a new step. Renumber existing step 9 to 10 and step 10 to 11:

```
9. GITHUB ISSUE CREATION (optional)
   Follow github-sync Section 8 (Graceful Degradation) caller pattern:
   - Check GitHub availability: gh auth status && git remote get-url origin
   - If github_available is false: skip to step 10

   If github_available is true:
   a. Ensure "agency" label exists (github-sync Section 2.1)
   b. If ROADMAP.md has milestones and the current phase falls within a milestone range:
      - Check if the GitHub milestone exists (github-sync Section 4.1)
      - If not: create it
   c. Create a GitHub issue for the phase (github-sync Section 2.2):
      - Title: "Phase {N}: {phase_name}"
      - Body: phase goal, plans checklist, requirements, success criteria
      - Label: "agency"
      - Milestone: GitHub milestone title (if available)
   d. Store the issue number in STATE.md ## GitHub section (github-sync Section 6)
   e. Confirm to user: "Created GitHub issue #{number} for Phase {N}: {phase_name}"
```

Then renumber the old step 9 (UPDATE STATE) to step 10, and old step 10 (DISPLAY SUMMARY) to step 11.

**Important:** After renumbering, search the file for any references to "step 9" or "step 10" in other steps' text and update them to match the new numbering.

Use the Edit tool for both changes.
  </action>
  <verify>
    <automated>grep -c "github-sync" .claude/commands/agency/plan.md</automated>
    <automated>grep -c "GITHUB ISSUE" .claude/commands/agency/plan.md</automated>
  </verify>
  <done>plan.md references github-sync.md in execution_context and has a GitHub issue creation step (step 9) between plan generation and state update, with steps correctly renumbered to 11 total</done>
</task>

<task type="auto">
  <name>Task 2: Update build.md with PR creation and issue checklist updates</name>
  <files>.claude/commands/agency/build.md</files>
  <action>
Three surgical edits to `.claude/commands/agency/build.md`:

**Edit 1: Add github-sync to execution_context**

Find:
```
@./.claude/skills/agency/memory-manager.md
```

Add after it:
```
@./.claude/skills/agency/github-sync.md
```

**Edit 2: Add issue checklist update after step 4.g2 (memory recording)**

After step 4.g2 (Record outcome in memory), add:

```
   g3. Update GitHub issue checklist (optional — follows github-sync Section 8)
       If github_available (checked once at start of step 4):
         - Read STATE.md ## GitHub section for the current phase's issue number
         - If an issue number exists:
           Update the issue body checklist (github-sync Section 2.4):
           Change "- [ ] Plan {NN}-{PP}" to "- [x] Plan {NN}-{PP}" in the issue body
         - If no issue number: skip
       If github_available is false: skip silently.
```

**Edit 3: Add PR creation step after step 5.e (final progress display)**

After step 5.e (Display final progress) and before step 6 (ROUTE TO NEXT ACTION), add:

```
   e2. CREATE GITHUB PR (optional — follows github-sync Section 8)
       Only if ALL plans passed (no PR for partial/failed phases):
       - Check GitHub availability: gh auth status && git remote get-url origin
       - If github_available is false: skip to step 6

       If github_available is true:
       a. Use AskUserQuestion: "Phase {N} complete. Create a GitHub PR?"
          Options:
          - "Create PR" — proceed with PR creation
          - "Skip PR" — skip, proceed to step 6
       b. If user chose "Create PR":
          - Check current branch (github-sync Section 3.1)
          - If on default branch: create feature branch agency/phase-{NN}-{slug}
          - Push branch to remote: git push -u origin {branch}
          - Create PR (github-sync Section 3.2):
            Title: "Phase {N}: {phase_name}"
            Body: phase goal, plan summaries table, files modified, requirements, "Closes #{issue_number}" if issue exists
          - Store PR number in STATE.md ## GitHub section (github-sync Section 6)
          - Confirm to user: "Created PR #{number}: {pr_url}"
       c. If PR creation fails: report error, do not abort — the phase is still valid
```

Use the Edit tool for all changes.
  </action>
  <verify>
    <automated>grep -c "github-sync" .claude/commands/agency/build.md</automated>
    <automated>grep -q "GITHUB PR" .claude/commands/agency/build.md && grep -q "g3" .claude/commands/agency/build.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>build.md references github-sync.md in execution_context, has issue checklist update step (4.g3), and PR creation step (5.e2) with user confirmation</done>
</task>

<task type="auto">
  <name>Task 3: Update status.md, review.md, and milestone.md with GitHub integration</name>
  <files>.claude/commands/agency/status.md, .claude/commands/agency/review.md, .claude/commands/agency/milestone.md</files>
  <action>
Three command files with targeted edits:

**File 1: status.md — Add GitHub section to dashboard**

Edit 1a: Add github-sync to execution_context:
Find:
```
@./.claude/skills/agency/memory-manager.md
```
Add after it:
```
@./.claude/skills/agency/github-sync.md
```

Edit 1b: Add GitHub state reading to step 2 (READ PROJECT STATE):
After step 2.f (memory reading), add:
```
   g. STATE.md `## GitHub` section — if present, extract:
      - Phase-to-issue mapping table
      - Phase-to-PR mapping table
      - Milestone mapping table (if present)
      If ## GitHub section does not exist: skip, set github_metadata_available = false
```

Edit 1c: Add GitHub section to step 4 (DISPLAY DASHBOARD):
After the Memory section display (the "If memory is not available" paragraph), add:
```
   If github_metadata_available:

   ## GitHub
   Follow github-sync Section 5 (Status Readback):
   - Fetch live issue/PR/milestone status via gh CLI
   - Display:

   | Phase | Issue | PR | Status |
   |-------|-------|----|--------|
   | Phase 1: {name} | #{n} ({state}) | #{n} ({state}) | {summary} |
   ...

   If milestones are linked:
   | Milestone | GitHub | Open/Closed Issues |
   |-----------|--------|--------------------|
   | {name} | #{n} ({state}) | {open}/{total} |

   If gh CLI is unavailable during readback: show STATE.md values with note "(cached — GitHub unreachable)"

   If github_metadata_available is false:
   Omit this section entirely. Do NOT show a placeholder.
```

**File 2: review.md — Add issue close after review passes**

Edit 2a: Add github-sync to execution_context. Find the last @-reference in the execution_context block and add:
```
@./.claude/skills/agency/github-sync.md
```

Edit 2b: After the review loop completes with a PASS verdict and the phase is marked complete, add a step:
```
GITHUB ISSUE CLOSE (optional — follows github-sync Section 8):
- Check GitHub availability
- If github_available and STATE.md ## GitHub section has an issue number for this phase:
  Close the issue: gh issue close {number} --comment "Phase {N}: {phase_name} review passed. All plans verified."
  Update STATE.md GitHub table if needed
- If github_available is false: skip silently
```

Note: The exact insertion point depends on the review.md structure. Find the step where the phase is marked complete and add the GitHub close step immediately after.

**File 3: milestone.md — Add GitHub milestone close after completion**

Edit 3a: Add github-sync to execution_context:
Find:
```
@./.claude/skills/agency/execution-tracker.md
```
Add after it:
```
@./.claude/skills/agency/github-sync.md
```

Edit 3b: In Path B (Complete milestone), after step f (git commit) and before step g (display completion), add:

```
   f2. CLOSE GITHUB MILESTONE (optional — follows github-sync Section 8)
       - Check GitHub availability
       - If github_available and STATE.md ## GitHub section has a milestone number for this milestone:
         Close the GitHub milestone: gh api "repos/{REPO_SLUG}/milestones/{number}" --method PATCH -f state=closed
         (github-sync Section 4.2)
       - If github_available is false: skip silently
```

Use the Edit tool for all changes.
  </action>
  <verify>
    <automated>grep -c "github-sync" .claude/commands/agency/status.md</automated>
    <automated>grep -c "github-sync" .claude/commands/agency/review.md</automated>
    <automated>grep -c "github-sync" .claude/commands/agency/milestone.md</automated>
  </verify>
  <done>status.md shows GitHub section in dashboard with live readback, review.md closes GitHub issues after review passes, milestone.md closes GitHub milestones after milestone completion — all with graceful degradation</done>
</task>

<task type="auto">
  <name>Task 4: Update execution-tracker.md, CLAUDE.md, and REQUIREMENTS.md</name>
  <files>.claude/skills/agency/execution-tracker.md, CLAUDE.md, .planning/REQUIREMENTS.md</files>
  <action>
Three housekeeping files with targeted edits:

**File 1: execution-tracker.md — Add PR commit convention**

In Section 6 (Commit Message Convention), after the existing commit types (plan completion, wave completion, phase completion, milestone completion, milestone archive), add:

```
PR creation state update (one per phase PR):
  chore(agency): create PR for phase {N} — {phase_name}

  PR #{pr_number}: {pr_url}
  Branch: {branch_name}
  Issues: #{issue_number}

  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

**File 2: CLAUDE.md — Add GitHub integration note**

No command table update needed (no new command). Find the "## Workflow" section and after the existing workflow description (`Each phase: plan (decompose + assign agents) → build (parallel execution) → review (QA loop)`), add:

```
GitHub integration is opt-in — when a GitHub remote exists, `/agency:plan` creates issues, `/agency:build` creates PRs, and `/agency:status` shows GitHub status.
```

**File 3: REQUIREMENTS.md — Check GH requirements and update traceability**

Edit 3a: Check GH-01, GH-02, GH-03:
- `- [ ] GH-01:` becomes `- [x] GH-01:`
- `- [ ] GH-02:` becomes `- [x] GH-02:`
- `- [ ] GH-03:` becomes `- [x] GH-03:`

Edit 3b: Update traceability table:
- `| GH-01 through GH-03 | TBD |` becomes `| GH-01 through GH-03 | Phase 11 |`

Use the Edit tool for all changes.
  </action>
  <verify>
    <automated>grep "GH-0[123]" .planning/REQUIREMENTS.md | grep -c "\[x\]"</automated>
    <automated>grep -c "PR creation state update" .claude/skills/agency/execution-tracker.md</automated>
    <automated>grep -c "GitHub integration is opt-in" CLAUDE.md</automated>
  </verify>
  <done>execution-tracker.md has PR creation commit convention, CLAUDE.md notes GitHub integration, REQUIREMENTS.md has all 3 GH requirements checked with traceability updated to Phase 11</done>
</task>

</tasks>

<verification>
- plan.md references github-sync.md in execution_context
- plan.md has a GitHub issue creation step (step 9) with correct renumbering
- build.md references github-sync.md in execution_context
- build.md has issue checklist update step (4.g3) and PR creation step (5.e2)
- status.md references github-sync.md in execution_context
- status.md has GitHub section in dashboard display
- review.md references github-sync.md in execution_context
- review.md closes GitHub issues after review passes
- milestone.md references github-sync.md in execution_context
- milestone.md closes GitHub milestones after milestone completion
- execution-tracker.md has PR creation commit convention
- CLAUDE.md notes GitHub integration
- REQUIREMENTS.md has GH-01, GH-02, GH-03 checked
- REQUIREMENTS.md traceability maps GH requirements to Phase 11
</verification>

<success_criteria>
- All six existing commands/skills receive targeted GitHub integration updates
- All GitHub operations use the graceful degradation pattern from github-sync Section 8
- Issue creation is automatic (in plan.md), PR creation is confirmable (in build.md)
- Status dashboard shows live GitHub data when available, omits section when not
- Milestone completion closes GitHub milestones (completing the GH-03 lifecycle)
- CLAUDE.md and REQUIREMENTS.md reflect Phase 11 completion
</success_criteria>

<output>
After completion, create `.planning/phases/11-github-integration/11-02-SUMMARY.md`
</output>
