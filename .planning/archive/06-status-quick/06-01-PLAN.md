---
phase: 06-status-quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/agency/status.md
autonomous: true
requirements: [STATUS-01, STATUS-03, STATUS-04]
user_setup: []

must_haves:
  truths:
    - "/agency:status reads STATE.md and ROADMAP.md to display a complete progress dashboard"
    - "The dashboard shows: project name, overall progress bar, current phase name and status, plans completed/total, recent activity log"
    - "Session resume is fulfilled by displaying enough context that a returning user understands where they left off"
    - "Next-action routing follows a deterministic decision tree based on project state"
    - "The routing covers all states: no project, phase unplanned, phase planned, phase executed, phase reviewed, all complete"
    - "The command is read-only — it never modifies STATE.md, ROADMAP.md, or any other file"
    - "The command uses only Read, Grep, Glob tools — no write operations"
    - "Progress bar calculation follows execution-tracker Section 5 formula"
  artifacts:
    - path: ".claude/commands/agency/status.md"
      provides: "Fully functional progress dashboard with session resume and next-action routing"
      min_lines: 80
      contains: "agency:status"
  key_links:
    - from: ".claude/commands/agency/status.md"
      to: ".claude/skills/agency/workflow-common.md"
      via: "execution_context reference for state paths, progress conventions"
      pattern: "workflow-common"
    - from: ".claude/commands/agency/status.md"
      to: ".planning/STATE.md"
      via: "context reference for current phase, progress, recent activity"
      pattern: "STATE.md"
    - from: ".claude/commands/agency/status.md"
      to: ".planning/ROADMAP.md"
      via: "context reference for phase details and completion status"
      pattern: "ROADMAP.md"
---

<objective>
Update `/agency:status` from its Phase 1 scaffold into a fully functional command that displays a clear progress dashboard, provides session resume context, and routes users to the appropriate next `/agency:` command.

Purpose: Give users a single command to understand where they are and what to do next.
Output: Updated `.claude/commands/agency/status.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-status-quick/06-CONTEXT.md

Relevant source files:
@.claude/skills/agency/workflow-common.md
@.claude/skills/agency/execution-tracker.md
@.claude/commands/agency/status.md

Format reference (established commands for style/structure):
@.claude/commands/agency/build.md
@.claude/commands/agency/review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update /agency:status command to full implementation</name>
  <files>.claude/commands/agency/status.md</files>
  <action>
Replace the scaffold content in `.claude/commands/agency/status.md` with a fully functional command. Keep the existing frontmatter fields but ensure they match the read-only nature of this command.

The updated command should have:

**Frontmatter** (preserve from scaffold — already correct):
```yaml
---
name: agency:status
description: Show project progress dashboard and route to next action
allowed-tools: [Read, Grep, Glob]
---
```

Note: This is intentionally read-only. No Write, Edit, Bash, or Agent tools needed. Status only reads and displays.

**execution_context** — Load workflow-common for conventions:
```
@./.claude/skills/agency/workflow-common.md
```

**context** — Reference state files:
```
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
```

**process** — Fully specified steps (not placeholders):

```
1. CHECK PROJECT EXISTS
   - Attempt to read .planning/PROJECT.md
   - If not found:
     Display:
     "No Agency project found in this directory.
      Run `/agency:start` to initialize a new project."
   - Exit — do not proceed to step 2

2. READ PROJECT STATE
   Read these files (all required for full dashboard):
   a. .planning/PROJECT.md — extract project name from the "# {name}" heading
   b. .planning/STATE.md — extract:
      - Current phase number and total phases (from "Phase: N of M")
      - Phase status (from "Status:" field)
      - Last activity (from "Last Activity:" field)
      - Next action (from "Next Action:" field)
      - Recent decisions (from "Recent Decisions" section, last 3)
      - Pre-existing issues (from "Pre-existing Issues" section, if any)
      - Phase results for the current phase (from "Phase {N} Results" section)
   c. .planning/ROADMAP.md — extract:
      - Phase list with names, goals, and completion status
      - Progress table: phase name, plans count, completed count, status per phase
   d. .planning/REQUIREMENTS.md — if exists, count checked vs unchecked requirements

3. CALCULATE PROGRESS
   Follow execution-tracker Section 5 (Progress Calculation):
   a. Read the ROADMAP.md progress table
   b. Sum all "Plans" values = total_plans
   c. Sum all "Completed" values = completed_plans
   d. Percentage = floor((completed_plans / total_plans) * 100)
   e. Progress bar: 20 characters wide
      - filled = floor(percentage / 5)
      - empty = 20 - filled
      - Format: [{"#" * filled}{"." * empty}] {pct}% — {completed}/{total} plans complete

4. DISPLAY DASHBOARD
   Output the dashboard in this exact format:

   # {project_name} — Status

   {progress_bar}

   ## Current Phase
   **Phase {N}: {phase_name}** — {status}
   Plans: {completed_in_phase}/{total_in_phase}
   Goal: {phase_goal from ROADMAP.md}

   ## Phase History
   | Phase | Status | Plans |
   |-------|--------|-------|
   | 1. {name} | {status_emoji} {status} | {completed}/{total} |
   | 2. {name} | {status_emoji} {status} | {completed}/{total} |
   ...

   Status emojis (use only these):
   - Complete: [x]
   - Executed (pending review): [~]
   - Planned (not yet executed): [-]
   - Pending (not yet planned): [ ]

   ## Recent Activity
   - {Last Activity from STATE.md}
   - {Phase results for current phase, last 3 entries}

   ## Requirements Progress
   {checked_count}/{total_count} requirements complete
   {List unchecked requirements from current phase, if any}

   If there are pre-existing issues in STATE.md:
   ## Known Issues
   {list pre-existing issues}

5. DETERMINE NEXT ACTION
   Apply this decision tree in order (first match wins):

   a. No ROADMAP.md exists:
      Next: "Run `/agency:start` to initialize the project."

   b. Current phase status contains "pending" or "Pending" in ROADMAP.md:
      Next: "Run `/agency:plan {N}` to plan Phase {N}: {phase_name}."

   c. Current phase status contains "Planned" in ROADMAP.md:
      Next: "Run `/agency:build` to execute Phase {N}: {phase_name}."

   d. Current phase status contains "Executed" or STATE.md says "pending review":
      Next: "Run `/agency:review` to verify Phase {N}: {phase_name}."

   e. Current phase is "Complete" AND there are more phases:
      - Find the next incomplete phase number (N+1)
      Next: "Run `/agency:plan {N+1}` to plan Phase {N+1}: {next_phase_name}."

   f. All phases are "Complete":
      Next: "All phases complete! Project is finished.
             Run `/agency:quick <task>` for any ad-hoc work."

   g. STATE.md says "escalated" or "failed":
      Next: "Phase {N} needs attention. Review the issues in STATE.md.
             Options:
             - Fix issues manually, then `/agency:review`
             - `/agency:quick <task>` to address specific issues
             - `/agency:plan {N}` to re-plan the phase"

6. DISPLAY NEXT ACTION
   Output:

   ## Next Action
   {next_action_text from step 5}

   Tip: Run `/agency:quick <task>` anytime for ad-hoc tasks outside the phase workflow.
```

IMPORTANT:
- The process steps must be detailed enough that Claude can execute them without ambiguity
- The command is pure read-only — absolutely no file modifications
- Progress bar follows the exact formula from execution-tracker Section 5
- The decision tree is deterministic — same state always produces same routing
- Dashboard format is human-readable and includes all context for session resume
- Handle edge cases: missing files, empty state, partially executed phases
  </action>
  <verify>
Verify the command file is updated:
```bash
wc -l .claude/commands/agency/status.md
```
Should be 80+ lines.

Verify it's read-only (no write tools):
```bash
grep "allowed-tools" .claude/commands/agency/status.md
```
Should contain Read, Grep, Glob and NOT contain Write, Edit, Bash, or Agent.

Verify it references workflow-common:
```bash
grep "workflow-common" .claude/commands/agency/status.md
```
Should match.

Verify it has the decision tree:
```bash
grep -c "agency:plan\|agency:build\|agency:review\|agency:start\|agency:quick" .claude/commands/agency/status.md
```
Should return 5+ matches (routes to all commands).

Verify progress bar logic:
```bash
grep -c "progress\|Progress\|percentage\|total_plans" .claude/commands/agency/status.md
```
Should return 3+ matches.

Verify session resume context:
```bash
grep -c "Recent Activity\|Phase History\|Current Phase\|Last Activity" .claude/commands/agency/status.md
```
Should return 3+ matches.

Verify it handles missing project:
```bash
grep "agency:start" .claude/commands/agency/status.md
```
Should match (routes to start when no project exists).

Verify it's no longer a scaffold:
```bash
grep -c "placeholder\|scaffold\|TODO\|Phase 1 will" .claude/commands/agency/status.md
```
Should return 0.
  </verify>
  <done>/agency:status command is fully functional with 6-step process: check project exists, read state, calculate progress, display dashboard, determine next action, display routing. Read-only, deterministic, handles all project states.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.claude/commands/agency/status.md` is no longer a scaffold — has full 6-step process
- [ ] Command is read-only: allowed-tools contains only Read, Grep, Glob
- [ ] execution_context references workflow-common.md
- [ ] context references PROJECT.md, ROADMAP.md, STATE.md
- [ ] Dashboard shows: project name, progress bar, current phase, phase history table
- [ ] Progress bar follows execution-tracker Section 5 formula exactly
- [ ] Decision tree covers all states: no project, pending, planned, executed, reviewed, complete, failed/escalated
- [ ] Routes to all 5 other /agency: commands based on state
- [ ] Session resume: dashboard includes enough context to orient a returning user
- [ ] Handles missing files gracefully (no PROJECT.md → suggest /agency:start)
- [ ] No write operations, no Agent spawning, no state modifications
- [ ] No placeholder text or scaffold remnants
</verification>

<success_criteria>
- /agency:status is a complete, executable command (not a scaffold)
- The 6-step process covers reading state, calculating progress, rendering dashboard, and routing
- The dashboard provides full session resume context: where the project is, what happened recently, what to do next
- Next-action routing is deterministic and covers all possible project states
- The command is safe to run anytime — purely read-only with no side effects
- Progress bar matches the exact formula from execution-tracker Section 5
</success_criteria>

<output>
After completion, create `.planning/phases/06-status-quick/06-01-SUMMARY.md`
</output>
