---
phase: 06-status-quick
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/agency/quick.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [STATUS-02]
user_setup: []

must_haves:
  truths:
    - "/agency:quick parses a task description from $ARGUMENTS and uses agent-registry to recommend the best agent"
    - "Agent selection follows agent-registry Section 3 (Recommendation Algorithm) for a single-task scope"
    - "The user confirms or overrides the recommended agent via AskUserQuestion before spawning"
    - "The selected agent is spawned with full personality injection following workflow-common's pattern"
    - "The agent executes the task autonomously and results are displayed to the user"
    - "An optional git commit is offered after the task completes if files were created or modified"
    - "Quick tasks do NOT update phase state (STATE.md, ROADMAP.md) — they are outside the phase workflow"
    - "The command uses Sonnet for the spawned agent per the cost profile convention"
  artifacts:
    - path: ".claude/commands/agency/quick.md"
      provides: "Fully functional ad-hoc task execution command with agent selection and personality injection"
      min_lines: 100
      contains: "agency:quick"
  key_links:
    - from: ".claude/commands/agency/quick.md"
      to: ".claude/skills/agency/workflow-common.md"
      via: "execution_context reference for personality injection pattern, cost profiles, error handling"
      pattern: "workflow-common"
    - from: ".claude/commands/agency/quick.md"
      to: ".claude/skills/agency/agent-registry.md"
      via: "execution_context reference for agent recommendation algorithm"
      pattern: "agent-registry"
    - from: ".claude/commands/agency/quick.md"
      to: ".planning/PROJECT.md"
      via: "context reference for project context that helps inform agent selection"
      pattern: "PROJECT.md"
---

<objective>
Update `/agency:quick` from its Phase 1 scaffold into a fully functional command that executes a single ad-hoc task with intelligent agent selection, personality injection, and result reporting.

Purpose: Give users a lightweight way to run any task with the right agent — no phase planning required.
Output: Updated `.claude/commands/agency/quick.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-status-quick/06-CONTEXT.md

Relevant source files:
@.claude/skills/agency/workflow-common.md
@.claude/skills/agency/agent-registry.md
@.claude/skills/agency/wave-executor.md
@.claude/commands/agency/quick.md

Format reference (established commands for style/structure):
@.claude/commands/agency/build.md
@.claude/commands/agency/review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update /agency:quick command to full implementation</name>
  <files>.claude/commands/agency/quick.md</files>
  <action>
Replace the scaffold content in `.claude/commands/agency/quick.md` with a fully functional command. Keep the existing frontmatter fields but update allowed-tools to include Agent and AskUserQuestion.

The updated command should have:

**Frontmatter** (update from scaffold):
```yaml
---
name: agency:quick
description: Run a single ad-hoc task with intelligent agent selection
argument-hint: <task-description>
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, Agent, AskUserQuestion]
---
```

Note: The `allowed-tools` list adds Agent (for spawning the task agent) and AskUserQuestion (for agent confirmation). No TeamCreate needed — quick tasks use a single agent, not a team.

**execution_context** — Load required skills:
```
@./.claude/skills/agency/workflow-common.md
@./.claude/skills/agency/agent-registry.md
```

**context** — Reference project context (if available):
```
@.planning/PROJECT.md
@.planning/STATE.md
```

**process** — Fully specified steps (not placeholders):

```
1. PARSE TASK DESCRIPTION
   - Read $ARGUMENTS for the task description
   - If $ARGUMENTS is empty or missing:
     Display: "Usage: `/agency:quick <task-description>`
              Example: `/agency:quick write unit tests for the auth module`
              Example: `/agency:quick create a content calendar for Q2`
              Example: `/agency:quick review the API rate limiting code`"
     Exit — do not proceed
   - Store the full task description for use in subsequent steps
   - Display: "Quick task: {task_description}"

2. LOAD PROJECT CONTEXT (optional)
   - Attempt to read .planning/PROJECT.md
   - If found: extract project name, tech stack, constraints
     - This context helps inform agent selection and task execution
   - If not found: proceed without project context
     - Quick tasks work with or without an initialized project
   - Attempt to read .planning/STATE.md
   - If found: note current phase for awareness (but quick tasks don't modify phase state)

3. SELECT AGENT
   Follow agent-registry Section 3 (Recommendation Algorithm) at single-task scope:

   a. Parse Task Description (Section 3, Step 1):
      - Extract key terms from the task description
      - Match terms against task_types tags in the Agent Catalog

   b. Match Agents (Section 3, Step 2):
      - Score agents using the weighting system:
        - Exact match on task type tag: 3 points
        - Partial match (substring in specialty): 1 point
        - Division alignment: 2 points

   c. Rank and Select (Section 3, Steps 3-4):
      - Rank by score, break ties by specificity
      - For quick tasks: select top 1-2 candidates (not full team assembly)
      - Cap at 1 agent for execution (quick = single agent)

   d. Present recommendation to user via AskUserQuestion:
      "Which agent should handle this task?"
      Options:
      - "{top_agent_id} — {specialty}" (Recommended)
        Description: "{brief rationale based on task match}"
      - "{second_agent_id} — {specialty}"
        Description: "{brief rationale for alternative}"
      - "No agent — run autonomously"
        Description: "Execute without personality injection (faster, generic)"

   e. If user selects "Other": accept a custom agent ID from user input
      - Validate the ID exists in agent-registry Section 1
      - If invalid: display available agent IDs for the closest division and re-prompt

4. CONSTRUCT TASK PROMPT
   Based on selection from Step 3:

   **Path A: Personality-injected agent**
   a. Look up the agent's file path from agent-registry Section 1
   b. Read the agent's full personality .md file
   c. Construct the execution prompt:
      """
      {full personality .md content}

      ---

      # Task

      {task_description from Step 1}

      ## Project Context
      {project name, tech stack, constraints from Step 2 — or "No project context available" if PROJECT.md not found}

      ## Instructions
      - Execute this task to completion
      - Use your specialist expertise to produce the best possible result
      - Create or modify files as needed
      - If the task is ambiguous, make reasonable decisions and note your assumptions
      - When done, provide a summary of:
        - What you did
        - Files created or modified (with paths)
        - Any decisions or assumptions you made
        - Any follow-up actions the user should consider
      """

   **Path B: Autonomous (no personality)**
   a. Construct a simpler prompt:
      """
      # Task

      {task_description from Step 1}

      ## Project Context
      {project context or "No project context available"}

      ## Instructions
      - Execute this task to completion
      - Create or modify files as needed
      - When done, provide a summary of what you did and any files changed
      """

5. SPAWN AGENT AND EXECUTE
   - Spawn via Agent tool:
     - subagent_type: "general-purpose"
     - prompt: {constructed prompt from Step 4}
     - model: "sonnet" (per cost profile: execution = Sonnet)
     - name: "{agent-id}-quick" or "quick-task" if autonomous
   - Wait for the agent to complete
   - Capture the agent's return value (summary of work done)

6. DISPLAY RESULTS
   Output the results to the user:

   ## Quick Task Complete

   **Agent**: {agent_id} ({specialty}) — or "Autonomous" if no personality
   **Task**: {task_description}

   ### Summary
   {agent's summary of what was done}

   ### Files Changed
   {list of files created or modified, from agent's report}

   If the agent reported errors or could not complete:
   ### Issues
   {description of what went wrong}
   Suggestion: Try a different agent or break the task into smaller pieces.

7. OFFER COMMIT (if files changed)
   - Check if the agent created or modified any files:
     Run `git status --short` to detect changes
   - If changes exist, use AskUserQuestion:
     "Commit the changes from this quick task?"
     Options:
     - "Yes — commit with conventional message" (Recommended)
       Description: "Creates a feat/fix/chore commit for the work done"
     - "No — leave uncommitted"
       Description: "Keep changes in working directory for further review"
   - If user chooses to commit:
     - Determine commit type from task description:
       - Task mentions "fix", "bug", "repair" -> fix(agency)
       - Task mentions "test", "spec" -> test(agency)
       - Task mentions "doc", "readme", "comment" -> docs(agency)
       - Task mentions "refactor", "clean", "reorganize" -> refactor(agency)
       - Default -> feat(agency)
     - Create the commit:
       {type}(agency): quick — {brief task summary}

       Task: {task_description}
       Agent: {agent_id}

       Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
   - If no changes detected:
     Display: "No file changes detected — nothing to commit."

   Note: Quick tasks do NOT update STATE.md or ROADMAP.md.
   They operate outside the phase workflow entirely.
```

IMPORTANT:
- The process steps must be detailed enough that Claude can execute them without ambiguity
- Quick tasks are intentionally lightweight — no Teams, no waves, no state tracking
- Agent selection reuses the exact algorithm from agent-registry Section 3
- Personality injection follows the exact pattern from workflow-common
- The "no agent" option provides a fast path for simple tasks
- Quick tasks work with or without an initialized project
- The commit step is optional and uses AskUserQuestion, never auto-commits
  </action>
  <verify>
Verify the command file is updated:
```bash
wc -l .claude/commands/agency/quick.md
```
Should be 100+ lines.

Verify Agent is in allowed-tools:
```bash
grep "Agent" .claude/commands/agency/quick.md
```
Should match.

Verify AskUserQuestion is in allowed-tools:
```bash
grep "AskUserQuestion" .claude/commands/agency/quick.md
```
Should match.

Verify it references agent-registry:
```bash
grep "agent-registry" .claude/commands/agency/quick.md
```
Should match.

Verify it references workflow-common:
```bash
grep "workflow-common" .claude/commands/agency/quick.md
```
Should match.

Verify personality injection pattern:
```bash
grep -c "personality\|\.md content\|\.md file\|personality injection" .claude/commands/agency/quick.md
```
Should return 3+ matches.

Verify it handles empty arguments:
```bash
grep "ARGUMENTS" .claude/commands/agency/quick.md
```
Should match.

Verify commit is optional:
```bash
grep -c "commit\|Commit" .claude/commands/agency/quick.md
```
Should return 3+ matches.

Verify it doesn't update phase state:
```bash
grep -c "do NOT update\|outside the phase\|don't modify" .claude/commands/agency/quick.md
```
Should return 1+ matches.

Verify it's no longer a scaffold:
```bash
grep -c "placeholder\|scaffold\|TODO\|Phase 1 will" .claude/commands/agency/quick.md
```
Should return 0.
  </verify>
  <done>/agency:quick command is fully functional with 7-step process: parse task, load context, select agent, construct prompt, spawn and execute, display results, offer commit. Lightweight single-agent execution outside the phase workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md to mark STATUS requirements complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update `.planning/REQUIREMENTS.md` to mark STATUS-01 through STATUS-04 as complete (change `- [ ]` to `- [x]`).

Only update the STATUS requirements — do not modify any other sections.
  </action>
  <verify>
```bash
grep -c "\- \[x\] STATUS" .planning/REQUIREMENTS.md
```
Should return 4.
  </verify>
  <done>STATUS-01 through STATUS-04 marked as complete in REQUIREMENTS.md.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.claude/commands/agency/quick.md` is no longer a scaffold — has full 7-step process
- [ ] Command references workflow-common and agent-registry in execution_context
- [ ] Command references PROJECT.md and STATE.md in context
- [ ] Process includes argument parsing with usage help on empty input
- [ ] Process follows agent-registry Section 3 recommendation algorithm
- [ ] Agent confirmation uses AskUserQuestion with recommended + alternative + autonomous options
- [ ] Personality injection follows workflow-common pattern (read .md, construct prompt, spawn)
- [ ] Agent spawned with model: "sonnet" per cost profile
- [ ] Results displayed with summary, files changed, and any issues
- [ ] Commit step is optional, uses AskUserQuestion, follows Conventional Commits
- [ ] Quick tasks do NOT update STATE.md or ROADMAP.md
- [ ] Works with or without an initialized project
- [ ] Agent and AskUserQuestion are in allowed-tools
- [ ] STATUS requirements marked complete in REQUIREMENTS.md
- [ ] No placeholder text or scaffold remnants
</verification>

<success_criteria>
- /agency:quick is a complete, executable command (not a scaffold)
- The 7-step process covers parsing, agent selection, execution, and result reporting
- Agent selection reuses the registry's recommendation algorithm
- Users can confirm, override, or skip personality injection
- The agent receives full personality plus task context
- Results are clearly displayed with actionable summaries
- Commits are optional and properly formatted
- Quick tasks are lightweight — no phase state updates, no Teams overhead
- Works both inside and outside an initialized project
</success_criteria>

<output>
After completion, create `.planning/phases/06-status-quick/06-02-SUMMARY.md`

Then update `.planning/STATE.md`:
- Phase: 6 of 6 (planned)
- Status: Phase 6 planned — 2 plans in 1 wave (parallel)
- Last Activity: Phase 6 planning (2026-03-01)
- Next Action: Run `/agency:build` to execute Phase 6: Status & Quick

And update `.planning/ROADMAP.md` progress table:
- Phase 6: Plans = 2, Completed = 0, Status = Planned
</output>
