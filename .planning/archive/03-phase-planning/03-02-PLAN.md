---
phase: 03-phase-planning
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .claude/commands/agency/plan.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PLAN-01, PLAN-03]
user_setup: []

must_haves:
  truths:
    - "/agency:plan loads the phase-decomposer skill and follows its full process"
    - "The command parses $ARGUMENTS for phase number or auto-detects the next unplanned phase"
    - "Agent recommendations are presented to the user before plan files are written"
    - "AskUserQuestion is used for agent confirmation with confirm/swap/adjust options"
    - "The command checks for existing plans and warns before overwriting"
  artifacts:
    - path: ".claude/commands/agency/plan.md"
      provides: "Fully functional phase planning command"
      min_lines: 60
      contains: "phase-decomposer"
  key_links:
    - from: ".claude/commands/agency/plan.md"
      to: ".claude/skills/agency/phase-decomposer.md"
      via: "execution_context reference"
      pattern: "@./.claude/skills/agency/phase-decomposer.md"
    - from: ".claude/commands/agency/plan.md"
      to: ".claude/skills/agency/agent-registry.md"
      via: "execution_context reference for agent recommendations"
      pattern: "agent-registry"
    - from: ".claude/commands/agency/plan.md"
      to: ".planning/ROADMAP.md"
      via: "context reference for phase details"
      pattern: "ROADMAP.md"
---

<objective>
Update `/agency:plan` from its Phase 1 scaffold into a fully functional command that decomposes a roadmap phase into wave-structured plans with agent recommendations.

Purpose: Make `/agency:plan N` the complete planning entry point — a user types the command and walks through phase analysis, agent selection, and plan file generation.
Output: Updated `.claude/commands/agency/plan.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-phase-planning/03-CONTEXT.md
@.planning/phases/03-phase-planning/03-01-SUMMARY.md

Relevant source files:
@.claude/skills/agency/workflow-common.md
@.claude/skills/agency/agent-registry.md
@.claude/skills/agency/phase-decomposer.md
@.claude/commands/agency/plan.md
@.claude/commands/agency/start.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update /agency:plan command to full implementation</name>
  <files>.claude/commands/agency/plan.md</files>
  <action>
Replace the scaffold content in `.claude/commands/agency/plan.md` with a fully functional command. Keep the existing frontmatter fields but update as needed.

The updated command should have:

**Frontmatter** (update from scaffold):
```yaml
---
name: agency:plan
description: Plan a specific phase with agent recommendations and wave-structured tasks
argument-hint: <phase-number>
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, AskUserQuestion]
---
```

**execution_context** — Load all required skills (uncomment phase-decomposer):
```
@./.claude/skills/agency/workflow-common.md
@./.claude/skills/agency/agent-registry.md
@./.claude/skills/agency/phase-decomposer.md
```

**context** — Reference state files:
```
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
```

**process** — Fully specified steps (not placeholders):

```
1. PARSE PHASE NUMBER
   - Read $ARGUMENTS for a phase number (e.g., "3" from `/agency:plan 3`)
   - If no phase number given: auto-detect the next unplanned phase
     - Read ROADMAP.md progress table
     - Find the first phase where Status = "Pending" and Completed = 0
     - Use that phase number
   - Validate: phase number must exist in ROADMAP.md
   - If invalid: error with "Phase {N} doesn't exist. ROADMAP.md has {count} phases."

2. CHECK FOR EXISTING PLANS
   - Construct the phase directory path using workflow-common conventions:
     `.planning/phases/{NN}-{phase-slug}/`
   - Check if any {NN}-{PP}-PLAN.md files already exist in that directory
   - If plans exist: use AskUserQuestion
     - "Phase {N} already has {count} plan(s). What would you like to do?"
     - Option 1: "Re-plan from scratch" — delete existing plans, proceed
     - Option 2: "Keep existing plans" — abort, suggest /agency:build instead
   - If no plans exist: proceed directly

3. READ PHASE DETAILS
   Follow phase-decomposer skill Section 2 (Phase Analysis):
   - Read ROADMAP.md and extract phase goal, requirements list, success criteria
   - Read REQUIREMENTS.md and cross-reference full requirement descriptions
   - Read PROJECT.md for broader context
   - Read STATE.md for current progress and completed phase outputs
   - If this phase builds on prior phases, read prior phase summaries

4. DECOMPOSE INTO PLANS
   Follow phase-decomposer skill Section 3 (Task Decomposition):
   - List all deliverables from phase requirements
   - Identify dependency layers
   - Map layers to waves
   - Group deliverables into plans (max 3 tasks per plan)
   - Validate: every requirement covered, no circular dependencies

5. RECOMMEND AGENTS
   Follow phase-decomposer skill Section 4 (Agent Recommendation):
   - For each plan, analyze task types and match against agent-registry
   - Score and rank candidates
   - Select 1-2 agents per plan (or mark as autonomous)
   - Apply mandatory roles (testing for code, coordinator for cross-division)

6. PRESENT TO USER FOR CONFIRMATION
   Follow phase-decomposer skill Section 5 (User Confirmation Gate):
   - Display the complete plan breakdown with wave structure
   - Show agent recommendations with rationale per plan
   - Show agent summary table
   - Use AskUserQuestion: "Does this plan breakdown look right?"
     - "Looks good, generate the plans" — proceed
     - "Swap an agent" — ask which plan, present alternatives, update
     - "Adjust the plan structure" — discuss changes, revise decomposition
   - Loop until user confirms

7. GENERATE CONTEXT FILE
   Follow phase-decomposer skill Section 7 (Context File Generation):
   - Create `.planning/phases/{NN}-{phase-slug}/` directory
   - Write `{NN}-CONTEXT.md` with phase goal, requirements, existing assets, decisions, plan structure

8. GENERATE PLAN FILES
   Follow phase-decomposer skill Section 6 (Plan File Template):
   - For each plan, generate a `{NN}-{PP}-PLAN.md` file
   - Include complete YAML frontmatter (phase, plan, wave, depends_on, must_haves, etc.)
   - Include all XML sections (objective, context, tasks, verification, success_criteria, output)
   - Each task has detailed action instructions, verify commands, and done sentence
   - Context references include prior plan summaries for wave 2+ plans

9. UPDATE STATE
   - Read current .planning/STATE.md
   - Update:
     - Phase: {N} of {total} (planned)
     - Status: Phase {N} planned — {plan_count} plans across {wave_count} waves
     - Last Activity: Phase {N} planning ({date})
     - Next Action: Run `/agency:build` to execute Phase {N}: {phase_name}
   - Write updated STATE.md

10. DISPLAY SUMMARY
    - Show the user a concise summary:
      - Phase: {N} — {phase_name}
      - Plans: {count} plans across {wave_count} waves
      - For each plan: name, wave, assigned agent(s)
      - Files to be created: list the plan file paths
    - End with: "Run `/agency:build` to execute Phase {N}: {phase_name}"
    - Do NOT dump full plan file contents — summary only
```

IMPORTANT:
- The process steps must be detailed enough that Claude can execute them without ambiguity
- Each step references the specific skill section it follows
- The command is a complete workflow — from phase number to generated plan files
- AskUserQuestion is used for structured choices (existing plans, agent confirmation)
- The command must handle the auto-detect case (no phase number argument)
  </action>
  <verify>
Verify the command file is updated:
```bash
grep "phase-decomposer" .claude/commands/agency/plan.md
```
Should match the execution_context reference.

Verify it's no longer a scaffold:
```bash
grep -c "Phase 3" .claude/commands/agency/plan.md
```
Should return 0 (no more "Phase 3 will implement this" placeholders).

Verify AskUserQuestion is in allowed-tools:
```bash
grep "AskUserQuestion" .claude/commands/agency/plan.md
```
Should match.

Verify it has at least 10 process steps:
```bash
grep -c "^[0-9]\+\." .claude/commands/agency/plan.md
```
Should return 10.
  </verify>
  <done>/agency:plan command is fully functional with 10-step process: parse phase, check existing, read details, decompose, recommend agents, confirm with user, generate context, generate plans, update state, display summary.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md to mark PLAN requirements complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update `.planning/REQUIREMENTS.md` to mark PLAN-01 through PLAN-05 as complete (change `- [ ]` to `- [x]`).

Only update the PLAN requirements — do not modify any other sections.
  </action>
  <verify>
```bash
grep -c "\- \[x\] PLAN" .planning/REQUIREMENTS.md
```
Should return 5.
  </verify>
  <done>PLAN-01 through PLAN-05 marked as complete in REQUIREMENTS.md.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.claude/commands/agency/plan.md` is no longer a scaffold — has full 10-step process
- [ ] Command references all 3 skills: workflow-common, agent-registry, phase-decomposer
- [ ] Command references all 4 state files in context section (PROJECT, ROADMAP, STATE, REQUIREMENTS)
- [ ] Process includes phase number parsing with auto-detect fallback
- [ ] Process includes existing plan check with AskUserQuestion
- [ ] Process follows phase-decomposer sections in order (analysis → decomposition → recommendation → confirmation → generation)
- [ ] Process includes context file generation before plan files
- [ ] Process includes state update and summary display
- [ ] AskUserQuestion is in allowed-tools
- [ ] PLAN requirements marked complete in REQUIREMENTS.md
- [ ] No placeholder references to "Phase 3" remain in plan.md
</verification>

<success_criteria>
- /agency:plan is a complete, executable command (not a scaffold)
- The 10-step process covers the full flow from phase number to generated plan files
- Skills are properly wired via execution_context references
- Agent recommendation and user confirmation gates are integrated
- Auto-detect works when no phase number is given
- Requirements tracking is updated
</success_criteria>

<output>
After completion, create `.planning/phases/03-phase-planning/03-02-SUMMARY.md`

Then update `.planning/STATE.md`:
- Phase: 3 of 6 (planned)
- Status: Phase 3 planned — 2 plans across 2 waves
- Last Activity: Phase 3 planning (2026-03-01)
- Next Action: Run `/agency:build` to execute Phase 3: Phase Planning

And update `.planning/ROADMAP.md` progress table:
- Phase 3: Plans = 2, Completed = 0, Status = Planned
</output>
